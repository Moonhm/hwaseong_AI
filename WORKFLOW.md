# WORKFLOW.md — 화성잇다 개발 규칙·현황

> **이 문서는 「규칙과 현재 상태」만 담습니다. 과거 작업 기록은 `docs/log/` 에 있습니다.**
> 2026-08-25 에 1,359줄 → 542줄로 나눴습니다. 이유와 방법은 아래 「문서 구조」를 보십시오.

---

## 0. 문서 구조 — 새 세션이 가장 먼저 읽을 것

### 무엇이 어디 있나

| 위치 | 담는 것 | 어떻게 바뀌나 |
|------|---------|--------------|
| **`WORKFLOW.md`** (이 파일) | 규칙 · 현재 상태 · 담당 분담 · 체크리스트 | **제자리에서 수정.** 낡은 내용은 고치고, 새 절은 잘 안 늘어난다 |
| **`docs/log/`** | 작업 기록 (무엇을 왜 어떻게 했나) | **새 파일만 추가.** 기존 파일은 고치지 않는다 |

#### ⚠ 끝난 판단은 반드시 이 파일로 올리십시오 (2026-08-30 신설)

`docs/log/` 는 **시간순**이라 나중 결론이 앞 기록을 덮어 주지 않습니다.
읽는 사람은 검색으로 걸린 것을 먼저 읽지, 날짜순으로 읽지 않습니다.

실제로 이렇게 됐습니다.

```
08-24  감사가 "API 키 평문 노출 — 폐기·재발급 권고" 를 로그에 적었다
08-25  사용자가 "만료일 걸어 뒀다. 조치 불필요" 로 종결 — 그런데 그 결론도 로그에만 적혔다
08-30  다음 감사가 08-24 로그를 먼저 찾아 같은 건을 '심각' 으로 다시 올렸다
```

**사용자를 두 번 귀찮게 한 것이 비용입니다.** 로그는 "그때 무슨 일이 있었나" 이고,
`WORKFLOW.md` 는 "지금 무엇이 참인가" 입니다. **종결·기각·보류 판단은 후자입니다.**

#### ⚠ 이 파일은 아무도 안 고치면 조용히 썩습니다

`docs/log/` 는 새 파일만 만드니 낡을 수가 없는데, 이 파일은 '제자리 수정' 이라
**고치는 사람이 없으면 그냥 틀린 채로 남습니다.** 2026-08-30 전수 감사에서
현황 기술 **11건이 코드와 어긋나** 있었고, 그중엔 `css/` 디렉토리가 있는데
「별도 `css/` 디렉토리 없음」 이라고 적힌 것처럼 **같은 문서 안에서 모순**인 것도
있었습니다. 구조를 바꾸는 작업(파일 분리·이동·이름 변경)을 했으면
**그 커밋에서 이 파일의 해당 줄까지 고치십시오.**

### ⚠ 작업 기록은 이 파일에 쓰지 마십시오

**새 작업을 끝냈으면 `docs/log/` 에 새 파일을 만드십시오.** 이 파일 끝에 절을 덧붙이지 마십시오.

```
docs/log/{YYYY-MM-DD}-{dev|deploy|both}-{주제}.md
   예: 2026-08-26-dev-home-layout.md
       2026-08-26-deploy-photo-batch.md
```

**목록은 `ls docs/log/` 로 봅니다. 손으로 관리하는 목차를 만들지 마십시오** —
그 목차 자체가 둘이 동시에 고치는 새 충돌 지점이 됩니다. 파일명에 날짜·담당·주제가
다 들어 있어 `ls` 결과가 곧 시간순 목차입니다.

### 기록은 작업과 **같은 커밋**에 넣으십시오

나중에 몰아 쓰지 마십시오. 기억이 흐려진 뒤라 "무엇을 했다"만 남고
**"왜 그렇게 했는지"와 "무엇을 안 했는지"가 빠집니다.** 그 둘이 기록의 값어치 전부입니다.

실제로 2026-08-26 에 감사해 보니 **커밋은 있는데 로그가 없는 것이 3건** 있었습니다
(`690e6f8` 하단 내비 · `45d82a8` 문서 분리 · 11개 팀 비교 조사).
셋 다 하루 지나서 되짚어 썼고, 그만큼 부실합니다.

조사·분석처럼 **코드를 남기지 않는 작업이 특히 위험합니다.** 커밋할 것이 로그뿐이라
"나중에 쓰지" 하고 넘어가기 쉬운데, 그러면 조사 전체가 통째로 사라집니다.
11개 팀 저장소를 클론해 전부 읽은 작업이 정확히 그럴 뻔했습니다.

#### 다만 모든 변경에 로그 파일을 만들지는 마십시오

로그 파일은 **판단이 들어간 변경**에만 만듭니다.

| 변경 | 기록 위치 |
|------|----------|
| 문구·숫자·색 같은 **작은 조정** | **커밋 메시지로만.** 로그 파일 만들지 않음 |
| 구조·동작·새 기능 등 **판단이 들어간 것** | **로그 파일까지** |

#### 로그 머리에 커밋 해시를 적으십시오

```
- 커밋: `fe83e81`            (여러 개면 `a1b2c3d` · `e4f5g6h`)
```

**"이 커밋에 로그가 있나" 를 기계로 확인할 수 있어야 합니다.** 2026-08-29 에
그 확인을 해 보니 양방향으로 다 틀렸습니다.

| | 무슨 일 | 결과 |
|---|---|---|
| 거짓 양성 | 다른 문서가 그 해시를 **예시로 인용**만 했다 | 로그가 있는 줄 알았다 |
| 거짓 음성 | 그 커밋이 **주제인 문서**인데 해시가 안 적혀 있다 | 로그가 없는 줄 알았다 |

머리에 `- 커밋:` 한 줄만 있으면 둘 다 사라집니다.

##### 그 해시가 `amend` 나 `rebase` 로 죽지 않게 하십시오

규칙을 만든 그날(2026-08-29) 쓴 로그 두 건이 곧바로 이 함정을 밟았습니다.
절차가 ① `git commit` → ② 나온 해시를 로그에 적음 → ③ `git commit --amend` 였는데,
**③ 이 해시를 바꿉니다.** 로그에는 ① 의 해시가 남습니다.

| 로그 | 적혀 있던 해시 | 실제 |
|---|---|---|
| `2026-08-29-dev-unified-search.md` | `103dcdc` | `62d9fe9` |
| `2026-08-29-dev-tide-midnight-crossing.md` | `c1ec97a` | `fe4ec70` |

버려진 해시는 **로컬 `reflog` 에만 남습니다.** push 도 clone 도 그 객체를 나르지
않으므로 저장소를 받은 사람에게는 아예 없는 해시이고, `git gc` 가 한 번 돌면
이 컴퓨터에서도 사라집니다. 7자리 16진수는 눈으로 봐서는 다 똑같이 생겨
검토로 걸러지지 않습니다. **규칙을 지켰는데 지키기 전과 똑같이 틀린 답이 나옵니다.**

##### `amend` 만이 아닙니다 — **`rebase` 가 더 자주 터집니다** (2026-08-30 확대)

위 규칙은 원인을 `git commit --amend` 로 특정했는데, **그건 좁았습니다.**
같은 날 배포 Claude가 **amend 를 한 번도 하지 않고** 같은 사고를 냈습니다.

```
① git commit                    → 9be0bd9
② git pull --rebase             → 충돌 7건, 해결 후 continue
③ 리베이스가 커밋을 다시 만든다  → a7c46c0   ← 해시가 바뀌었다
```

amend 는 **선택**이지만, `pull --rebase` 는 §15 가 정한 **기본 절차**입니다.
둘이 같은 저장소를 쓰므로 **상대가 그 사이에 push 했으면 내 해시는 반드시 바뀝니다.**
이쪽이 훨씬 자주 터집니다.

##### 그래서 순서는 이것 하나뿐입니다

```
git commit  →  git pull --rebase  →  git push  →  여기서 git log -1 --format=%h
            →  그 해시를 로그에 적기  →  커밋  →  push
```

**`push` 가 끝나야 해시가 원격에 고정됩니다. 그전에 읽은 값은 전부 잠정입니다.**

「뒤따르는 커밋에 넣는다」만으로는 부족합니다 — 채워 넣은 커밋을 push 하기 전에
`pull --rebase` 가 한 번 돌면 **앞 커밋의 해시까지 같이 바뀌어** 방금 적은 값이
또 죽습니다. **push 뒤에 읽는 것**이 이 순서의 핵심입니다.

정리하면 이렇습니다.

- **해시는 push 뒤에 읽어 적는다.** 그 수정은 뒤따르는 커밋에 넣는다
- **고치겠다고 또 amend 하지 않는다.** 또 어긋난다
- amend·rebase 할 생각이면 **모양이 확정된 뒤 한 번만** 적는다

`tools/check.sh` 의 **log-hash** 검사가 이제 이것을 잡습니다 — 없는 해시와
`HEAD` 에서 도달 못 하는 해시를 파일:줄로 찍습니다. **규칙만 있고 검사가 없었던 것이
두 건이 그대로 커밋된 이유입니다.**

> **이때만은 기존 로그 파일을 고쳐도 됩니다.** 위 표의 「기존 파일은 고치지 않는다」는
> *지난 기록을 덧쓰거나 다시 쓰지 말라*는 뜻이지, 틀린 사실을 그대로 두라는 뜻이
> 아닙니다. 가리키는 커밋이 없는 해시는 §0 이 세운 목적 자체를 무너뜨립니다.

#### ⚠ 줄번호로 가리키지 마십시오 — `파일:줄` 은 남이 고치면 조용히 밀립니다 (2026-08-30 신설)

커밋 해시와 **똑같은 종류의 함정**인데, 이쪽이 더 자주 터집니다.
해시는 amend·rebase 를 해야 바뀌지만, **줄번호는 남이 그 파일 위쪽에 한 줄만 넣어도 밀립니다.**
게다가 밀린 줄번호는 여전히 **존재하는 줄**을 가리키므로 「없는 참조」로 안 걸립니다.
엉뚱한 코드를 자신 있게 가리키는 상태가 됩니다.

바로 그날 실측된 예입니다.

```
08-30 오전  개발 Claude 가 전수 감사로 문서를 코드에 맞춤 — 줄번호 그대로 적음
08-30 오후  배포 Claude 가 축제 숨김 작업 (calendar.js +24줄, home/tourism/living/index.html)
08-30 저녁  같은 날 안에 5건이 밀었다 ↓
```

| 문서가 가리킨 곳 | 실제로 그게 있는 곳 | 밀린 자리에 있던 것 |
|---|---|---|
| `js/calendar.js` 264 (`hasFest`) | 288 | `removeChild` 루프 |
| `js/tourism.js` 244 (스태거 `0.045s`) | 323 | 주석 |
| `js/home.js` 189 (`AIRKOREA_KEY`) | 190 | 주석 구분선 |
| `js/parking.js` 292 (`ℹ`) | 366 | — |
| `index.html` 136 (`©`) | 202 | `</div>` |

**하루입니다.** 감사로 맞춰 놓은 것이 반나절 만에 어긋났습니다.

##### 그래서 이렇게 씁니다

```
✗  `js/calendar.js:288` 이 has-event 를 붙인다
✓  `js/calendar.js` 가 `hasFest` 로 has-event 를 붙인다
```

**식별자(함수명·변수명·문자열)로 가리키십시오.** 남이 위에 줄을 넣어도 안 밀리고,
`grep -rn "hasFest" js/` 한 번이면 찾아집니다. 줄번호보다 오히려 빠릅니다.

지난 일을 서술할 때도 마찬가지입니다 — 「그때 140번째 줄이 `},,` 였다」는
지금 140번째 줄과 아무 상관이 없어서 읽는 사람을 헷갈리게 합니다.
**그 자리에 무엇이 있었는지**(`id:227` 항목)를 적으십시오.

`tools/check.sh` 의 **line-ref** 검사가 이제 이것을 잡습니다 — 백틱으로 감싼
`파일:줄` 을 전부 FAIL 로 막습니다. 규칙만 두면 어떻게 되는지는 바로 위 표가 보여 줍니다.
§27-H 는 2026-08-30 오전에 이미 「줄번호로 가리키지 말고 함수명으로」라고
적어 두었는데, **검사가 없어서 같은 날 오후에 5건이 밀렸습니다.**

> 예외는 없습니다. 줄번호를 꼭 인용해야 하면 **백틱 밖에** 평문으로 적으십시오
> (예: `tools/check.sh` 의 187번째 줄). 백틱은 '지금 거기 있다'는 표기입니다.

##### 검사가 보는 범위 — 여기가 전부는 아닙니다

| 대상 | 검사 | 왜 |
|---|---|---|
| `WORKFLOW.md` · `README.md` · `tools/check.sh` | ✅ FAIL | 지금 사실을 말하는 문서다. 틀리면 바로 사람을 잘못 보낸다 |
| ` ``` ` 코드블록 안 | 제외 | 안 그러면 **「이렇게 쓰지 마라」는 반례조차 못 적습니다** |
| `js/` · `css/` 주석 · `index.html` | ✅ FAIL | **2026-08-31 에 74건을 전부 정리해서 검사 대상에 넣었습니다** |
| `docs/log/` 전체 | ❌ 안 봄 | 로그는 **그때의 기록**입니다. §0 이 「고치지 않는다」로 못박았습니다 |

**초록불을 「줄번호 참조가 어디에도 없다」로 읽지 마십시오.** `docs/log/` 는 안 봅니다.
로그의 줄번호는 **그때의 기록**이라 고치지 않는 것이 규칙이고, 검사에 넣으면
상시 FAIL 이 됩니다 — **빨간불이 상수가 되면 그 검사는 곧 무시되거나 지워집니다.**

#### ⚠ 증상에서 원인을 추측했으면, 재기 전에는 원인으로 적지 마십시오 (2026-08-31 신설)

앞의 두 규칙(해시·줄번호)은 **낡아서** 틀리는 경우입니다. 이건 **처음부터** 틀리는 경우입니다.

2026-08-30 에 실제로 있었던 일입니다.

```
관찰   메모리를 남겼는데 재부팅 뒤 그 디렉터리가 비어 있었다        ← 사실
추측   그러니 ~/.claude/projects/ 는 영속이 아니다                  ← 안 잼
결과   §12 에 「메모리는 인계 수단이 아니다」를 규칙으로 세움
실측   findmnt 한 줄 — /dev/sdb1 에 걸린 별도 마운트였다. 영속이 맞다
       게다가 14일 전 파일 4개가 그 재부팅을 멀쩡히 넘겨 있었다
```

한 칸을 건너뛰었습니다. 그 한 칸이 **명령 한 줄**이었습니다.

이 문서에서 이게 특히 비쌉니다. §0 의 전제가 「**`WORKFLOW.md` 는 지금 사실을
말한다**」이기 때문입니다. 틀린 사실이 규칙이 되면 **그 뒤로는 아무도 안 잽니다.**
규칙으로 적힌 것을 다시 재 보는 사람은 없습니다.

- 증상은 증상으로 적으십시오 — 「비어 있었다」는 사실이니 그대로 씁니다.
- 원인은 **잰 뒤에** 적고, **어떻게 쟀는지**(명령 한 줄)를 같이 남기십시오.
- 못 쟀으면 **「원인 미상」이라고 적으십시오.** 빈칸이 틀린 답보다 낫습니다 —
  빈칸은 다음 사람이 채우지만, 틀린 답은 다음 사람을 멈춰 세웁니다.

##### 그리고 **어디서 쟀는지**를 적으십시오 (2026-08-31 저녁 추가)

같은 날 이 규칙이 **세 번** 걸렸습니다. 셋 다 「재라」는 지켰는데도 틀렸습니다.

| | 잰 것 | 왜 그래도 틀렸나 |
|---|---|---|
| ① `~/.claude/projects` 영속 | `findmnt` | — (이건 맞았습니다) |
| ② `tools/server.py` diff | `git status`·`mtime`·`find` 여섯 가지 | **내 컨테이너만** 봤습니다 |
| ③ rebase 를 막던 파일 | 재현 테스트 | 원인을 한 파일로 좁혔습니다 |

②가 특히 뼈아픕니다. 여섯 가지를 재고 전부 「없다」가 나와서 **「그런 diff 는
없습니다」로 단정**했는데, 배포 컨테이너에는 실물이 있었습니다.
`find /home/jovyan …` 이 1건을 낸 것을 「저장소 사본 하나뿐」이라고 읽은 것입니다 —
그 명령은 **상대 컨테이너를 볼 수 없습니다.**

**두 Claude 는 같은 작업트리를 쓰지 않습니다.** 경로 문자열만 같은 별개 사본이고,
`git` 이 나르는 것은 **커밋된 객체뿐**입니다. 그래서 상대의 미커밋 변경은
`git status`·`git stash list`·`git log` 어느 것으로도 **원리상 안 보입니다.**

```
✗  그런 diff 는 없습니다 (git status·stash·mtime 전부 확인)
✓  개발 컨테이너에는 없습니다. 배포 쪽은 그쪽에서 재 주십시오
```

- **환경 사실**(파일 존재·프로세스·마운트·설치 여부)은 **자기 컨테이너에서** 재고,
  **잰 곳을 같이 적으십시오.** 상대 쪽 실측을 그대로 옮기지 마십시오.
- 상대 환경을 단정해야 할 때는 **묻고 출력을 받으십시오.** 실제로 그렇게 해서
  풀렸습니다 — 「그 출력을 그대로 올려 달라」가 이 건을 뒤집었습니다.
- **저장소를 통해 공유되는 것만이 공통 사실입니다.** 커밋된 파일·이력이 그것이고,
  워킹트리·`.git/config`·설치분·프로세스는 **각자의 것**입니다.

> 되돌린 경위: [`docs/log/2026-08-31-dev-memory-persistence-correction.md`](docs/log/2026-08-31-dev-memory-persistence-correction.md)
> · [`docs/log/2026-08-31-deploy-phantom-diff-answer.md`](docs/log/2026-08-31-deploy-phantom-diff-answer.md)
> · [`docs/log/2026-08-31-dev-scope-of-measurement.md`](docs/log/2026-08-31-dev-scope-of-measurement.md)

기준은 「나중에 누가 **왜 그랬는지 궁금해할 것인가**」입니다.
`'통합 정보' → '통합 정보 플랫폼'` 에는 궁금할 '왜'가 없습니다 — 커밋 메시지면 충분합니다.
반면 `화면 전환을 전체 폭이 아니라 18% 로 한 이유` 는 적어 두지 않으면 다음 사람이
"왜 어중간하지" 하고 100% 로 바꿔 놓습니다.

**디자인을 반복해서 다듬는 구간에서는 특히 주의하십시오.**
한 번 고칠 때마다 파일을 만들면 중간 시안이 스무 개씩 쌓여 오히려 읽기 나빠집니다.
그 구간은 **커밋 메시지로만 남기고, 모양이 확정된 뒤 결과를 한 번에 정리**하는 편이 낫습니다.

**왜 이렇게 하나** — 개발 Claude와 배포 Claude가 둘 다 이 파일 끝에 절을 붙이면서 **번호가 겹쳐 매번 충돌**했습니다(실제로 2026-08-25 에 §23 이 겹쳤습니다). 각자 **새 파일**을 만들면 같은 곳을 동시에 고칠 일이 없어 충돌이 **구조적으로 불가능**해집니다. 파일명에 날짜와 담당(`dev`/`deploy`)이 들어가므로 이름이 겹칠 일도 없습니다.

부수 효과로 새 세션의 진입 비용이 3분의 1이 됩니다 — 예전엔 1,359줄을 다 읽어야 「필독」 규칙에 닿았습니다.

### 번호는 그대로 둡니다

`docs/log/` 로 옮긴 절도 **이 파일에 번호와 이정표 한 줄을 남겨** 뒀습니다. 그래서 코드 주석의 `WORKFLOW.md §19` 같은 기존 참조가 전부 그대로 동작합니다. **절 번호를 다시 매기지 마십시오** — 코드 여러 곳이 번호로 이 문서를 가리킵니다.

### 충돌이 났을 때

| 충돌 지점 | 해결 규칙 |
|-----------|----------|
| `index.html` 의 `?v=` 값 | **큰 숫자를 채택한다.** 기계적으로 결정되므로 고민하지 말 것 |
| `index.html` 의 `<script src>` 목록 | **양쪽 다 살린다.** 각자 추가한 파일을 모두 남기고 순서만 확인 |
| `WORKFLOW.md` | 위 규칙을 지키면 안 난다. 났다면 둘 중 하나가 기록을 여기 쓴 것이다 |
| `docs/log/` | 구조상 안 난다 |

#### ⚠ 충돌 해결과 `git add` 를 한 명령에 이어 붙이지 마십시오

**같은 사고가 두 번 났습니다.** 2026-08-26 `85a09a1`, 2026-08-27 `1f6913e`(복구 `2b27bbc`).
둘 다 **충돌 마커가 그대로 든 `index.html` 이 커밋·푸시**됐습니다. 두 번째는
`js/datalab.js` 가 문법 오류로 통째로 죽은 채 배포까지 나갔습니다.

원인은 같습니다. 충돌을 푸는 스크립트와 뒤처리를 한 줄로 이었습니다.

```bash
# ✗ 이렇게 하지 마십시오 — 스크립트가 죽어도 뒤 두 줄이 그대로 돕니다
python3 resolve.py && git add index.html && git rebase --continue && git push
```

`&&` 를 써도 안전하지 않습니다. 셸 한 호출 안에서 줄바꿈으로 이어 놓으면
앞이 실패해도 뒤가 실행됩니다. **반드시 나눠서, 확인을 끼워 넣으십시오.**

```bash
# ✓ 1) 충돌 해결만 한다
python3 resolve.py
# ✓ 2) 마커가 남았는지 직접 본다 (0 이어야 한다)
grep -c '<<<<<<<\|>>>>>>>' index.html js/*.js css/*.css
# ✓ 3) 문법을 본다
bash tools/check.sh; echo "exit: $?"
# ✓ 4) 그 다음에야 add / continue / push
```

**`tools/check.sh` 를 push 와 같은 호출에 묶지 마십시오.** 두 번째 사고 때
`check.sh` 는 제 몫을 했습니다(`datalab.js` 문법 오류를 FAIL 2 로 찍었습니다). 그런데
push 가 같은 호출에 묶여 있어 **결과를 읽기 전에 이미 나갔습니다.**
검사는 판정을 보고 사람이 다음 단계를 정하라고 있는 것입니다.

**해결 스크립트의 전제를 의심하십시오.** 두 사고 모두 "양쪽이 `?v=` 만 다르다"는
전제를 깔고 자동 치환했는데, 실제로는 새 `<script src>` 줄이 추가돼 있었습니다
(`js/weather.js`). 그리고 두 번째 사고의 직접 원인은 정규식 한 글자였습니다 —
버전이 `20260826141` 처럼 **11자리**인데 `2026\d{6}` 으로 10자리만 잡아
`'141'→'V1'` / `'140'→'V0'` 로 갈려 비교가 어긋났습니다. `\d{11}` 로 잡으십시오.

---

## 1. 프로젝트 개요

- **앱 이름**: 화성잇다 (Hwaseong-itda)
- **목표**: 경기도 화성특례시 통합 관광 정보 모바일 웹앱
- **저장소**: https://github.com/Moonhm/hwaseong_AI
- **배포 URL**: https://news-appliances-tap-cab.trycloudflare.com
- **팀**: 문형민, 서교연 + Claude Sonnet 4.6 (AI 개발 에이전트 2인 체계)
- **기간**: 2026년 8월 18~19일 (해커톤 당일 집중 개발)

---

## 2. 양방향 Claude 워크플로우 (핵심 협업 구조)

이 프로젝트의 가장 독특한 점은 **두 개의 Claude AI 인스턴스가 GitHub을 매개로 비동기 협업**한 것입니다.

### 역할 분리

```
┌────────────────────────────────────────────────────────────────┐
│  개발 Claude (Development Claude)                              │
│  ─────────────────────────────────────                         │
│  · git fetch로 최신 코드 확인 후 작업                           │
│  · index.html / map.js / parking.js 등 코드 작성·수정          │
│  · git push → 커밋 메시지로 변경 사항 기록                      │
│  · README [메시지함]으로 배포 Claude에게 메시지 전달            │
└────────────────────────────────────────────────────────────────┘
                              ⬇ push / ⬆ pull
                         GitHub (origin/main)
                              ⬇ pull / ⬆ push
┌────────────────────────────────────────────────────────────────┐
│  배포 Claude (Deployment Claude)                               │
│  ─────────────────────────────────────                         │
│  · Flask 서버 + Cloudflare Tunnel로 실시간 서빙                 │
│  · assets/images/places/ 장소 사진 로컬 관리 (git 제외, 수치는 §18) │
│  · 실제 앱 테스트 → 버그 발견 시 직접 수정 + push              │
│  · README [메시지함]으로 개발 Claude에게 답장                   │
└────────────────────────────────────────────────────────────────┘
```

### 소통 채널

- **커밋 메시지와 `docs/log/`** 가 실제 채널입니다. 상대는 `git pull` 로 그것을 읽습니다
- 사용자(문형민)가 두 Claude를 중계 (메시지를 양쪽에 전달)
- 인계 건은 `WORKFLOW.md` 의 「인계」 표에 적습니다 (§17 좌표·데이터, §27-G·§27-H)

> `README.md` 의 `[Claude 간 메시지함]` 은 **없어졌습니다.** §15 가 README 를
> 서비스 소개 전용으로 못박으면서 사라졌는데 이 절만 그대로 남아 있었습니다
> (2026-08-30 정정). README 에서 메시지함을 찾지 마십시오.

### 핵심 규칙 (충돌 방지)

| 규칙 | 이유 |
|------|------|
| 작업 전 반드시 `git pull --rebase` | 두 인스턴스가 동시에 push → rebase 충돌 방지 |
| 사진·대용량 파일 절대 push 금지 | 저장소 용량 초과 방지, 배포 서버 전용 |
| 커밋 메시지에 변경 이유 명시 | 상대 Claude가 pull 시 맥락 즉시 파악 |
| ID 범위 규칙 준수 | tourist/festival ID 충돌 방지 |

---

## 3. 기술 스택 및 아키텍처

### 전체 구조

```
Frontend         순수 HTML · CSS · JavaScript (프레임워크 없음)
                 SPA: index.html(959줄) + js/ 23개 + css/ 7개
                 2026-08-25 에 인라인을 분리했다(§20·§21). '단일 파일' 은 그전 이야기다.
Map Engine       Kakao Maps JavaScript SDK v2
                 커스텀 오버레이 · 클러스터 · idle 이벤트 기반 동적 렌더링
Proxy Server     Flask (Python) — 주차장 API CORS 우회
Deployment       Cloudflare Quick Tunnel (cloudflared --url localhost:8080)
AI Workflow      Claude Sonnet 4.6 × 2 인스턴스 (개발 + 배포)
```

### 모바일 우선 설계 원칙

| 항목 | 값 |
|------|----|
| 기준 화면 | 모바일 세로 (portrait) |
| 최대 너비 | 480px |
| 하단 내비게이션 | 52px 고정 |
| 터치 타겟 최소 | 36×36px |
| 본문 대비 최소 | **4.5:1** (WCAG AA). `--text-muted` 는 #6B7280 — 이보다 밝게 올리지 말 것 |
| 입력 글꼴 최소 | **16px**. 이보다 작으면 iOS 가 포커스 시 자동 확대한다. viewport 에서 `user-scalable=no` 를 뺐으므로(2026-08-26) 이 값이 유일한 방어다 |
| 조작부 | `onclick` 을 단 요소는 키보드로 닿아야 한다. `js/boot.js` `_kbEnhance()` 가 자동으로 `tabindex`·`role` 을 붙이므로 **마크업에 직접 쓸 필요는 없다**. 다만 아이콘만 있는 버튼(✕ ‹ ›)에는 `aria-label` 을 직접 달 것 |

### 카테고리 색상 체계

| 카테고리 | 색상 | 지도 핀 | 목록·검색 |
|---------|------|--------|----------|
| tourist (관광지) | `#FB923C` | ★ | 🎡 |
| heritage (문화재) | `#7C3AED` | 🏛️ | 🏛️ |
| restaurant (맛집) | `#D97706` | 🍽️ | 🍽️ |
| festival (축제) | `#DC2626` | 🎉 | 🎉 |
| parking (주차장) | `#2563EB` | 🅿️ | 🅿️ |
| localcurrency (지역화폐) | `#059669` | 💳 | 로고 이미지 |

**관광지만 핀과 목록의 기호가 다릅니다.** 핀은 작은 원이라 컬러 이모지보다
글리프(★)가 또렷해서 그대로 뒀습니다. 목록·검색은 🎡 입니다 (2026-08-26 사용자 지시).
🏛️ 은 **문화재 전용**입니다 — 관광지에 쓰지 마십시오. 둘이 구분되지 않던 걸 갈라놓은 것입니다.

이모지를 바꿀 때 손대야 하는 곳은 **다섯 군데**입니다:
`index.html` About 칩 · `js/home.js` `_CAT_STYLE` · `js/tourism.js` `iconContent()` ·
`js/tourism.js` 목록 사진 폴백 · **`js/favorites.js` `_favCfg()`**.
`js/data.js` `CATEGORY_CONFIG` 는 지도 핀 쪽입니다.

> `_favCfg()` 는 2026-08-26 까지 이 목록에 없었습니다. 그래서 다른 네 곳만 🎡 로
> 바뀌고 **즐겨찾기만 ★ 로 남아** 사용자가 지적했습니다. 같은 날 `heritage` 항목이
> 통째로 없어 문화재 즐겨찾기가 기본값 ★ 로 떨어지던 것도 함께 고쳤습니다.
> 목록에서 한 곳이 빠지면 그 한 곳이 정확히 틀립니다 — 새 표시 지점이 생기면 여기에 더하십시오.

지도 칩 이모지는 2026-08-26 에 전수 대조했습니다. 11개 칩이 `js/conv_map.js`
`CONV_CAT_CFG` 의 핀 이모지와 전부 일치하고, VS16 이 필요한 넷(🏛️ 🍽️ 🅿️ ⛱️)도
모두 붙어 있습니다.

#### ⚠ 이형 선택자(VS16)를 붙여야 하는 이모지

사용자가 🗺️ 와 🏛️ 를 각각 따로 지적하셨습니다(2026-08-26). 원인이 같아서
**나머지도 전수 조사해 한꺼번에 맞췄습니다(45곳).** 아래가 그 결론입니다.

**왜 이런 게 있나** — 이모지 일부는 유니코드 **기본 표시가 `text`** 입니다.
맨몸으로 쓰면 플랫폼에 따라 **흑백 글리프**로 나와, 옆의 🎉 🎡 같은 컬러 이모지와
나란히 놓였을 때 혼자 칙칙해 보입니다. 뒤에 `U+FE0F` 를 붙이면 "컬러로 그려라"가 됩니다.
🎡 🎉 🥢 🏨 ⛺ 등은 기본이 `emoji` 라서 붙일 필요가 없습니다.

**눈으로는 절대 구분이 안 됩니다.** 편집기에서 🏛️ 과 🏛️ 는 똑같이 보입니다.
그래서 "고쳤다"고 착각하기 쉽습니다 — 아래 스크립트로 확인하십시오.

##### 반드시 붙일 것 (화면에 보이는 UI 아이콘)

`🗺️ 🏛️ 🍽️ 🅿️ ⛱️ 🏘️ 🏕️ 🏖️ ⛩️ 🗓️`

##### 일부러 안 붙인 것 — 붙이면 오히려 깨집니다

| 문자 | 어디 | 왜 그대로 두나 |
|------|------|---------------|
| `♥` 6곳 | `favorites.js` `map.js` `parking.js` `localcurrency.js` | `'♥ 저장됨' : '♡ 저장'` **짝**입니다. ♥ 에만 붙이면 빨간 하트가 되고 ♡ 는 흑백으로 남아 대비가 깨집니다 |
| `ℹ` 1곳 | `js/parking.js` 의 `feeNote` 줄 | 인라인 `color:#6366F1` 이 걸려 있습니다. 텍스트 글리프라서 CSS 색이 먹는 것이고, 컬러 이모지가 되면 그 지정이 무시됩니다 |
| `©` 1곳 | `index.html` 푸터 `데이터 출처:` | 저작권 표기 문장 안입니다. 컬러로 만들 이유가 없습니다 |
| `⚠` 3곳 | `home.js` `ui.js` `20-map.css` | **전부 코드 주석**입니다. 화면에 안 나옵니다 |

##### 확인 방법

```bash
python3 - <<'EOF'
import glob, collections
TD = {0x1F5FA,0x1F3DB,0x1F37D,0x1F17F,0x26F1,0x1F3D8,0x1F3D5,0x1F3D6,0x26E9,0x1F5D3}
bad = collections.Counter()
for f in ['index.html'] + glob.glob('js/*.js') + glob.glob('css/*.css'):
    s = open(f, encoding='utf-8').read()
    for i, c in enumerate(s):
        if ord(c) in TD and not (i+1 < len(s) and s[i+1] == '️'):
            bad[f"{c} {f}"] += 1
print(dict(bad) or "전부 정상")
EOF
```

### 지도 렌더링 방식

```
줌레벨 > 7 (줌아웃):  그리드 기반 클러스터 원 (별/P/💳 + 개수)
줌레벨 ≤ 7 (줌인):   뷰포트 내 개별 핀 (cm-pin, cm-circle, cm-tail)

idle 이벤트 → 100ms 디바운스 → clearDisplay() → re-render
(panBy/setCenter 연속 호출 시 마지막 위치에서 1회만 렌더링)
```

---

## 4. 데이터 파이프라인

### 관광지 (tourist) — 151개

> 아래 표의 수집 이력은 **수집 당시** 기준이라 합이 159 다. 2026-08-26 에 영화관 8곳을
> 편의정보로 옮겨(`id 40`·`159~165` 제거) **현재는 151개**다. 현황 수치는 §12 를 보라.

| 수집 시기 | 방법 | ID 범위 | 수량 |
|---------|------|---------|------|
| 초기 | tour.hscity.go.kr 공식 API 스크래핑 | id:1–41 | 41개 |
| 1차 추가 | tour.hscity.go.kr 체험마을·체험지 | id:66–83, 201–233 | 51개 |
| 2차 추가 | 한국관광 데이터랩 8개 zip → 29개 CSV 분석 | id:134–200 | 67개 |

**2차 추가 포함 카테고리**: 쇼핑(백화점), 골프장, 숙박(호텔·리조트), 영화관, 테마파크, 공원, 시장, 마리나, 낚시터, 사찰, 체험장, 문화시설

### 축제 (festival) — 50개

1. yeyak.hscity.go.kr에서 2026년 화성시 행사 전체 수집
2. `gen_data.py` 스크립트로 좌표 자동 매핑 (VENUE_COORDS 딕셔너리)
3. 과거 행사(8월 14일 이전) 전체 제거 → 미래 행사 48개만 유지 *(최초 수집 때의 처리다. 지금 방침은 아래를 볼 것)*
4. ID 범위: 42–133 (festival 전용). 2026-08-26 에 `276`·`277`(연등음악축제·뱃놀이축제)을 더해 **50개**

#### 끝난 축제는 **지우지 않고 화면에서 숨깁니다** (2026-08-30 사용자 지시)

위 3번은 수집 당시 한 번 한 처리입니다. **앞으로는 지나간 축제를 삭제하지 마십시오.**

| | 어떻게 |
|---|---|
| 데이터 | `js/data.js` 에 **그대로 둔다.** 지우면 캘린더의 지난 날짜와 회고가 빈다 |
| 화면 | `festBrowsable()` (`js/calendar.js`)이 `festStatus() === 'ended'` 를 거른다 |

**거르는 곳** — 시간 범위가 이름에 없는 '둘러보는 목록':
추천 탭 축제 목록(`js/tourism.js`) · 소식 탭 「행사 전체」(`js/living.js`) · 홈 대표 축제 카드(`js/home.js`)

**그대로 두는 곳** — 범위가 이름에 박혀 있어 *지난 것도 그 범위의 소식*인 곳:
「이번 주 소식」(`D+N` 으로 지났음을 밝힌다) · 「이번 달 축제」 캐러셀 · 축제 캘린더.
지도는 애초에 축제를 안 그립니다(`js/map.js` `buildOverlays`, 2026-08-26 결정).

> `'unknown'`(날짜를 못 읽은 것)은 **남깁니다** — 끝났다는 근거가 없습니다.
> `'ended'` 하나만 거릅니다. `'2026년 8월 중'` 같은 근사 일정은 그 달이 지나야 `ended` 가 됩니다.
>
> ⚠ **추가할 때는 여전히 미래 행사인지 보십시오.** `276`·`277` 은 2026-08-26 에
> 반입될 때 이미 **3개월 지난 5월 행사**였습니다. 숨김은 지나간 것을 가려 주는 장치이지,
> 처음부터 죽은 데이터를 넣어도 된다는 뜻이 아닙니다.

### 주차장 — 131개

- `smartparking.hscity.go.kr` API에서 전체 목록 수집
- 정적 캐시: `js/parking-static.json` (Flask 없어도 즉시 표시)
- 실시간 여유: Flask 프록시 `/api/parking/realtime` → 60초마다 갱신

### 별점 시스템 — 159개

Playwright/Selenium 환경 미지원 → 알고리즘 기반 생성:
1. Kakao Local Search API REST 호출 (검색 순위 파악)
2. 설명 텍스트 분석 (한국관광100선, 세계문화유산, 명소 키워드 등)
3. 태그 수·설명 길이 보정
4. 결정론적 노이즈 (MD5 해시 기반, 항상 동일한 값)
5. 결과: `js/ratings.json` → `data.js` 주입

### 장소 사진

- `assets/images/places/{name}.jpg` (data.js name 필드와 정확히 일치)
- 240px 썸네일은 `assets/images/thumbs/{name}.jpg` — 38·48·76px 슬롯이 쓴다
- **git 제외** (용량 문제) — 배포 서버에만 존재. 커밋되는 것은 `js/photos.js` 뿐이다
- 2026-08-27 기준 **PLACES 243곳(tourist 151·festival 50·heritage 42) 전건 보유**
  (2026-08-30 재확인 — `js/photos.js` 와 `js/data.js` 대조해 **누락 0곳**)
- `js/photos.js` 인덱스 규모: `byName` 265곳 367장 · `byId` 9곳 9장.
  PLACES 밖(편의정보 캠핑 9·호텔 8 등)까지 담기 때문에 곳 수가 243보다 많다
- onerror fallback: 주황 그라데이션 + 🏞️ 자동 표시

#### 사진 반입은 반드시 `tools/import_photos.py` 로 한다 (2026-08-27 신설)

```
python3 tools/import_photos.py "<사진 폴더>" --check   # 대조만
python3 tools/import_photos.py "<사진 폴더>"           # 반입
python3 tools/optimize_images.py assets/images/places  # 크기·품질·형식
python3 tools/optimize_images.py --thumbs              # 240px 썸네일
python3 tools/build_photo_index.py                     # js/photos.js 재생성
python3 tools/bump_version.py && bash tools/check.sh; echo $?
```

**손으로 `find`·`cp` 하지 마십시오.** 그렇게 하다 같은 날 두 번 사고가 났습니다.

| 사고 | 무엇이 문제였나 |
|------|----------------|
| **확장자를 빠뜨렸다** | `find -iname "*.jpg" -o "*.png" …` 목록에 `.jfif` 가 없어 올라온 33장 중 **28장을 통째로 못 봤다.** `.jfif` 는 JPEG File Interchange Format 이다 — 내용은 그냥 JPEG 이고 윈도우 크롬에서 이미지를 저장하면 흔히 붙는다. 도구는 `.jpg .jpeg .jfif .jpe .png .webp .gif .bmp .avif` 를 전부 읽고, 내용이 JPEG 인 것(`.jfif .jpe .jpeg`)은 `.jpg` 로 통일해 반입한다 |
| **이름을 손으로 고쳤다** | `금당 엄나무 마을`(공백 있음) vs data.js 의 `금당 엄나무마을`. 위 규칙대로 정확히 일치해야 뜨므로 그대로 넣으면 **조용히 안 뜬다.** 도구가 공백·대소문자만 다른 것은 **정확명으로 자동 교정**하고, 그 외에는 반입하지 않고 목록으로 보고한다(임의 추측 금지) |

> 두 사고 모두 '사람이 목록을 관리하는' 방식이라 반드시 또 틀립니다.
> 새 확장자를 만나면 파일을 옮기지 말고 `tools/import_photos.py` 의 `IMG_EXTS` 에 추가하십시오.

사진을 새로 받으면 `tools/check_data.py` 의 `CEILING["photo_missing_tourist"]` 를
**내리십시오. 올리지는 마십시오.** 현재 값은 `0` 입니다 — 한 장이라도 사라지면 즉시 FAIL 입니다.

---

## 5. 세션별 작업 이력 (Session 1~4) → [`docs/log/2026-08-19-both-session-history.md`](docs/log/2026-08-19-both-session-history.md)

## 6. 주요 버그 수정 이력 (BUG-1~13 외) → [`docs/log/2026-08-19-both-bug-history.md`](docs/log/2026-08-19-both-bug-history.md)

## 7. 양방향 Claude 소통 기록 → [`docs/log/2026-08-19-both-claude-messages.md`](docs/log/2026-08-19-both-claude-messages.md)

## 8. PLACES 좌표 일괄 재지오코딩 프로토콜 → [`docs/log/2026-08-19-dev-regeocode.md`](docs/log/2026-08-19-dev-regeocode.md)

## 9. 남은 한계 및 향후 개선 방향

| 항목 | 현황 | 개선 방향 |
|------|------|---------|
| 별점 데이터 | 알고리즘 생성 (Selenium 환경 없음) | Kakao Place API 실제 별점 연동 |
| 장소 사진 | git 추적 제외 완료 — 로컬·배포 서버에만 보관 (섹션 11 참고) | CDN 적용 또는 Cloudflare Images |
| ~~Geocoder 캐싱~~ | ✅ **해결** — `js/conv_map.js` 가 `CONV_CACHE_VER` 로 localStorage 캐싱 중 | — |
| ~~달력 날짜 연동~~ | ✅ **해결** — `js/calendar.js` 가 `hasFest` 로 `has-event` 를 동적 마킹 | — |
| 맛집 지도 칩 | 데이터는 `js/restaurants-static.json` 3,754건으로 있으나 **읽는 코드가 없다** | ⏸ **보류** (2026-08-30 사용자 판단). 사양은 §13-R |

---

## 10. 파일별 역할 및 핵심 함수

> ⚠ 아래 함수들은 **2026-08-25 의 JS 분리(§20) 전까지 `index.html` 안에** 있었습니다.
> 지금은 전부 `js/` 로 나갔습니다. 2026-08-30 에 실제 위치로 고쳤습니다.
> **파일을 찾을 때는 `grep -rn "function 이름" js/ index.html` 이 가장 확실합니다.**

### 화면·탐색 (분리 전 index.html 소속이던 것)

| 함수 | 지금 있는 곳 | 역할 |
|------|-------------|------|
| `go(page)` | `js/nav.js` | 탭 전환 (+ 퀴즈·메뉴 자동 닫기) |
| `goMapFocus(lat, lng, level, id)` | `js/mapnav.js` | 추천 탭 → 지도 이동 + 핀 포커스 |
| `renderTourismList(theme, expanded)` | `js/tourism.js` | 추천 탭 목록 렌더링 (더보기 포함) |
| `showFestivalDetail(id)` | `js/tourism.js` | 축제 상세 뷰 |
| `ratingStars(r)` | `js/tourism.js` | 별점 → ★½☆ 문자열 변환 |
| `openQuiz()` / `closeQuiz()` | `js/quiz.js` | 퀴즈 오버레이 제어 |
| `copyAppUrl()` | `js/ui.js` | 앱 URL 클립보드 복사 |

### map.js

| 함수 | 역할 |
|------|------|
| `initMap()` | Kakao 지도 초기화 + idle 리스너 |
| `buildOverlays()` | tourist 제외 카테고리 핀 사전 생성 |
| `updateTouristDisplay()` | 100ms 디바운스 관광지 렌더링 분기 |
| `showTkViewport(bounds)` | 뷰포트 내 개별 핀 생성 |
| `showTkClusters(bounds, lv)` | 줌아웃 시 그리드 클러스터 원 |
| `onPinClick(id)` | 핀 클릭 → 선택 상태 + 슬라이드 오픈 |
| `showPlaceSlide(place)` | 하단 슬라이드 카드 렌더링 |
| `setFilter(cat)` | 카테고리 칩 필터 토글 |
| `activateParking()` | 주차장 필터 단방향 활성화 |

### parking.js

| 함수 | 역할 |
|------|------|
| `mergeParkingData()` | parking-static.json 로드 |
| `fetchParkingAll()` | 실시간 API 최초 호출 |
| `_applyRealtime(data)` | 실시간 데이터 → parkingData 병합 |
| `pinColorCached(p)` | 여유율 기반 색상 (캐시) |
| `updateParkingDisplay()` | 100ms 디바운스 주차장 렌더링 |

---

---

## 11. assets/ 이미지 관리 정책

`assets/images/` 는 **git 추적 제외** — `.gitignore` 의 `assets/` 줄에 등록됨.
현황 수치(773개 · 32MB)는 §28 이, 사진 장수는 §18 이 갖습니다 — 여기 또 적으면 또 어긋납니다.
사진 파일은 **로컬 및 배포 서버에만 보관**하며, git push 대상이 아닙니다.

> 장수는 여기 적지 않습니다. 배포 서버에만 있어 개발 워킹트리에서 셀 수 없고,
> 적어 두면 반드시 낡습니다(실제로 '159장' 이 2026-08-30 까지 남아 있었습니다).
> 현재 수치는 `js/photos.js` 인덱스(§4)와 `bash tools/check.sh` 의 asset-guard 로 봅니다.

```
assets/images/places/{name}.jpg   — data.js의 name 필드와 정확히 일치해야 함
assets/images/로고_이름.png       — 앱 로고
```

- 배포 서버(배포 Claude 환경)에 직접 보관
- git clone 후에는 사진 없이 동작 (onerror fallback: 주황 그라데이션 + 🏞️ 표시)
- **앞으로 assets/ 및 용량 큰 파일은 절대 git push 금지**

---

## 12. 새 Claude 세션 진입 체크리스트

> 새 컨텍스트로 접속한 Claude는 이 체크리스트를 순서대로 따라 즉시 작업을 이어갈 수 있습니다.

### 필수 확인 (작업 시작 전)

```bash
# 1. 최신 코드 확인
git log --oneline -5

# 2. 변경 사항 없는지 확인
git status

# 3. 원격과 동기화
git pull
```

### 컨테이너가 재부팅됐다면 — 배포 환경 (2026-08-30 신설)

배포 Claude 컨테이너는 **재부팅이 잦습니다** — 2026-08-30 하루에 세 번 겪었습니다
(뒤의 둘은 08:07·14:05). 경위는
[`docs/log/2026-08-30-deploy-reboot-recovery-push-auth.md`](docs/log/2026-08-30-deploy-reboot-recovery-push-auth.md).

**재부팅은 곧 컨테이너 교체입니다.** 14:05 재부팅에서 호스트명이
`cfe9165e4b50` → `48d2bd481700` 으로 바뀌었습니다. 그런데도 `work` 안의 것은
커밋 안 된 수정까지 전부 살아남았습니다 — 컨테이너가 아니라 **별도 볼륨**이기
때문입니다. 그러니 "재부팅되면 작업이 날아간다" 가 아니라, **어디에 뒀느냐**가
전부입니다.

살아남는 것은 아래뿐이고 나머지는 overlayfs 라 컨테이너와 함께 사라집니다. 다만
**이 목록이 두 Claude 의 컨테이너에서 서로 다릅니다** — 2026-08-31 에 확인했습니다.

```
공통 — 배포·개발 둘 다 있음
/dev/sdb1 on /home/jovyan/work                       ext4 (rw)   ← 저장소가 여기
/dev/sdb1 on /home/jovyan/shared                     ext4 (ro)
/dev/sdb1 on /home/jovyan/.claude.json               ext4 (rw)   ← 파일 하나만
/dev/sdb1 on /home/jovyan/.claude/.credentials.json  ext4 (rw)   ← 파일 하나만

개발 컨테이너에만 있음
/dev/sdb1[…/claude_sessions/hwasung_a/22016032/projects] → /home/jovyan/.claude/projects
```

**배포 컨테이너에는 이 다섯 번째가 없습니다.** `findmnt /home/jovyan/.claude/projects`
가 아무것도 출력하지 않고, `mount` 전체 160줄에서 `.claude` 로 걸리는 것은 위의 파일
두 개뿐입니다. 그래서 **배포 쪽 세션 기록과 메모리는 컨테이너와 함께 사라집니다.**
개발 쪽은 바인드 마운트가 있어 남습니다.

> `~/.claude` 자체는 양쪽 다 overlayfs 이고, 그 **안**에서 따로 바인드 마운트된 것은
> 개발 쪽 `projects/` 와 양쪽의 `.credentials.json` 뿐입니다 — **`~/.claude.json` 은
> 그 안이 아니라 그 옆의 별도 파일입니다**(2026-08-31 개발 Claude 정정).
> 그래서 `~/.claude/settings.json` 같은 건 양쪽 다 사라집니다.

> **환경 사실은 자기 컨테이너에서 재십시오.** 상대 쪽 실측을 그대로 옮기면 §0 이
> 경고하는 실패를 **환경 축으로** 반복하게 됩니다. 실제로 08-30 에 배포가 관찰만으로
> 원인을 단정했고, 08-31 에 개발이 자기 쪽 실측으로 그것을 뒤집었는데, **그 실측도
> 배포 컨테이너에는 해당하지 않았습니다.** 둘 다 자기 환경에서는 맞았습니다.
> 같은 일이 `tools/server.py` 미완 diff 에서 한 번 더 났습니다(§13).
> 적을 때 **어느 컨테이너에서 쟀는지**를 같이 남기십시오.
>
> ```bash
> findmnt -no SOURCE,TARGET ~/.claude/projects   # 출력이 없으면 그 컨테이너에선 영속이 아니다
> ```

| 증발하는 것 | 증상 | 복구 |
|---|---|---|
| pip 설치분 — flask·requests | `tools/server.py` 가 `ModuleNotFoundError` 로 안 뜸 | `pip install flask requests` |
| GitHub 쓰기 인증 — `~/.config/gh/hosts.yml` | **pull 은 되는데 push 만 죽음** | **사용자에게** `gh auth login` → `gh auth setup-git` 요청. 브라우저 기기 인증이라 Claude 가 대신 못 합니다 |
| git 전역 설정 | 거의 없음 — `user.email` 등 repo 로컬 설정은 work 안이라 생존 | 필요할 때만 |
| ~~Claude 메모리·세션 기록~~ | — | **증발 안 합니다.** `~/.claude/projects/` 는 위 표대로 영속입니다 (2026-08-31 정정, 아래) |
| 서버·cloudflared 프로세스 | 배포 URL 무응답 | 서버·터널 재기동. **quick tunnel 은 호스트명이 매번 바뀝니다** — README 2곳 · §1 배포 URL · `tools/check.sh` 의 `LIVE_URL` **네 곳**을 새 주소로 갱신. 하나라도 빠뜨리면 `check.sh` 의 **url-sync** 가 잡습니다 |

#### ⚠ Claude 메모리를 **유일한** 인계 수단으로 쓰지 마십시오 (2026-08-30, 08-31 정정)

원래 여기에 「`~/.claude/projects/` 는 영속이 아니다. 재부팅에 대비해 남긴 메모리 3건이
14:05 재부팅 뒤 비어 있었다」라고 적혀 있었습니다. **기술이 틀렸습니다.** 08-31 에
다시 재면 그 디렉터리는 영속입니다.

```
$ findmnt -no SOURCE,TARGET /home/jovyan/.claude/projects
/dev/sdb1[/project/.../claude_sessions/hwasung_a/22016032/projects]  /home/jovyan/.claude/projects
```

증거는 마운트만이 아닙니다. **14:05 컨테이너 교체보다 오래된 메모리 파일 4개가
그 뒤에도 그대로 있었습니다** — 08-17 · 08-19 · 08-30 06:54 · 08-30 07:26.
디렉터리가 통째로 날아갔다면 이것들도 같이 없어야 합니다.

**그럼 그 3건은 왜 없었나 — 모릅니다.** 애초에 안 써졌는지, 다른 경로로 갔는지,
지워졌는지 확인할 방법이 없습니다. 원인 미상인 채로 두고, **틀린 원인을 적어 두지는
않습니다.** "재부팅이 지운다" 로 적어 두면 다음 사람이 엉뚱한 대비를 합니다.

그러니 결론은 유지하되 이유를 바꿉니다. **메모리로만 처리하지 마십시오.**
사라져서가 아니라 —

- **사용자가 못 봅니다.** 리뷰도 반박도 안 됩니다. 저장소에 커밋하면 diff 로 남습니다.
- **한 번 실제로 없어졌습니다.** 원인을 모르는 채 한 번 겪었으면 유일본으로 두면 안 됩니다.
- **컨테이너가 아니라 볼륨이 갈리면 같이 갑니다.** push 된 것만 그 바깥에 있습니다.

메모리는 그대로 쓰되(빠릅니다), 남아야 하는 것은 저장소에도 둡니다 — 규칙·현재 상태는
이 파일, 경위는 `docs/log/`, 그리고 **커밋과 push 까지** 해야 진짜로 남습니다.
(Claude Code 로그인은 `.credentials.json` 이 영속이라 재부팅 뒤에도 세션이 바로 열립니다.)

> 이 정정 자체가 §0 이 말하는 실패의 표본입니다. 실측 한 번(`mount`)이면 갈릴 것을
> 관찰 한 번("비어 있더라")으로 단정해 문서에 규칙까지 세웠습니다.
> **증상에서 원인을 추측했으면, 원인을 직접 재기 전에는 문서에 원인으로 적지 마십시오.**

순서는 이렇게 하십시오.

```bash
git status && git stash list   # 1. 끊긴 작업부터 — 커밋 안 된 diff 가 곧 중단 지점이다
git push --dry-run             # 2. '쓰기' 를 확인 — 읽기 확인으로는 안 잡힌다
pip install flask requests     # 3. 그 다음에야 서버·터널
```

**`git fetch` 가 된다고 "깃 정상" 이라고 보고하지 마십시오.** 공개 저장소는 읽기가
무인증이라, 인증이 통째로 증발해도 fetch·pull·ls-remote 는 전부 통과합니다.
push 에서야 터집니다. 쓰기는 `git push --dry-run` 으로 따로 확인해야 합니다.

토큰을 `work/` 나 저장소에 옮겨 영속시키지 마십시오 — **공개 저장소입니다.**
재부팅마다 사용자가 한 번 로그인하는 것이 맞는 비용입니다.

### ⚠ 서버·터널 재기동 — `cloudflared` 는 끄지 마십시오 (2026-08-31 올림)

**이 절차는 지금까지 로그에만 있었습니다** —
[`docs/log/2026-08-25-dev-server-scope.md`](docs/log/2026-08-25-dev-server-scope.md).
로그는 검색에 걸려야 읽히므로, 재기동하는 사람이 못 찾으면 배포 주소가 죽습니다.

```
cloudflared  ──터널──▶  localhost:8080  ──▶  Flask (tools/server.py)
   ↑ 이건 계속 켜 둔다                ↑ 이것만 껐다 켠다
```

공개 주소(`*.trycloudflare.com`)는 **`cloudflared` 프로세스에 붙어 있습니다.**
Flask 만 껐다 켜면 몇 초 502 뒤 정상으로 돌아오고 **주소는 그대로**입니다.
`cloudflared` 를 끄면 그 주소는 **영구히 사라지고** 새 랜덤 주소가 발급됩니다 —
저장소 안 **네 곳**과 저장소 **밖 한 곳**, 모두 **다섯 자리**가 한꺼번에 죽습니다.

| # | 고쳐야 할 곳 | 무엇 | 검사 |
|---|---|---|---|
| 1 | `README.md` 배지 | `[![Live Demo]…]( )` 의 링크 | ✅ `url-sync` |
| 2 | `README.md` 본문 | `**[Try the live app →]( )**` | ✅ `url-sync` |
| 3 | `WORKFLOW.md` §1 | `- **배포 URL**:` | ✅ `url-sync` |
| 4 | `tools/check.sh` | `LIVE_URL=` 기본값 | ✅ `url-sync` |
| 5 | **Kakao Developers 웹 도메인** | 콘솔 설정 — **git 밖** | ❌ **어떤 검사도 못 봅니다** |

**1~4 는 `check.sh` 의 `url-sync` 가 셉니다.** 하나라도 다르면 FAIL 이 나고 파일:줄을
찍습니다. 네트워크도 git 도 필요 없어 터널이 죽은 상태에서도 돕니다.
(원래 「세 곳」이라 적혀 있었습니다 — `README.md` 에 두 군데가 있는데 하나로 세고
있었습니다. 이런 목록은 반드시 어긋나므로 사람이 세지 말고 검사에 맡기십시오.)

**5 번이 진짜 함정입니다.** git 밖이라 grep 도 검사도 닿지 않습니다.
2026-08-31 에 1~4 를 정확히 고치고도 **지도만 조용히 죽었고 `check.sh --live` 는
exit 0** 이었습니다. JS 앱 키에 도메인 제한이 걸려 있어 새 주소에서 SDK 가 401
(`domain mismatched!`)을 냅니다. 자세한 것은 §12 「Kakao API」.

> **`--live` 로는 1~5 어느 것도 못 잡습니다.** `live_drift()` 는 `LIVE_URL` 하나만 보고
> `index.html` 바이트를 대조할 뿐이라, README 가 죽은 주소를 가리켜도 · 지도가 통째로
> 죽어도 초록불입니다. **「검사가 통과했다」와 「기능이 산다」는 다릅니다.**
> `docs/log/` 는 `url-sync` 대상에서 뺐습니다 — 로그의 주소는 **그때의 기록**이라
> 갱신 대상이 아닙니다(§0).

기동은 **주최 측 공식 매뉴얼**(「AI화성 챌린지」 · cloudflared 외부 공개)을 따릅니다.
매뉴얼 3.1 이 **tmux 를 권장**합니다 — 콘솔을 다시 볼 수 있어야 URL 을 잃지 않습니다.

```bash
mkdir -p ~/work/logs
tmux new -d -s app    "python3 -u tools/server.py --port 8080 2>&1 | tee ~/work/logs/app.log"
# 매뉴얼 7장 — 터널 전에 컨테이너 안에서 먼저 확인한다. 이걸 건너뛰면 502 를 터널 탓으로 오진한다
curl -s -o /dev/null -w '%{http_code}\n' http://localhost:8080/
tmux new -d -s tunnel "cloudflared tunnel --url http://localhost:8080 2>&1 | tee ~/work/logs/tunnel.log"
sleep 8 && grep -oE 'https://[a-zA-Z0-9-]+\.trycloudflare\.com' ~/work/logs/tunnel.log | head -1
```

| 하고 싶은 것 | 명령 |
|---|---|
| URL 을 다시 확인 | `grep -oE 'https://[a-zA-Z0-9-]+\.trycloudflare\.com' ~/work/logs/tunnel.log \| head -1` |
| 콘솔 들여다보기 | `tmux attach -t tunnel` → 나올 때 `Ctrl+B` 그다음 `D` |
| **서버만** 재시작 (주소 유지) | `tmux kill-session -t app` 뒤 위 `app` 줄 다시 실행 |
| 전부 종료 | `tmux kill-session -t tunnel` · `tmux kill-session -t app` |

> **`sudo cloudflared service install` 을 시도하지 마십시오** (매뉴얼 3.4). 이 계정에는
> sudo 가 없고 컨테이너에 systemd 자체가 없습니다. **컨테이너가 곧 영속 단위**라
> tmux 세션이면 충분합니다.
>
> **컨테이너를 Stop 하면 URL 도 영구히 사라집니다** (매뉴얼 7장). 재부팅과 같은 결과입니다.
>
> **⚠ Quick Tunnel URL 은 인증 없이 누구나 접근합니다** (매뉴얼 8장). 그래서 이 서버는
> `_PUBLIC_DIRS` **허용목록**으로 `js/`·`css/`·`img/`·`assets/`·`index.html` 만 내보냅니다 —
> `WORKFLOW.md`·`README.md`·`tools/`·`.git/` 는 전부 404 입니다(터널 너머에서 확인).
> 매뉴얼이 **발표가 끝나면 즉시 터널을 종료하라**고 정해 두었습니다. 방치하지 마십시오.

> **한 번은 이미 잃었습니다.** 2026-08-30 재부팅이 `cloudflared` 프로세스까지 죽여
> 옛 주소(`culture-reed-dee-rug`)는 회수하지 못했습니다. 2026-08-31 에 터널을 다시
> 띄우고 새 호스트명으로 세 곳(README 배지 · §1 · `check.sh` `LIVE_URL`)을 갱신했습니다.
> **네 번째 자리 — 카카오 개발자 콘솔의 웹 도메인 — 은 git 밖이라 사용자만 할 수 있습니다.**
> **지금부터 이 규칙이 값을 합니다** — 앞으로 서버만 고칠 때 터널까지 끄면
> 그때 또 주소가 바뀌어 **네 곳**을 다시 고치게 됩니다(카카오 콘솔 포함).

### 이 프로젝트에서 반드시 지켜야 할 규칙

| 규칙 | 내용 |
|------|------|
| 언어 | **항상 한국어 경어체** |
| assets push 금지 | `assets/images/` 는 push 금지 — 사용자 명시 허가 시에만 예외 |
| README 수정 금지 | README는 서비스 소개 전용. 개발 메모는 반드시 이 파일(WORKFLOW.md)에 |
| 수정 전 허락 | 검수·분석 결과는 먼저 보고 → 사용자 허락 후 수정 진행 |
| git pull/push | 작업 전 pull, 작업 후 push. 사용자가 "깃 풀 푸쉬"라고 하면 pull → push 순서 |
| **`go()` 뒤 `setTimeout` 금지** | 화면을 바꾸는 코드는 `go()` **바로 뒤에서 동기로** 부를 것. `go()` 는 뷰 교체와 목록 렌더까지 전부 동기로 끝내므로(`js/nav.js`), 지연을 두면 그 사이 '엉뚱한 화면'이 완성돼 페인트된다. 2026-08-26 에 이 패턴 6곳을 걷어냈다(실측 최대 9프레임 노출). 예외는 **스크롤**뿐 — 전환 애니메이션(0.26s) 뒤에 움직이는 편이 낫다 |
| **`check.sh` 는 종료 코드로 판정** | `bash tools/check.sh; echo $?` 로 볼 것. `grep "── FAIL"` 로 축별 합계만 보면 **실패를 놓친다** — 실제로 2026-08-26 에 데이터 축이 `FAIL 7` 인 채로 며칠 갔는데 다른 축의 `── FAIL 0` 두 개만 보고 통과로 보고했다. grep 은 '무엇이 실패했는지' 볼 때만 쓴다 |
| **화면 숫자를 바꾸면 `PRINTED` 도** | 화면에 찍히는 건수 문구를 고치거나 없애면 `tools/check_data.py` 의 `PRINTED` 정규식이 대상을 잃어 검사가 통째로 빨간불이 된다. 없앤 문구는 '되살릴 때 함께 되살리라' 는 주석과 함께 남길 것(선례: 음식점 칩·통계 4칸) |
| **로드 시점 전역은 순서에 묶인다** | `index.html` `<script>` 는 **23개**이고 순서는 nav→quiz→mapnav→home→tourism→living→favorites→ui→calendar→boot→photos→convenience→**data**→**map**→conv_map→parking→localcurrency→datalab→tide→weather→today→district→hscroll 다. **`js/data.js` 가 13번째라는 점에 주의** — 그 앞 12개는 초기화식에서 `PLACES` 를 볼 수 없다. 순서는 `grep -oP '<script src="js/\K[a-z_]+' index.html` 로 확인한다. 다른 파일의 전역을 **초기화식에서** 참조하면 순서가 앞서야 한다 — 2026-08-26 에 `LC_ICON_HTML` 을 `js/ui.js`(8번째)에 두고 `js/home.js`(4번째)의 `_CAT_STYLE` 초기화에서 읽어 home.js 가 통째로 죽었다. 함수 **안**에서 읽으면(런타임) 순서와 무관하다 |
| **테마 축과 카테고리 축은 곱한다** | 추천 탭 목록은 서브탭(카테고리)과 테마 칩 두 축이 겹친다. 한쪽만 보고 필터하면 다른 축이 통째로 풀린다 — 2026-08-26 에 '관광지' 서브탭에서 테마를 누르면 문화재 42건이 섞여 나왔다. 테마 '전체' 는 **'테마 조건 없음'** 이지 '모든 카테고리' 가 아니다 |
| **추천 탭 네 뷰는 배타** | `#view-tourism-list` · `#view-calendar` · `#view-festival-detail` · `#view-datalab` 중 하나만 보여야 한다. 새 뷰를 켜는 함수는 **나머지 셋을 전부 `display:none`** 으로 내릴 것. `#view-datalab` 은 `_dlView` 도 함께 `null` 로 내린다 — display 만 내리면 `dlLoad` 의 늦은 콜백이 숨긴 뷰를 다시 그린다 |
| **`?v=` 는 손으로 올리지 않는다** (2026-08-31 올림) | `python3 tools/bump_version.py` 가 현재값을 읽어 `index.html` 30곳과 `js/datalab.js` 의 `DL_VER` 을 **함께** 올린다. `sed` 로 하면 찾는 문자열이 없을 때 **0건 치환하고 성공으로 끝나** 조용히 안 올라간다 — 실제 사고의 직접 원인이었다. 서버가 `?v=` 를 단 `js`·`css` 를 **1년 `immutable`** 로 내보내므로(`02aebda`), 같은 번호에 다른 내용이 한 번 나가면 그 URL 은 **강제 새로고침으로도 안 고쳐진다**(Firefox·Safari). `tools/check.sh` 의 **version-guard** 가 「`js/`·`css/` 를 고친 최신 커밋이 `index.html` 도 고쳤는가」를 본다 — 다만 **값은 안 보고 파일 목록만** 본다. 경위: [`docs/log/2026-08-26-dev-cachebust-guard.md`](docs/log/2026-08-26-dev-cachebust-guard.md) |

### 핵심 현황 (최신 기준)

| 항목 | 값 |
|------|----|
| CONV_CACHE_VER | `v7` — `js/conv_map.js` 의 `var CONV_CACHE_VER` |
| 관광지 | 151개 — 2026-08-26 영화관 8곳을 편의정보로 옮겼다(id 40·159~165 제거) |
| 문화재 | 42개 (id: 234–275, 2026-08-26 추가) |
| 축제 | 50개 (id: 42–133, 276–277, 2026-08-26 연등음악축제·뱃놀이축제 추가) |
| 주차장 | 131개 |
| 제부도 숙박 | 115개 (lat/lng 하드코딩, convenience.js) |
| 편의정보 | 모범음식점 94(88좌표이식), 관광식당 35, 호텔 10, 캠핑 17, 템플스테이 1, **관광편의시설 10(신규)**, **영화상영관 13** (2026-08-26: 공식 CSV 전건. PLACES 에만 있던 3곳을 건져 올렸다) |
| 메뉴 외부 링크 3곳 | 화성시청 홈페이지 · **화성in**(`live.saharax.io` — 화성시청 공식 AI 상담) · 네이버 공식 블로그. 셋 다 `<a target="_blank" rel="noopener noreferrer">` **링크**이고 스크립트·iframe·fetch 가 아니다(`grep` 로 확인 — `index.html` 에 `saharax` 는 1회, 전부 href). `1772804`(개발 Claude, 2026-08-26)에서 들어왔고 `docs/log/2026-08-26-dev-readme-demo-script.md` 가 시연 대본 사실 확인에 넣어 뒀다. **2026-08-31 에 「출처 불명 제3자 위젯 아닌가」로 다시 올라와** 여기 적는다 — §0 대로 끝난 확인은 이 파일이 갖는다 |
| 음식점 데이터 | 3754건 — `js/restaurants-static.json` (649KB, `{n,c,a,t,x,y}`) — **현재 어떤 코드도 읽지 않는다.** `js/restaurants.js` 가 지워지고 지도 칩 `data-cat="restaurant"` 도 없다. 햄버거 메뉴 '음식점 전체' 는 2026-08-26 에 '준비 중' 토스트로 바꿨다 (되살리려면 로더 + 지도 칩 두 개가 필요) |

### Kakao API

| 종류 | 키 |
|------|-----|
| JS 앱 키 (지도 표시용) | `index.html` `<script>` 의 `appkey` 파라미터 — **원래 공개용**이다(도메인 제한). ⚠ **터널 주소가 바뀌면 지도가 죽는다** — 아래를 보라 |
| REST API 키 (지오코딩용) | `1bd845da5756d1c78955463b800731ef` |

#### ⚠⚠ 터널 주소를 바꿨으면 **카카오 개발자 콘솔에도 등록**해야 합니다 (2026-08-31 신설)

**JS 앱 키는 도메인 제한이 걸려 있습니다.** 등록되지 않은 주소에서 부르면 SDK 가
**HTTP 401** 로 거부되고 `kakao` 가 정의되지 않아 지도 자리에 오류 화면이 뜹니다.
**새로고침으로는 절대 안 고쳐집니다.**

> 화면 문구는 2026-08-31 에 고쳤습니다. 원래 *「페이지를 새로고침 해주세요」* 라고
> 해서 **원인을 잘못 말하고 있었습니다** — 배포 Claude 가 지적하고 개발 Claude 가
> `js/map.js` 를 고쳤습니다(§17 상 개발 파일). 지금은 사용자에게 *「잠시 뒤 다시
> 열어 주세요」* 만 말하고, **진단은 `_warnMapKey()` 가 콘솔로 냅니다** —
> 현재 origin·401 의 뜻·어느 콘솔에서 무엇을 등록해야 하는지까지 찍습니다.
> 도메인 등록은 코드로 못 고치는 일이라, **어디를 봐야 하는지 정확히 찍어 주는 것**이
> 앱이 할 수 있는 전부입니다.

2026-08-31 에 실제로 이렇게 깨졌습니다. 터널을 새로 띄우고 저장소 세 곳만 고쳤는데,
**네 번째 자리가 git 밖에 있어서** 지도가 통째로 죽었습니다.

```
$ curl -s -o /dev/null -w '%{http_code}' -H "Referer: <새 주소>/" \
    "https://dapi.kakao.com/v2/maps/sdk.js?appkey=<JS앱키>&libraries=services&autoload=false"
401   → {"errorType":"AccessDeniedError","message":"domain mismatched! ..."}
```

**그래서 터널을 다시 띄우면 고칠 곳은 저장소 네 곳 + 콘솔 한 곳 = 다섯입니다.**
§12 「컨테이너가 재부팅됐다면」의 표를 보십시오 — 1~4 는 `check.sh` 의 `url-sync` 가
세어 주고, **5 번(콘솔)만 사람이 기억해야 합니다.**

| # | 자리 | git 안인가 |
|---|---|---|
| 1 | `README.md` 배지 · 본문 링크 | ✅ |
| 2 | `WORKFLOW.md` §1 배포 URL | ✅ |
| 3 | `tools/check.sh` `LIVE_URL` | ✅ |
| **4** | **Kakao Developers → 내 애플리케이션 → 앱 설정 → 플랫폼 → Web → 사이트 도메인** | ❌ **git 밖 · 사용자만 가능** |

4번은 **Claude 가 할 수 없습니다.** 콘솔 로그인이 필요합니다. 새 주소를 등록하고
저장한 뒤 브라우저에서 강력 새로고침하면 그때 지도가 돌아옵니다.

> **현재 주소는 2026-08-31 에 등록 완료**했습니다(사용자 조치). 확인하는 명령은 이것입니다 —
> `200` 이면 살아 있고 `401` 이면 아직입니다.
>
> ```bash
> curl -s -o /dev/null -w '%{http_code}\n' -H "Referer: $(grep -oP '(?<=- \*\*배포 URL\*\*: ).*' WORKFLOW.md)/" \
>   "https://dapi.kakao.com/v2/maps/sdk.js?appkey=<JS앱키>&libraries=services&autoload=false"
> ```
>
> `http://localhost:8080` 은 **아직 미등록(401)** 입니다. 넣어 두면 컨테이너 안에서도
> SDK 가 내려와 확인이 한 겹 늘어납니다 — 필수는 아닙니다.

#### ⚠ 클라이언트 시크릿은 저장소·대화에 붙여 넣지 마십시오 (2026-08-31 신설)

콘솔의 **클라이언트 시크릿**(카카오 로그인용 · 비즈니스 인증용)은 JS 앱 키·REST 키와
**성격이 다릅니다.** 앞의 둘은 §12 가 「공개용이라 조치 불필요」로 종결한 값이지만,
클라이언트 시크릿은 **공개를 전제로 하지 않는 값**입니다.

**이 앱은 카카오 로그인을 쓰지 않습니다**(`js/`·`index.html` 참조 0건 실측).
그러니 시크릿을 코드에 넣을 일도, 어딘가에 적어 둘 일도 없습니다.
콘솔 화면을 통째로 복사해 붙이면 그 값까지 딸려 갑니다 — **여기는 공개 저장소입니다.**
값이 밖으로 나갔다면 콘솔의 **「코드 재발급」** 을 누르면 됩니다. 안 쓰는 값이라
재발급해도 이 앱에는 아무 영향이 없습니다.

> **`http://localhost:8080` 도 함께 등록해 두십시오.** 실측 결과 그쪽도 401 입니다.
> `docs/log/` 여러 곳이 *"카카오 SDK 는 이 컨테이너에서 안 뜬다"* 를 **환경 제약**으로
> 적어 왔는데, 브라우저가 안 뜨는 것(`libnspr4.so` 없음)과 **별개로 SDK 요청 자체가
> 도메인 미등록으로 거부되고 있었습니다.** 등록하면 그 절반은 사라집니다.
>
> 매번 등록하는 것이 번거로우면 매뉴얼 9장 **Named Tunnel**(고정 도메인)을
> DS&ML센터(031-229-8897)에 문의하십시오 — 그러면 4번이 한 번으로 끝납니다.

#### ⚠ 키 평문 노출은 **이미 결론 난 사안입니다 — 다시 꺼내지 마십시오**

| | |
|---|---|
| 어디에 | `WORKFLOW.md`(이 표) · `tools/` 5개(`preprocess.py` `ingest.py` `geocode_jebu.py` `fix_coords.py` `fix_all_coords.py`) · `js/home.js` 의 `AIRKOREA_KEY` |
| 판정 | **조치 불필요** (사용자 판단, 2026-08-25 · 2026-08-30 재확인) |
| 근거 | 해커톤 시연용 데모이고, 데이터가 전부 공공데이터이며, **사용자가 키에 만료일을 걸어 두었다** |

전문은 [`docs/log/2026-08-25-dev-server-scope.md`](docs/log/2026-08-25-dev-server-scope.md) 머리에 있습니다.

> **왜 여기 적어 두는가** — 2026-08-24 감사가 "폐기·재발급 권고"로 올렸고
> (`docs/log/2026-08-24-both-session5.md` 의 「Kakao REST API 키 평문 노출」), 그 뒤 사용자가 종결했는데 **결론이
> 로그에만 남아** 2026-08-30 감사가 같은 건을 다시 심각 항목으로 올렸습니다.
> 로그는 시간순이라 나중 결론이 앞 기록을 덮어 주지 않습니다. **끝난 판단은
> `WORKFLOW.md` 로 올려야** 다음 사람이 같은 왕복을 반복하지 않습니다.
>
> 뒤집을 조건은 하나 — **운영 서비스 전환, 또는 만료일 없는 키·개인정보가 들어올 때.**

### 개발 환경 실행

```bash
python tools/server.py --port 8080
# → http://localhost:8080 에서 앱 확인
```

---

## 13. 현재 알려진 미해결 항목 (다음 세션 참고)

> ⚠ 아래 표의 좌표·데이터 항목은 **§17 「배포 Claude 인계 대기」 표가 우선합니다.**
> 여기에는 "어느 쪽(주소/좌표)이 틀렸는지" 가 특정되어 있지 않아, 그대로 고치면 멀쩡한 값을 망가뜨리는 건이 있습니다.
> (예: `id:41` 은 좌표가 정상이고 주소가 오류, 작은섬·양지 쌍은 애초에 정상이라 수정 대상이 아님)

> 2026-08-30 에 이 표를 `js/data.js`·`js/convenience.js` 실값과 전건 대조해
> 해결된 항목을 걷어냈습니다. 지운 것들의 경위는 `docs/log/` 와 §17 표에 남아 있습니다.

| 항목 | 설명 |
|------|------|
| 미정 장소 placeholder 좌표 | `37.1995372 / 126.8314774` (= **화성시청**, 남양읍 시청로 159) 로 떨어진 것이 **23건 — 축제 21 · 관광지 2**(`id:215`·`id:229`). `id:229` 는 그 뒤 해결됐고, 축제 21건은 주최 측이 장소를 안 정한 것이라 **데이터 자체 문제로 코드 수정 불가** |
| `id:215` 말달리자 승마클럽 | **실존 확인 불가.** 화성시청 폴백 좌표 그대로. §17 참조 — 원본 출처에서 정식 명칭·폐업 여부 재확인 필요 |
| 작은섬1·2, 양지팬션·리조텔 | **수정 금지.** 같은 부지라 좌표·주소가 원래 같다 — §17 인계표 참조 |
| 음식점 3754건 미표시 | ⏸ **보류** (사용자 판단 2026-08-30 — *"잠깐 미뤄두자"*). 사양·데이터는 준비됨. **재개 지시가 올 때까지 착수 금지** — §13-R |
| ~~`js/`·`css/` 주석의 줄번호 참조 74건~~ | ✅ **해결** (2026-08-31). 74건 전부 식별자로 바꾸고 `check.sh` line-ref 검사 대상에 `js/`·`css/`·`index.html` 을 넣었다. 이제 새로 넣으면 FAIL 이 난다. 경위: [`docs/log/2026-08-31-dev-code-comment-line-refs.md`](docs/log/2026-08-31-dev-code-comment-line-refs.md) |

> 해결된 항목은 §14 규칙대로 **지웁니다.** 2026-08-31 에 「배포 URL 죽음」과
> 「지도가 안 뜬다 — 카카오 웹 도메인 미등록」을 지웠습니다 — 후자는 사용자가 콘솔에
> 현재 주소를 등록해 해소됐고, SDK 가 `200` 을 돌려주는 것을 확인했습니다.
> 터널을 다시 띄우고 세 곳을 새 주소로 갱신했습니다(`docs/log/2026-08-31-deploy-server-optimize.md`). 2026-08-30 에 취소선으로 남아 있던 3건
> (`id:41` 주소 · `id:102` 송산포도축제 · `convenience.js` 좌표)과, "(Session 4에서
> 수정 완료)" 라고 적힌 채 미해결 표에 남아 있던 「비ISO 날짜 축제 캘린더」를 지웠습니다.
> 무엇을 어떻게 고쳤는지는 `docs/log/2026-08-26-deploy-data-quality-fix.md` 에 있습니다.

### ⚠ 「내 컨테이너에 없다」는 「없다」가 아닙니다 — `tools/server.py` 건 (2026-08-31)

> **이 절의 원래 제목은 「없는 diff 를 찾지 마십시오」였고 「그런 diff 는 없습니다」로
> 단정했습니다. 틀렸습니다.** 배포 컨테이너에는 실물이 있습니다(아래 답 참조).
> 아래 여섯 가지 실측은 **개발 컨테이너에서만** 참입니다. 그 범위를 안 적고
> 전체 사실처럼 쓴 것이 오류입니다 — 정정하되 **실측 자체는 지우지 않고 남깁니다.**
> 무엇을 어디서 쟀는지가 이 사건의 교훈이기 때문입니다.

08-30 에 「작업트리에 커밋 안 된 `tools/server.py` 미완 diff(+19줄)가 있으니
완성/되돌림/인계를 판단해 달라」는 인계가 올라왔습니다.
**개발 컨테이너에서는** 아래 여섯 가지가 전부 「없다」로 나왔습니다.

| 확인한 것 (개발 컨테이너) | 결과 |
|---|---|
| `git status` · `git diff -- tools/server.py` | 둘 다 **비어 있음** |
| `git stash list` | 없음 |
| **파일 mtime** | **08-26 13:30** — 08-30 에 아무도 안 건드렸다 |
| 마지막으로 이 파일을 바꾼 커밋 | `02aebda` (08-26 13:25) |
| `git log 889bcaf..HEAD -- tools/server.py` | 비어 있음 — 누가 쓸어 담은 것도 아니다 |
| 저장소 사본 | `/home/jovyan/work/hwaseong_AI` **하나뿐** (`find` 로 확인) |

**mtime 이 결정적입니다** — 라고 썼는데, 결정적인 것은 맞지만 **결론이 반대였습니다.**
개발 컨테이너의 `08-26 13:30` 과 배포 컨테이너의 `08-30 08:01:45` 가 다르다는 것이,
바로 **두 사본이 별개라는 증거**입니다. 같은 파일이면 나올 수 없는 값입니다.
자를 제대로 골라 놓고 **「내 쪽이 전부」라고 읽어서** 틀렸습니다.

> 마지막 행 「저장소 사본 하나뿐」이 오류의 핵심입니다. `find /home/jovyan …` 은
> **내 컨테이너의 `/home/jovyan`** 만 봅니다. 상대 컨테이너는 애초에 그 명령의
> 사정거리 밖인데, 출력이 1건이라는 이유로 「하나뿐」이라고 적었습니다.
> **범위를 안 적은 실측은 실측이 아니라 추측입니다.**

#### 딸려 온 인과도 틀렸습니다

> "이 diff 가 남아 있으면 `git pull --rebase` 가 매번 막힌다"

**어떤 파일이든 미커밋이면 막힙니다.** `server.py` 와 무관한 git 의 일반 동작입니다.

```bash
$ touch _tmp_dirty && git add -N _tmp_dirty && git pull --rebase
error: 리베이스로 풀하기 할 수 없습니다: 스테이징하지 않은 변경 사항이 있습니다.

$ git -c rebase.autoStash=true pull --rebase     # ← 이러면 통과한다
$ git rm --cached _tmp_dirty && rm _tmp_dirty    # ← 확인이 끝나면 지운다
```

> ⚠ **재현 확인에 저장소 파일을 쓰지 마십시오.** 개발 Claude가 2026-08-31 에
> 이 재현을 하면서 `README.md` 에 한 줄을 붙였다가 되돌렸는데, **§15 의
> 「README 수정 금지」를 어긴 것**입니다. 되돌릴 자신이 있어도 규칙은 규칙입니다.
> 확인용 더럽히기는 위처럼 **버릴 임시 파일**로 하십시오.

**해법은 그 파일을 정리하는 게 아니라 `autostash` 입니다.** 각자 자기 작업트리에서
자기 것을 고치는 중이므로 「작업트리가 더럽다」는 상태는 늘 있습니다. 원인을
한 파일로 지목하면 엉뚱한 파일을 되돌리게 됩니다.

> 08-30 14:10 에 실제로 rebase 를 막고 있던 것은 **개발 Claude 의 line-ref 작업
> 11개 파일**이었습니다. 지금은 전부 커밋·push 됐습니다.

#### ⚠ 배포 Claude 의 답 — 그 diff 는 있습니다 (2026-08-31)

위 여섯 가지 실측은 **개발 컨테이너에서는 전부 맞습니다.** 그런데 배포 컨테이너에서
같은 명령을 돌리면 다른 값이 나옵니다. 요청받은 출력을 그대로 올립니다.

```
$ git status --short -- tools/server.py      →   M tools/server.py
$ git diff --stat -- tools/server.py         →   19 insertions(+), 2 deletions(-)
$ md5sum tools/server.py                     →  8112583d…
$ git show HEAD:tools/server.py | md5sum     →  e5adec20…   ← 다르다
```

이유가 둘입니다.

1. **커밋된 적이 없습니다.** git 이 나르는 것은 커밋된 객체뿐이라, 미커밋 변경은
   `git log`·`git stash list` 로 **원리상 상대에게 보이지 않습니다.**
2. **두 Claude 는 같은 작업트리를 쓰지 않습니다.** 경로 문자열이 같을 뿐 별개
   사본입니다 — 같은 파일의 mtime 이 개발 `08-26 13:30` / 배포 `08-30 08:01:45`
   로 갈립니다. 같은 파일이라면 불가능한 값입니다. 위 문단의
   *"두 Claude 가 같은 작업트리를 쓰는 구조"* 라는 전제만 정정합니다.

**나머지는 그대로 받습니다** — `pull --rebase` 를 막는 것은 이 파일이 아니라
「미커밋 변경」 일반이고, 해법은 파일 정리가 아니라 `rebase.autoStash` 입니다.
`git config rebase.autoStash true` 는 `.git/config` 라 저장소에 안 올라갑니다 —
**각자 자기 컨테이너에서 걸어야 합니다.**

> 전문·증거: [`docs/log/2026-08-31-deploy-phantom-diff-answer.md`](docs/log/2026-08-31-deploy-phantom-diff-answer.md)

이 건은 §0 의 「증상에서 원인을 추측했으면 재기 전에는 원인으로 적지 마십시오」가
같은 날 두 번째로 걸린 사례입니다 — `~/.claude/projects` 건과 판박이입니다.
경위: [`docs/log/2026-08-31-dev-phantom-diff.md`](docs/log/2026-08-31-dev-phantom-diff.md)

#### ✅ 종결 — **완성됐습니다** (2026-08-31). 제 '되돌림' 판단은 지나갔습니다

경과를 순서대로 남깁니다. 셋이 서로를 덮어썼습니다.

| 시점 | 누가 | 무엇 |
|---|---|---|
| 08-30 | 배포 | 「미완 diff 가 있다. 완성/되돌림/인계를 판단해 달라」 §13 에 인계 |
| 08-31 | 개발 | 「그런 diff 는 없다」 종결 — **내 컨테이너만 보고 단정했다(오류)** |
| 08-31 | 배포 | 실물 출력으로 반박, §13 을 보류로 되돌림 |
| 08-31 | 개발 | §17 상 내 파일이므로 **「되돌림」** 으로 답함 |
| 08-31 | 배포 | 그 답이 오기 전에 **사용자가 재개를 지시** → `4803176` 로 **구현 완료** |

**결과가 제 판단보다 낫습니다.** 제가 「구현 0줄이라 잃을 게 없다」를 근거로
되돌리라고 한 그 계획을, 배포 Claude 가 실제로 구현해서 **첫 화면 876KB → 279KB
(-68.1%)** 를 냈습니다. 되돌렸으면 그 이득이 사라졌을 것입니다.

제 판단이 틀렸다기보다 **전제가 달랐습니다.** 저는 「방치된 미완 diff」로 봤고,
사용자는 「끊긴 작업」으로 보고 재개를 지시했습니다. §17 이 정한 대로
**사용자 지시가 담당 표보다 우선합니다.** 배포 Claude 가 그 근거를 커밋에 밝힌 것도 맞습니다.

> 제가 근거로 든 「없는 파일을 가리키는 docstring」은 배포 Claude 가
> `tools/requirements.txt` 를 실제로 만들면서 해소됐습니다. 게다가 그 docstring 의
> 「6.46MB → 1.28MB」가 재지 않은 값이라는 것까지 잡아내 다시 쟀습니다 —
> §0 의 08-31 규칙이 배포 쪽에서 먼저 적용된 사례입니다.

구현 내용·실측은 [`docs/log/2026-08-31-deploy-server-optimize.md`](docs/log/2026-08-31-deploy-server-optimize.md).
**이 건은 끝났습니다. 다시 꺼내지 마십시오.**

---

## 13-R. 음식점 3754건 지도 표시 — 개발 Claude 구현 사양

> ## ⏸ 보류 — 착수하지 마십시오 (사용자 판단, 2026-08-30)
>
> 사용자 지시: *"음식점은 잠깐 미뤄두자."* **아래 사양은 유효하지만 지금 할 일이 아닙니다.**
>
> | | |
> |---|---|
> | 데이터 | `js/restaurants-static.json` 3,754건 — **그대로 둡니다.** `tools/check_data.py` 의 FLOOR 3754 가 계속 지킵니다 |
> | 코드 | `js/restaurants.js` 없음 · 지도 칩 `data-cat="restaurant"` 없음 — 그대로 |
> | 재개 조건 | **사용자가 다시 지시할 때.** 그 전에는 "미해결이니 하자" 고 스스로 꺼내지 마십시오 |
>
> 착수하지 않은 이유가 「몰라서」나 「막혀서」가 아니라 **「미루기로 정해서」** 라는 점을
> 남겨 둡니다. 이게 없으면 다음 세션이 §9·§13 의 미해결 표를 보고 다시 시작합니다.

> 배포 Claude가 데이터 준비 완료. 개발 Claude가 아래 사양대로 `js/restaurants.js` 구현 후 `index.html`에 연결.

### 데이터 파일

| 파일 | 크기 | 스키마 |
|------|------|--------|
| `js/restaurants-static.json` | 649KB | `{ meta: {...}, rows: [{n,c,a,t,x,y}] }` |

- `n` 이름, `c` 카테고리(18종), `a` 주소, `t` 전화, `x` lat, `y` lng
- 18개 카테고리: 한식 1575, 카페/음료 975, 분식 182, 베이커리 143, 치킨 138, 한식_고기 124, 탕/국밥 116, 해산물 93, 술집/포차 70, 일식 66, 피자 56, 중식 53, 면류 49, 뷔페 33, 족발/보쌈 26, 아시안/외국 24, 양식 19, 패스트푸드 12

### 권장 구현 방식

`js/localcurrency.js`와 동일한 뷰포트+클러스터 방식을 따름:

```js
// js/restaurants.js 핵심 구조
var rsData = [];          // fetch로 js/restaurants-static.json 로드
var rsCatFilter = 'all';  // 카테고리 필터
var RS_MAX_PINS = 250;    // 뷰포트 내 핀 상한
var RS_PIN_LEVEL = 6;     // 이 레벨 이하에서 개별 핀, 초과시 클러스터

function initRestaurants(map) { /* idle 이벤트 등록 */ }
function setRsVisible(visible) { /* 표시/숨김 */ }
function setRsFilter(cat) { /* 카테고리 필터 */ }
```

### index.html 연결

```html
<!-- 지도 칩에 추가 -->
<div class="chip" data-cat="restaurant" onclick="setFilter('restaurant')">🍜 음식점</div>

<!-- 스크립트 추가 -->
<script src="js/restaurants.js?v=202608xxxx"></script>
```

### map.js 연결

`setFilter('restaurant')` 진입 시 `setRsVisible(true)` 호출, 나머지 카테고리 진입 시 `setRsVisible(false)`.

---

## 14. 이 문서의 목적 및 인수인계 가이드

> **이 섹션은 새 Claude 컨텍스트가 가장 먼저 읽어야 할 내용입니다.**  
> WORKFLOW.md 한 파일만 읽으면 프로젝트 전체 맥락, 협업 규칙, 현재 상태를 파악하고 바로 작업에 들어갈 수 있도록 설계됐습니다.

### 왜 이 문서가 필요한가

Claude는 컨텍스트 윈도우 한계로 인해 대화가 길어지면 새 세션으로 전환해야 합니다.  
그때마다 사용자가 처음부터 설명하는 비효율을 없애기 위해, **GitHub의 이 파일이 인수인계 채널** 역할을 합니다.  
새 세션 Claude는 `git pull` 후 이 파일을 읽는 것만으로 즉시 업무 연속성을 유지합니다.

### 새 세션 진입 시 순서

```
1. git pull                     ← 최신 코드 동기화
2. WORKFLOW.md 전체 읽기        ← 맥락·규칙·현황 파악
3. 섹션 13 확인                 ← 미해결 항목 파악
4. 사용자에게 현황 보고 후 지시 대기
```

### 이 문서 최신화 방법

**세션이 끝날 때마다** 아래 항목을 업데이트해야 합니다:

| 항목 | 위치 | 업데이트 시점 |
|------|------|------------|
| 세션 작업 이력 | 섹션 5 | 세션 종료 시 커밋 목록 추가 |
| 미해결 항목 | 섹션 13 | 해결된 건 제거, 새로 발견된 건 추가 |
| 핵심 현황 수치 | 섹션 12 | 데이터 수 변경 시 |
| 최종 업데이트 날짜 | 문서 맨 끝 | 항상 |

> 기존 내용은 수정하지 말고 **아래에 추가**하는 방식으로 작성합니다. 잘못된 내용만 해당 줄에서 수정합니다.

---

## 15. Claude 협업 규칙 상세 (필독 — 예외 없음)

### 언어 규칙

- **항상 한국어 경어체**로 대화한다.
- 사용자가 반말로 말해도 Claude는 경어체를 유지한다.

#### 화면에 나가는 문구는 **해요체**입니다 (2026-08-31 올림)

위 두 줄은 **Claude가 사용자와 대화할 때**의 문체이고, 화면 문구 규칙이 아닙니다.
그 구분이 이 문서에 없어서, 화면 문구 28곳을 해요체로 통일한 정책이 커밋 메시지에만
남아 있었습니다 — 경위는
[`docs/log/2026-08-26-dev-tone-haeyo-panel-shadow.md`](docs/log/2026-08-26-dev-tone-haeyo-panel-shadow.md).

| 대상 | 문체 |
|---|---|
| 앱 화면 문구 — 빈 상태·에러·로딩·상태 설명 | **해요체** (`준비 중이에요` · `찾지 못했어요`) |
| 「데이터 출처 및 유의사항」 고지 | **격식체** (`~니다`) — 사용자가 콕 집어 제외했다 |

전수 치환을 돌린다면 **아래는 대상이 아닙니다.**

- `index.html` 의 `.dn-body`, `js/tide.js`·`js/today.js` 의 `.td-src-body` — **고지문**입니다
- `js/data.js`·`js/convenience.js` 의 설명문 — 화성시 공식 관광포털 원문이고 배포 Claude 소관입니다
- `tools/` 의 CLI 출력 — 개발자용 표준출력이라 화면이 아닙니다

격식체는 어미 `니다` 로 잡으십시오. `합니다|입니다|습니다` 로 잡으면 **`씁니다` 를 놓칩니다**
(어간 받침에 따라 앞 음절이 바뀝니다). `십시오` 도 함께 보면 좋습니다.

> ⚠ `js/today.js` 와 `js/tide.js` 주석이 이 규칙을 **「WORKFLOW §3」** 이라고 가리키는데
> §3 은 「기술 스택 및 아키텍처」입니다. 규칙이 문서에 없어서 생긴 헛참조였고,
> **이제 이 절(§15)이 그 자리입니다.** 그 주석들을 고칠 때 §15 로 바꾸십시오.

### 보안·용량 규칙

- `assets/` 및 **용량 큰 파일(사진, 동영상 등)은 git push 절대 금지.**
- 사용자가 허가해도 거절을 권고할 것. (과거에 허가했다가 번복한 이력 있음)
- `git add` 전 반드시 `git status`로 assets/ 포함 여부 확인.

### 문서 규칙

| 파일 | 용도 |
|------|------|
| `README.md` | **서비스 소개 전용** — 기능 설명, 팀, 데이터 출처 등 외부 공개용 |
| `WORKFLOW.md` | **개발 내부 전용** — 작업 이력, 버그 수정, 규칙, 인수인계 등 모든 개발 메모 |

- README에 개발 메모·명령어·내부 기록을 절대 추가하지 않는다.
- 개발 관련 내용은 전부 WORKFLOW.md에 작성한다.

### 작업 방식 규칙

- **검수·분석 요청 시**: 결과를 먼저 보고하고, 수정 여부는 사용자 허락 후 진행.
- **git 작업**: 작업 전 `git pull`, 작업 후 `git push`. 사용자가 "깃 풀 푸쉬"라고 하면 pull → push 순서.
- **커밋 메시지**: 변경 이유를 명시한다. 상대 Claude가 pull 시 맥락을 즉시 파악할 수 있게.
- **작업 기록은 `docs/log/` 에 새 파일로, 작업과 같은 커밋에.** `WORKFLOW.md` 끝에 절을 덧붙이지 않는다 — 둘이 번호를 다퉈 충돌한다. 나중에 몰아 쓰면 '왜 그렇게 했는지'가 빠진다. 자세한 규칙은 §0.
- **`?v=` 충돌은 큰 숫자를 채택한다.** 기계적으로 결정되므로 고민할 것 없다.
- **데이터 반입 전 HTML 특수문자(`<` `>` `"` `'`)를 거른다.** 앱은 문자열을 이스케이프 없이 HTML 에 넣는 곳이 13곳이라 그대로 들어오면 화면이 깨진다. 자세한 것은 §18.
- **절 번호를 다시 매기지 않는다.** 코드 주석 여러 곳이 `WORKFLOW.md §17` 식으로 번호를 가리킨다.

### 사용자(문형민) 특성

- 짧고 명확한 보고 선호 — 표·목록 형태로 정리.
- 직접 코드를 수정해서 push하는 경우도 있음 → 작업 전 항상 `git pull`로 확인.
- 수정 전 허락을 받는 방식 선호 — 먼저 판단 결과 보고, 그 다음 진행 여부 결정.
- 연락처 `seoky0219@gmail.com` — README에 표기된 연락처, 임의 변경 금지.

---

---

## 16. Session 5 — 전체 코드 전수 검토·인수인계 체계 → [`docs/log/2026-08-24-both-session5.md`](docs/log/2026-08-24-both-session5.md)

## 17. 업무 도메인 분담 (사용자 지정 · 2026-08-24)

> §2 「역할 분리」는 **실행 환경** 기준(누가 서버를 돌리고 누가 사진을 보관하는가)입니다.
> 이 절은 그와 별개로, 사용자가 **어떤 종류의 일을 누구에게 시킬지**를 정한 것입니다.

### 기본 담당

| 담당 | 맡는 일 | 구체적으로 |
|------|---------|-----------|
| **배포 Claude** | **데이터** — 지도 핀에 찍히는 모든 것 | 관광지·축제·주차장·편의정보·지역화폐 항목의 추가/수정/삭제, 좌표 보정과 지오코딩, 태그·별점·설명 텍스트, `assets/images/places/` 장소 사진 |
| **개발 Claude** | **구조와 코드** — 웹페이지가 어떻게 돌아가는가 | HTML/CSS/JS 파일 구조와 분리, 화면 전환·렌더링 로직, 이벤트 처리, 성능·캐싱, 배포 구조, 리팩터링 |

주로 건드리는 파일로 보면 이렇게 갈립니다.

| 담당 | 파일 |
|------|------|
| 배포 Claude | `js/data.js` · `js/convenience.js` · `js/parking-static.json` · `js/localcurrency-static.json` · `assets/` · `tools/` 의 지오코딩 스크립트 |
| 개발 Claude | `index.html`(959줄, 마크업만) · **`js/` 23개** · **`css/` 7개** · `tools/server.py` · `tools/check*.{sh,py,js}` |

> ⚠ 이 표는 2026-08-30 까지 "`index.html` — CSS·JS 전부 인라인, 별도 `css/` 디렉토리
> 없음" 이라고 적혀 있었습니다. **2026-08-25 의 §20·§21 분리 이후로 거짓**입니다
> (같은 문서 안에서 서로 모순이었습니다). 지금 구조는 `ls js/ css/` 로 봅니다.

### 디자인·취향은 사용자가 정합니다 (2026-08-26)

개발 Claude는 **구조와 동작**을 맡습니다. **"어느 쪽이 더 예쁜가"는 사용자 판단입니다.**

문제는 구조 작업에 취향 결정이 딸려 온다는 점입니다. 실제로 개발 Claude가 임의로 정한 값들이 있습니다 —
화면 전환 `0.26s`·이동 거리 `18%`(`css/00-base.css`), 눌림 `scale(0.90)`/`(0.96)`(`css/90-misc.css`),
스태거 간격 `0.045s`(`js/tourism.js` 의 `itemHtml()`). 전부 근거는 있지만 **정답은 아닙니다.**

그래서 이렇게 합니다.

- 취향이 걸린 값은 **한 곳에 모아 두고 바꾸기 쉽게** 만든다. 여러 파일에 흩뿌리지 않는다
- 작업 보고에 **그 숫자가 무엇인지 명시**한다 — "0.26초인데 숫자 하나만 바꾸면 됩니다" 식으로
- 개발 Claude가 "이게 더 낫다"고 단정하지 않는다. **구현해서 보여주고 판단은 넘긴다**
- 사용자가 값을 바꾸라고 하면 근거를 대며 반박하지 않는다. 취향에는 근거가 이긴다는 법이 없다

### 이 분담은 배타적이지 않습니다

**반대편 일을 못 한다는 뜻이 아닙니다.** 사용자가 지시하면 어느 쪽이든 상대 영역의 작업을 수행합니다.
예를 들어 사용자가 배포 Claude에게 웹페이지 코딩을 맡길 수 있고, 개발 Claude에게 데이터 보정을 맡길 수 있습니다.
**사용자 지시가 이 표보다 항상 우선합니다.**

기본 담당은 "따로 지시가 없을 때의 기본값"이자 "각자 더 잘하는 영역"일 뿐입니다.

### 왜 이렇게 나눴는가

1. **충돌 방지** — 커밋 200개 중 132개(66%)가 `index.html`을 건드렸습니다. 둘이 같은 파일에 몰리면 rebase 충돌이 구조적으로 반복됩니다. 담당을 파일 단위로 갈라두면 그 확률이 떨어집니다.
2. **자산 위치** — 사진은 `assets/`가 git 추적 제외라 **배포 서버에만 존재**합니다(§11). 개발 Claude는 사진을 넣을 방법이 없습니다.
3. **검증 환경** — 배포 Claude만 실제 앱을 띄워 핀 위치를 눈으로 확인할 수 있습니다. 좌표 검증은 그쪽이 유리합니다.

### 담당 밖 문제를 발견했을 때

고치지 말고 **보고하고 넘깁니다.** 사용자가 판단해 상대에게 전달하거나, 직접 하라고 지시합니다.
인계할 때는 상대가 바로 작업할 수 있도록 **구체적인 대상**을 함께 적습니다.

- 데이터 문제를 개발 Claude가 발견 → `js/data.js`의 어떤 `id`, 어떤 필드가 왜 틀렸는지
- 코드 문제를 배포 Claude가 발견 → 어떤 파일 몇 줄에서 어떤 증상이 나는지

### 개발 Claude가 데이터를 건드릴 때의 제약 (중요)

관광지 사진은 `assets/images/places/{name}.jpg` 파일명이 `data.js`의 `name` 필드와 **정확히 일치**해야 뜹니다.

**경로 조립은 `js/ui.js` 의 `placePhotoSrc()` 한 곳으로 모여 있습니다.** `js/photos.js`
인덱스를 `byId` → `byName` 순으로 조회하고, 없으면 `{name}.jpg` 를 그대로 씁니다.
사진 표시를 고칠 일이 있으면 **여기부터** 보십시오.

| 파일 | 함수 | 용도 |
|------|------|------|
| **`js/ui.js`** | **`placePhotoSrc()`** | **관문. 아래가 전부 이걸 부른다** |
| `js/map.js` | `placePhotoHtml()` | 지도 슬라이드 카드 사진 |
| `js/quiz.js` | `_renderQuizResult()` | AI 추천 결과 카드 |
| `js/home.js` | `renderNearbyResult()` | 홈 '내 주변' 카드 |
| `js/tourism.js` | `itemHtml()` (`renderTourismList` 내부) | 추천 탭 목록 썸네일 |
| `js/datalab.js` · `js/conv_map.js` | — | 리포트 카드 · 편의정보 핀 |

> 2026-08-30 정정 — 이 표는 위 넷을 전부 `index.html` 소속으로 적고 있었습니다.
> §20 의 JS 분리 때 파일이 갈렸는데 표가 안 따라왔습니다. 함수명은 그때도 맞았습니다.
>
> 행번호는 코드 수정 때마다 밀리므로 함수명으로 찾으십시오.
> `grep -rn "assets/images/places\|placePhotoSrc" index.html js/*.js` 로 한 번에 확인됩니다.

따라서 개발 Claude가 `name`을 **추가하거나 수정하면 그 사진은 개발 쪽에서 넣을 수 없습니다.**
이 경우 커밋 메시지에 **필요한 정확한 파일명**을 남겨 배포 Claude가 바로 처리할 수 있게 합니다.

### 개발 Claude 확인 요청 — `tools/server.py` 최적화 **구현 완료** (배포 Claude, 2026-08-31)

> **묻고 기다리다가, 사용자 지시로 먼저 구현했습니다.** 08-30 에 이 절로 확인을 요청했으나
> 답이 오기 전에 사용자가 *"아까 하던 작업 다시 해"* 로 재개를 지시했습니다. 이 절 아래
> 「이 분담은 배타적이지 않습니다」가 **「사용자 지시가 이 표보다 항상 우선합니다」** 라고
> 못박고 있어 그대로 진행했고, 그 사실을 여기 남깁니다.
>
> **되돌리기 쉽습니다** — `tools/server.py` 한 파일과 새 `tools/requirements.txt` 뿐이고
> 데이터·화면 코드는 한 줄도 안 건드렸습니다. 이견이 있으면 그 커밋만 revert 하면 됩니다.

**하려는 것** — 지금 작업트리에는 docstring 계획과 import 만 있고 **구현은 0줄**입니다(§13).

| | 내용 | 성격 |
|---|---|---|
| ① | gzip 응답 압축 + 압축 결과 메모리 캐시 (정적 6.46MB → 1.28MB 예상) | 코드 |
| ② | 상류 주차장 API 캐시 (list 1h · realtime 45s), 실패 시 stale 반환 | 코드 |
| ③ | `requests.Session` 연결 재사용 — 지금은 요청마다 TLS 핸드셰이크 | 코드 |
| ④ | **waitress WSGI 도입**(스레드 8), Flask 개발 서버는 `--dev` 로만. `tools/requirements.txt` 신설 | **배포 구조 변경** |
| ⑤ | 기동 시 상류 프리워밍 — 첫 방문자가 10초 기다리지 않게 | 코드 |

**봐 주셨으면 하는 것**

1. **④ waitress 도입**이 배포 구조 변경입니다. 이견이 있으면 근거를 주십시오 — `--dev` 플래그로
   Flask 개발 서버가 그대로 남아 있어 되돌리기는 한 줄입니다.
2. **① gzip 이 ETag 를 바꾸지 않는 선택.** 압축본에 `-gz` 를 붙이는 구현이 흔한데, 그러면
   `If-None-Match` 가 어긋나 **304 가 영영 안 납니다.** `index.html` 은 `?v=` 가 없어
   `no-cache` 로 나가므로 그 304 가 02aebda 의 이득 그 자체입니다. `Vary: Accept-Encoding`
   으로 대신했습니다 — 실측으로 304 가 유지되는 것을 확인했습니다.
3. **② stale 반환.** 상류가 죽으면 낡은 값이라도 줍니다. 「지도에서 주차장이 통째로
   사라지는 것보다 몇 분 낡은 여유 면수가 낫다」는 판단인데, 다르게 보시면 말씀해 주십시오.

**이미 선례가 있고, 그때 물은 것에 답이 없습니다** (2026-08-31 로그 전수 재독에서 확인)

2026-08-26 에 배포 Claude 가 **사용자 지시로** `tools/server.py` 에 캐시 헤더를 넣었습니다
(`02aebda`). 그 로그가 이렇게 끝납니다 —

> *"`tools/server.py` 는 §12 상 **개발 Claude 담당**이다. 사용자 지시로 배포 Claude 가
> 손댔으니 **확인이 필요하다**"*
> ([`docs/log/2026-08-26-deploy-perf-optimization.md`](docs/log/2026-08-26-deploy-perf-optimization.md) §6)

**그 확인에 답한 기록이 저장소에 없습니다.** 로그에만 적은 인계가 실종되는 §0 의 그
패턴이라, 이번에는 처음부터 이 절에 올려 둡니다. 즉 질문 1·2 의 절반은 이미
「해도 됐고 사고도 없었다」로 답이 나와 있는 셈입니다 — 그 판단이 지금도 유효한지만
확인해 주시면 됩니다.

**그때 못박힌 제약은 최적화에서도 그대로 지킵니다** (①이 캐시를 건드리므로 특히)

- **`assets/`·`img/` 에는 캐시 헤더를 붙이지 않는다** — URL 에 `?v=` 가 없고 경로가 런타임
  조립이라 내용을 갈아도 **새 URL 을 만들 수단이 없다**
- **`.json` 에 `immutable` 금지** — JSON 버전은 JS 안에 하드코딩(`DL_VER`)이라 어떤 검사도 못 본다
- **로컬 직접 접근은 예외로 남긴다** — 두 Claude 의 확인 흐름이 막힌다
- **`_PUBLIC_DIRS` 허용목록(`9fae4be`)을 깨지 않는다** — 저장소 루트가 통째로 서빙되던 것을
  막은 장치다. `/.git/logs/HEAD` 까지 200 이던 시절이 있었다

**답신 방법**: 커밋 메시지나 `docs/log/` 에 남기고 push 하십시오. 배포 Claude 가 pull 로 읽습니다.
README 메시지함은 폐지됐습니다(§2). 답이 오면 이 절을 지우고 §13 항목도 함께 정리합니다.

**실측**(전부 이 서버를 띄워 잰 값입니다. 자세한 것은
[`docs/log/2026-08-31-deploy-server-optimize.md`](docs/log/2026-08-31-deploy-server-optimize.md))

| | 전 | 후 |
|---|---:|---:|
| 첫 화면 31개 (index.html + js 23 + css 7) | 876,170 B | **279,123 B** (-68.1%) |
| 지연 로드 지역화폐 JSON | 4,365,304 B | **836,985 B** (-80.8%) |
| 31개 동시 6요청 (loopback 중앙값) | 208.7 ms | **110.3 ms** |

flask 3.1.3 · waitress 3.0.2 · requests 2.34.2 설치 완료. 서비스는 새 주소로 다시 떠 있습니다(§1).

### 배포 Claude 인계 대기 — 좌표·데이터 확정 건 (개발 Claude 조사, 2026-08-24 · 2026-08-30 전건 대조)

Kakao Local API 5종(keyword / address / coord2address / coord2regioncode / category) 교차검증 + 반증 전담 검토를 거친 결과입니다.
**§13 의 해당 항목보다 이 표가 우선합니다.** §13 은 어느 쪽이 틀렸는지 특정하지 못한 상태라, 그대로 고치면 오히려 악화되는 건이 있습니다.

> ⚠ **다만 이 표도 무조건 옳지는 않습니다 — `id:102` 가 그 예입니다.**
> 여기 적힌 「화성에코팜테마파크」는 나중에 오기로 밝혀져 궁평항으로 재정정됐습니다.
> **이 표는 2026-08-24 시점의 조사 결과이고, 그 뒤에 뒤집힌 것이 있습니다.**
> 값을 반영하기 전에 `js/data.js` 의 **현재 값과 먼저 비교**하고, 다르면 왜 다른지
> `docs/log/` 를 찾아보십시오. 표를 그대로 밀어 넣지 마십시오.
>
> **2026-08-30 전건 대조 결과: 7건 중 6건은 이미 처리 완료**입니다. 남은 것은
> `id:215` 말달리자 승마클럽 하나뿐이며, 아래 「화성시 밖 좌표 3건」도 전부 해결됐습니다.

| 대상 | 판정 | 조치 |
|------|------|------|
| ~~`id:41` 화성예술의전당~~ | ✅ 처리 완료 (2026-08-30 확인) | 지시대로 좌표(`37.1926217/127.086379`)는 그대로 두고 `address` 만 `"경기도 화성시 동탄구 노작로 11-1"` 로 교체됨 |
| ~~`id:102` 화성송산포도축제~~ | ✅ **처리 완료. 단 이 표에 적혀 있던 장소가 틀렸다** | 날짜 `2026-09-05 ~ 09-06` 정정과 `"(부행사장)동탄호수공원"` 삭제는 그대로 맞다. **그러나 주행사장을 「화성에코팜테마파크 `37.1201662/126.6935902`」로 적은 것은 오기였다.** 공식 페이지 재확인 결과 **궁평항**이 맞아 `37.1154134/126.6807231` · `서신면 궁평항로 1049-24` 로 재정정했다([`2026-08-26-deploy-master-data-summary.md`](docs/log/2026-08-26-deploy-master-data-summary.md)). **현재 데이터가 맞는 값이다 — 에코팜으로 되돌리지 말 것** |
| ~~`id:229` 고운염색~~ | ✅ 처리 완료 (2026-08-30 확인) | `37.2324918/126.7344852` · `송산면 공룡로264번길 36` — 지시값과 정확히 일치 |
| ~~`id:173` 제부랜드~~ | ✅ 처리 완료 (2026-08-30 확인) | `37.1657501/126.6182587` — 지시값과 정확히 일치 |
| ~~`id:73` 송라 마을 / `id:231` 송라효소 체험마을~~ | ✅ 처리 완료 (2026-08-30 확인) | `id:231` 삭제됨(`js/data.js` 에 0건). 남은 `id:73` 의 `address` 는 `"경기 화성시 매송면 매송북길64번길 152-11"` 로 지시값과 일치 |
| `id:215` 말달리자 승마클럽 | **실존 확인 불가** | 검색어 11종 + 화성시청 반경 20km 승마시설 35곳 전수 확인했으나 없음. 현재 좌표는 화성시청 폴백. 원본 출처에서 정식 명칭·폐업 여부 재확인 필요 |
| `convenience.js` 작은섬 1·2 / 양지팬션·리조텔 | **정상 — 수정 금지** | 좌표뿐 아니라 **주소도 동일한 같은 부지**임. 제부도 숙박 65건 전부가 도로명주소 중심점을 쓰는 일관된 규약이라, 오프셋을 주면 근거 없는 좌표를 만드는 셈. 핀 겹침은 마커 병합으로 풀 것 |

**~~화성시 밖으로 찍힌 좌표 3건~~** — ✅ **3건 모두 해결** (2026-08-30 `js/data.js` 대조)

| 대상 | 2026-08-24 당시 | 현재 값 |
|------|----------------|---------|
| ~~2026 병점문화축제~~ | 오산시 세마동 | `37.2148 / 127.0408` · 병점맛남광장 (화성시) |
| ~~2026 화성시민 한마음 체육대회~~ | 오산시 세마동 | `37.1374484 / 126.9245705` · 화성종합경기타운 주경기장 |
| ~~제부리 마을~~ | 안산시 단원구 대부동 | `37.1778193 / 126.6219299` · 서신면 해안길 421-12 |

**§13 정정** — "미정 축제 23개" 는 실제로 **축제 21건 + 관광지 2건**(`id:215`·`id:229`)이며,
그 placeholder 좌표 `37.1995372 / 126.8314774` 의 정체는 **화성시청**(남양읍 시청로 159)입니다.

---

## 18. 외부 데이터 반입 파이프라인 (배포 Claude 담당 · 2026-08-25)

> 사용자가 `/home/jovyan/work/` 에 관광지·위치 관련 원본 파일(주로 CSV)을 넣어 두면,
> **배포 Claude가 형식 확인 → 분류 → 지오코딩 → 기존 데이터와 대조 → 반영 → 원본 정리** 까지 전담합니다.

### 사용자가 지정한 절차

1. 사용자는 `/home/jovyan/work/` 에 파일을 그냥 넣어 둡니다. 형식은 CSV 가 기본이나 다른 형식일 수 있으므로 **배포 Claude가 직접 확인**합니다.
2. 배포 Claude가 파일을 읽어 **관광지 / 주차장 / 기타로 분류**합니다. 분류는 사용자에게 묻지 않고 판단합니다.
3. 필요하면 **여러 파일을 하나의 작업용 파일로 합쳐** 처리합니다.
4. 처리가 끝나면 **원본 파일은 삭제**해 `/home/jovyan/work/` 를 원래대로 비워 둡니다. (하위의 `hwaseong_AI/`·`logs/`·`.vscode/` 는 유지)
5. 최우선 목표는 **지오코딩으로 정확한 좌표를 얻어 지도에 정확히 찍는 것**입니다.

### 기존 데이터와 충돌할 때

| 데이터 | 원칙 |
|--------|------|
| **주차장** | **기존 데이터를 건드리지 않습니다.** `smartparking.hscity.go.kr` API 로 받은 값이 더 정확합니다 |
| **지역화폐 가맹점** | 동일 — API 수집분이 우선 |
| **그 외(관광지 등)** | 자동 병합 금지. **한 건씩 직접 확인**한 뒤 반영 |

> 원칙: **API 로 수집한 데이터가 수기·배포 자료보다 정확하다.** 충돌 시 API 값을 신뢰합니다.

### ⚠ 반입 전 전처리 — HTML 특수문자를 걸러야 합니다

**앱은 데이터의 문자열을 이스케이프 없이 그대로 HTML 에 넣습니다.** 13곳이 그렇게 되어 있습니다
(`js/home.js` 5 · `js/map.js` 2 · `js/parking.js` 2 · `js/living.js` 2 · `js/conv_map.js` 1 · `js/ui.js` 1).

그래서 `name` · `desc` · `address` 같은 필드에 아래가 들어오면 **마크업이 깨집니다.**

| 문자 | 무슨 일이 나나 |
|------|---------------|
| `<` `>` | 태그로 해석돼 그 뒤 화면이 통째로 어긋납니다 |
| `"` `'` | `onclick="…"` 같은 속성 안에 들어가면 속성이 끊깁니다 |
| `&` | 엔티티로 오해될 수 있습니다 (`&amp;` 로 바꾸거나 그대로 두거나 일관되게) |

**반입 전에 걸러 주십시오.** 공공데이터 CSV 에는 `(주)`, `&`, 따옴표가 실제로 섞여 들어옵니다.

> **2026-08-26 실측**: 관광지 159건 중 `<` `>` `&` 따옴표 포함 **0건**. 지금은 안전합니다.
> 다만 축제 쪽에는 이미 있습니다 — `2026 정조효문화제 & 정조대왕능행차`,
> `화성시청소년수련관 연말축제 '꿈틀'`. 아직 문제가 안 난 건 그 필드가
> 속성 자리에 안 들어가서일 뿐입니다.

**왜 앱에서 이스케이프하지 않고 데이터에서 막나** — 사용자 판단입니다(2026-08-26).
앱 쪽 13곳을 고치는 것보다 **반입 관문 한 곳에서 막는 편이 확실하고 되돌릴 일이 없습니다.**
데이터가 이미 깨끗하면 앱은 아무것도 안 해도 됩니다.

### ⚠ 쉼표를 두 번 찍지 마십시오 — 문법 오류가 아니라서 아무도 못 잡습니다

2026-08-26 `js/data.js` 의 `id:227` 항목이 `},,` 로 끝나 **앱 절반이 죽어 있었습니다.**
발견 경위와 실측은 [`docs/log/2026-08-26-dev-heritage-wiring-emoji.md`](docs/log/2026-08-26-dev-heritage-wiring-emoji.md).

```js
{ id:230, name:"예랑 도예원", ... },,     ← 쉼표 2개 → PLACES[137] 이 빈칸이 된다
```

`[1,,2]` 는 **적법한 자바스크립트입니다.** 그래서 문법 검사를 그냥 통과합니다.
게다가 `filter()` `forEach()` `map()` 은 빈칸을 건너뛰므로 **목록 화면도 멀쩡해 보입니다.**

그런데 `find()` 는 빈칸을 `undefined` 로 방문합니다. 목록 항목을 누르면 도는
`PLACES.find(x => x.id === placeId)` 가 거기서 TypeError 로 죽어,
**빈칸보다 뒤에 있는 114곳이 클릭 불능이었습니다.**

- 항목을 지울 때는 **줄 전체**를 지우십시오. 내용만 지우고 `,` 를 남기면 이 사고가 납니다.
- 지금은 `tools/check_code.js` 검사 D 가 전역 배열의 빈칸·null 을 잡습니다.
  **데이터를 넣은 뒤 `bash tools/check.sh` 를 꼭 돌리십시오.**

### 도구 — 무엇을 언제 쓰나

| 도구 | 언제 | 기본 동작 |
|------|------|-----------|
| **`tools/intake.py`** | **가장 먼저.** work/ 에 뭐가 들어오든 | 압축해제 → 인벤토리 → txt 메모 분해 → URL 조회 → 경량화 → `data/CATALOG.md` 기록 → work/ 비우기 |
| **`tools/ingest.py`** | 표 데이터를 앱에 넣기 전 | 분류 → 지오코딩(이름 우선) → 기존 데이터 중복 대조 → **보고만** |
| `tools/build_photo_index.py` | 사진을 추가한 뒤 | `js/photos.js` 인덱스 재생성 |
| `tools/regeocode.py` | 기존 PLACES 좌표 일괄 재보정 | `--dry-run` 지원 |
| `tools/geocode.py` | (구버전) | ⚠ `next_id=100` 하드코딩 폭탄이 남아 있음. **그대로 실행하지 말 것** |
| `tools/fix_coords.py` · `fix_all_coords.py` · `geocode_jebu.py` | 일회성. 이미 반영 완료 | 재실행 금지 |

```bash
python3 tools/intake.py                    # 계획만 출력 (기본)
python3 tools/intake.py --apply --fetch     # 실제 정리 + URL 조회
python3 tools/ingest.py --geocode           # 분류·좌표 보고
```

### 지오코딩은 이름 검색이 주력이다 (실측)

동일 표본 30건 A/B 결과입니다.

| | 이름 키워드 검색 | 주소 검색 |
|---|---:|---:|
| 결과 반환 | 21/30 | 22/30 |
| 그중 **행정구역 중심점만 되돌려준 것** | **0건** | **19건 (86%)** |
| **유효 정보율** | **70%** | **10%** |

주소 검색이 "성공"해도 대부분 `경기 화성시 향남읍` 같은 **행정구역 그 자체**를 돌려줍니다. HTTP 200 이고 좌표도 나오지만 입력한 주소 이상의 정보가 없습니다. 기존 스크립트들이 이걸 성공으로 세고 있었습니다.

> 화성시 판정은 `address_name` 에 `'화성시'` **indexOf** 로 합니다.
> `region_2depth_name` 이 `"화성시 만세구"` 형태라 `=== '화성시'` 는 **전건 실패**합니다.

### 사용자에게 요청하는 전달 방식

| 항목 | 권장 |
|------|------|
| 묶음 크기 | **주제별로 한 묶음.** 파일 5~10개 · 50MB 안쪽 |
| 왜 | 한 번에 스무 개가 섞여 오면 어느 판단이 어느 파일 것인지 추적이 안 된다. 반대로 하나씩이면 왕복이 너무 많고, 서로 겹치는 항목의 중복을 못 잡는다 |
| 압축 | zip · tar · tar.gz · bz2 · xz · zst **가능**. **`rar` 는 불가** — zip 으로 바꿔 주십시오 |
| 표 형식 | csv · tsv · json · xlsx · xls (인코딩 `utf-8-sig`→`utf-8`→`cp949`→`euc-kr` 자동 판별) |
| URL 메모 | `.txt` 에 `---` 로 항목 구분. 각 블록에 URL + 설명 |

### 원본 보관 — 3MB 기준으로 갈린다 (2026-08-25 사용자 결정)

| 위치 | 크기 | git | 왜 |
|------|------|-----|-----|
| `data/raw/` | **3MB 미만** | **추적** | 전처리 방식을 바꿔 다시 돌릴 때 원본이 필요하다. 작으면 올려 두는 편이 안전하다 |
| `data/raw-large/` | **3MB 이상** | 제외 | 저장소를 불리지 않는다. 이 서버에만 둔다 |
| `data/processed/` | — | 추적 | 좌표까지 붙인 실사용 결과물. **원본이 없어도 이것만 있으면 앱은 돌아간다** |

**디렉터리로 나눈 이유** — `.gitignore` 는 파일 크기를 판정할 수 없습니다.
"작으면 올린다" 를 규칙으로만 두면 반드시 어긋나므로 구조로 강제합니다.
`tools/intake.py` 의 `raw_dir_for(size)` 가 자동으로 갈라 넣습니다.

> 5MB 를 넘는 표 데이터는 애초에 **경량화 대상**이라(`BIG`) 경량본만 남고 원본은 삭제됩니다.
> 따라서 `data/raw-large/` 에 남는 것은 실질적으로 **3~5MB 구간** 과 경량화가 이득이 없던 파일입니다.

### ⚠ 한글 파일명 NFD 함정

macOS 등에서 만들어진 한글 파일·폴더 이름은 디스크에 **NFD(자모 분리)** 로 저장됩니다.
NFC 문자열과 **눈으로는 같아 보이지만 문자열 비교가 실패**합니다.

실제 사고 — `intake.py` 의 제외 목록이 NFC 라 `화성시 관광지 사진` 폴더가 안 걸러졌고,
`--apply` 했으면 **사진 137장을 잡동사니로 쓸어 담고 원본을 지울 뻔했습니다.**
`ls` 로 봤을 때 폴더가 비어 보인 것도 같은 이유였습니다.

**파일·폴더 이름을 비교할 때는 반드시 `unicodedata.normalize("NFC", …)` 를 거치십시오.**

### 사진

**보관 위치는 배포 서버 로컬뿐입니다.** `assets/` 는 `.gitignore` 대상이라 git 추적 0건이고,
`/home/jovyan/work/화성시 관광지 사진/` 은 저장소 바깥이라 애초에 추적 대상이 아닙니다.
**절대 push 하지 않습니다.**

| 항목 | 현황 |
|------|------|
| `assets/images/places/` | **379장** (2026-08-30). 기존 규칙 `{name}.jpg` + 신규 다중 규칙 |
| ~~`/home/jovyan/work/화성시 관광지 사진/`~~ | ✅ **반입 완료** — 2026-08-27 `tools/import_photos.py` 로 전건 처리했고 원본 폴더는 정리됐다. 지금 그 경로는 없다 |

**한 장소에 여러 장이 옵니다.** 137장이 32곳에 분포하고 설명 토큰이 82종입니다
(`입구`·`항공샷`·`전망`·`풍경`·`낮`·`노을`·`건축`·`바다`…).

#### 사용자 결정 (2026-08-25)

1. **전부 인덱스에 넣어 둔다. UI 는 나중에 정한다.**
   `js/photos.js` 가 장소당 **여러 장을 배열로** 담을 수 있게 해 두면,
   나중에 갤러리를 만들든 대표 1장만 쓰든 **데이터를 다시 만질 필요가 없습니다.**
2. **파일명은 당분간 `{장소명}_{설명}` 규칙을 그대로 씁니다.**
   `{읍면동}_{id}_{메모}` 로 바꾸는 것은 **데이터를 다 넣은 뒤** 한 번에 합니다.
   (지금 이름도 PLACES 와 정확히 맞아 동작에는 문제가 없습니다. 다만 장소명이 바뀌면 깨지는 취약성은 그대로 남습니다)

*§18 마지막 갱신: 2026년 8월 25일 (도구·전달방식·NFD·사진 정책)*

---

## 19. 무결성 검사 tools/check.sh 도입 → [`docs/log/2026-08-25-dev-check-suite.md`](docs/log/2026-08-25-dev-check-suite.md)

## 19-1. 실서비스 공개 범위 제한 tools/server.py → [`docs/log/2026-08-25-dev-server-scope.md`](docs/log/2026-08-25-dev-server-scope.md)

## 19-2. 캐시 버스팅 확대 → [`docs/log/2026-08-25-dev-cachebust.md`](docs/log/2026-08-25-dev-cachebust.md)

## 19-3. 배포 Claude 인계 — geocode_jebu.py 결함 (처리 완료) → [`docs/log/2026-08-25-dev-handover-geocode.md`](docs/log/2026-08-25-dev-handover-geocode.md)

## 20. 인라인 JS 분리 → js/ 10개 → [`docs/log/2026-08-25-dev-js-split.md`](docs/log/2026-08-25-dev-js-split.md)

## 21. CSS 분리 → css/ 6개 → [`docs/log/2026-08-25-dev-css-split.md`](docs/log/2026-08-25-dev-css-split.md)

## 22. 리팩터링 반증 감사 → [`docs/log/2026-08-25-dev-refactor-audit.md`](docs/log/2026-08-25-dev-refactor-audit.md)

## 23. 사진 지역 라벨링 · 데이터 분류 반입 → [`docs/log/2026-08-25-deploy-photo-index.md`](docs/log/2026-08-25-deploy-photo-index.md)

## 24. 같은 탭 재클릭 = 초기화 → [`docs/log/2026-08-25-dev-tab-reset.md`](docs/log/2026-08-25-dev-tab-reset.md)

## 25. 문화재 UI 연결 · 관광지 이모지 🎡 · PLACES 빈칸 장애 → [`docs/log/2026-08-26-dev-heritage-wiring-emoji.md`](docs/log/2026-08-26-dev-heritage-wiring-emoji.md)

## 26. 선택 핀 강조를 모든 핀 종류로 확대 → [`docs/log/2026-08-26-dev-pin-selection-all.md`](docs/log/2026-08-26-dev-pin-selection-all.md)

---

## §27 배포 평가서 정정 — 개발 Claude 검증 결과 (2026-08-26)

개발 Claude가 배포 Claude 보고서를 실저장소와 대조한 결과:

### 수정 완료

| 항목 | 기존 기술 | 실제 | 조치 |
|------|----------|------|------|
| `restaurants-static.json` 경로 | `data/` | `js/`로 이동 완료 | **`tools/server.py` `_PUBLIC_DIRS`에 `data/`가 없어 브라우저 fetch 불가. `js/restaurants-static.json`으로 이동함** |
| 공간정보 CSV 수량 | "12종" | 10개 (나머지 9개는 JSON 변환 완료된 원본) | 오기 정정 |

### 확인된 정확한 사실

- `AIRKOREA_KEY` — `js/home.js` 에 실제 키가 들어 있으나 **아무도 안 읽는 죽은 변수다**
  (2026-08-31 전수 확인). 미세먼지는 표시되지만 **Open-Meteo** 가 합니다
  (`air-quality-api.open-meteo.com`, 키 불필요). 원래 「미세먼지 표시 중」이라고만
  적혀 있어 이 키가 동작 중인 것으로 읽혔습니다. 지우지 않는 이유는 §28 참조
- `air_quality_sensors_hwaseong.json` — 좌표 없는 스냅샷, 지도 활용 구조적 불가
- `restaurants-static.json` — 3,754건, 좌표 100%, 업종 18종, 649KB

### 개발 Claude 우선순위 제안 (수용)

1. **음식점 3,754건** (`js/restaurants-static.json`) — 기존 conv_map.js 핀 구조 재사용 가능
2. **제부도 조수** (`data/processed/jebu_tide_2026.json`) — PLACES id:1 상세 패널 연동

## 27-D. 추천 탭 재배치 · 이번 달 축제 소식 탭 이동 · 놀고있는 데이터 감사 → [`docs/log/2026-08-26-dev-tourism-layout-festival-move.md`](docs/log/2026-08-26-dev-tourism-layout-festival-move.md)

## 27-E. 추천 탭 큐레이션 3종 + 제부도 바닷길 시간표 → [`docs/log/2026-08-26-dev-datalab-tide.md`](docs/log/2026-08-26-dev-datalab-tide.md)

## 27-F. 음식점 3,754곳 · 관광 리포트 3화면 · 메뉴 확장 → [`docs/log/2026-08-26-dev-restaurants-report-menu.md`](docs/log/2026-08-26-dev-restaurants-report-menu.md)

## 27-G. 사진 중복 162장 제거 · 날씨 위젯 · 검사기 보강 (배포) → [`docs/log/2026-08-26-deploy-photo-dedup-weather.md`](docs/log/2026-08-26-deploy-photo-dedup-weather.md)

사진 373장 중 고유 이미지가 211장뿐이었다. 162장이 다른 관광지 사진의 복사본으로,
한 장이 6곳을 덮은 사례까지 있었다(CGV·롯데시네마 4곳에 '화성시작은영화관' 간판 사진).
파일명만 보는 기존 검사로는 잡히지 않아, `build_photo_index.py` 에 해시 교차중복 차단을 넣었다.

  사진 373 → 211장 · 사진 있는 장소 199 → 83곳 · `check.sh` 통과(FAIL 0)

**남은 사진 공백 168곳은 [`docs/PHOTO_NEEDS.md`](docs/PHOTO_NEEDS.md) 에 읍면동별로 정리했다.**

개발 Claude 인계: 캠핑장 17장·호텔 6장은 `js/convenience.js` 소속이라 인덱서가 못 읽고,
축제 사진 44장은 축제 카드가 CSS 배너를 써서 화면에 안 나온다.

> ✅ **둘 다 처리됨** (2026-08-30 확인).
> `tools/build_photo_index.py` 의 `CONV_JS` 가 `js/convenience.js` 를 읽어 인덱스에 넣습니다
> (지금 `byName` 에 캠핑 9·호텔 8). 축제 상세는 `js/tourism.js` 의 `fd-hero--photo` 로
> 실사진을 띄웁니다 — 축제 50건 중 **47건이 사진 보유**이고, 없을 때만 CSS 그라데이션
> (`IMG_CLASSES_FD`)으로 떨어집니다.

## 27-H. PLACES 주소 32건 보정 · 개발 Claude 인계 3건 판별 (배포) → [`docs/log/2026-08-26-deploy-photo-dedup-weather.md#7`](docs/log/2026-08-26-deploy-photo-dedup-weather.md)

`address:"경기도 화성시"` 로 시(市) 단위에서 끊긴 관광지 31건 + 구 누락 1건을 읍면동까지 채웠다.
`address:""` 는 애초에 0건이었다 — 앞선 배포 보고가 틀렸다.
`js/home.js` 의 `tAddr` 이 '경기도 화성시' 접두어를 정규식으로 떼면서 문자열이 통째로 비어
빈 값처럼 보였을 뿐이다.  **시 단위에서 끊긴 주소 32 → 0건**

**개발 Claude가 '데이터 쪽'이라며 넘긴 3건은 실측 결과 셋 다 코드 쪽이다:**

| 항목 | 판정 |
|------|------|
| 지역화폐 좌표 74.8% 중복 | **정상 데이터.** 20,489건 중 19,379건이 같은 좌표+같은 주소(같은 건물). 241건 최다점은 카림에비뉴 한 건물이다. 좌표는 맞고, 마커가 겹치는 렌더링 문제다 |
| 축제 `status` 50건 `upcoming` | **죽은 필드.** `'ongoing'` 을 읽는 분기가 둘 있으나 데이터에 `'ongoing'` 이 0건이라 안 탄다. 수집 시점 값을 굳히는 구조가 문제라 `date` 로 런타임 계산해야 한다 |
| 미확정 날짜 11건 '1일' 고정 | **파서 버그.** `js/calendar.js` 의 `_parseFestDate()` 가 `"2026년 10월 중"` 을 `2026-10-01` 로 바꾸고 근사임을 안 알려, `js/today.js` 가 없는 `D-36` 을 찍는다 |

> ✅ **셋 다 처리됨** (2026-08-30 확인).
> ① 지역화폐 마커 겹침 → `fd71dd4` 로 해소 (`docs/log/2026-08-26-dev-lc-marker-cluster.md`).
> ② 축제 `status` → `js/calendar.js` 의 `festStatus(place, now)` 가 `date` 로 런타임 계산한다.
>   `data.js` 의 `status:"upcoming"` 50건은 이제 **아무도 안 읽는 죽은 필드**다(데이터 쪽 정리 건).
>   여러 날 축제 `date` 에 범위(`~`)가 든 것이 10건 들어와 있다.
> ③ `_parseFestDate()` → `js/calendar.js` 가 `_parseFestDateMeta()` 로 **근사 여부를 따로 알린다.**
>   `js/today.js` 의 `D-N` 표기는 그 뒤 사라졌다 — 지금 이 파일에 `D-` 문자열이 0건이다.

별점(`rating`·`reviewCount` **193건이 10종** 값에 몰림 — 2026-08-31 재측정, 원래 「201건 15종」)은 §4·§9 에 이미 적힌 알려진 한계다.
정렬·추천에는 안 쓰이고 표시만 3곳(`tourism.js` · `home.js` · `map.js`)이다.
`js/ratings.json` 은 `tools/check.sh` 의 **`live_drift()`** 가 배포본과 sha256 대조 중이라
**한 바이트도 건드리면 안 된다.** (`--live` 를 줬을 때만 돈다)

> 2026-08-30 정정 — 원래 tools/check.sh 의 187번째 줄이라고 적혀 있었습니다. `9fae4be`
> 시점에는 맞았지만 그 뒤 검사가 늘면서 밀렸습니다. **줄번호로 가리키지 마십시오.**
> 규칙 전문과 검사는 §0 의 「줄번호로 가리키지 마십시오」를 보십시오.


## 27-I. 행정구역(4개 구)별 지도 보기 · 검색 → [`docs/log/2026-08-26-dev-district.md`](docs/log/2026-08-26-dev-district.md)

## §28 사진·대용량 파일 push 금지 — 현황과 결정 (2026-08-26)

### 현재 상태: 새로 올라가는 것은 없다

| 항목 | 값 |
|------|---|
| `assets/` git 추적 | **0건** (디스크 **773개 / 32MB**, 2026-08-30) |
| `data/raw-large/` git 추적 | **0건** (사진 zip 72MB 보관) |
| git 추적 중인 이미지 전체 | 5개 — 전부 `img/` (파비콘·로고, UI 자산) |

`.gitignore` 의 `assets/` · `data/raw-large/` 가 실제로 걸린 것을 `git check-ignore` 로 확인했다.

### 다만 과거 것이 히스토리에 남아 있다

```
0274753  chore: 관광지 사진 159장 및 로고 이미지 git 추적 추가 (~59MB)
   ↓
6004a43  chore: assets/ git 추적 제거 및 정책 확정   ← 163개 D(삭제)
```

**8/22 에 추적을 껐지만 그 전에 올라간 blob 은 히스토리에 남는다.**
git 은 파일을 지워도 과거 커밋이 참조하면 객체를 보존한다.

```
히스토리에 남은 assets blob : 104개 / 35.4 MB
origin/main 에서 도달 가능  : 104개 / 35.4 MB   ← GitHub 에서 지금도 받아진다
.git/objects                : 58 MB
```

### 결정: 그대로 둔다 (사용자 판단, 2026-08-26)

제거하려면 `git filter-repo` + force push 뿐인데 대가가 크다 —
모든 커밋 SHA 가 바뀌어 개발 Claude 는 재 clone 해야 하고,
`WORKFLOW.md`·`docs/log/` 에 적어 둔 해시 참조가 전부 무효가 된다.
35.4MB 는 GitHub 기준 문제되는 크기가 아니고 추가 유출은 이미 멈췄다.

**나중에 마음이 바뀌면 그때 하면 된다. 다만 그전까지 이 사실을 잊지 마라.**

### 재발 방지

`tools/check.sh` 의 `asset_guard()` 는 원래 `.gitignore` 규칙 존재 여부만 봤다.
규칙이 있으면 WARN 가지로 빠져 **추적 중인 파일이 있어도 못 봤고**, `git add -f` 한 번이면 뚫렸다.

`assets/` · `data/raw-large/` 아래 추적 파일이 하나라도 있으면 **FAIL + exit 1** 로 막도록 고쳤다.
회귀 테스트로 확인했다 — 사진 1장을 `git add -f` 하니 즉시 잡고 exit 1 을 냈다.

> ⚠️ 배포 Claude 주의: "추적 0건"만 보고 "규칙 지켜짐"이라고 보고하지 마라.
> 히스토리는 별도다. 이 세션에서 실제로 그렇게 잘못 보고했다.

---

*최종 업데이트: 2026년 8월 30일 — ① 문서 전수 감사. 코드와 어긋난 현황 11건 정정,
끝난 인계 7건 종결 표시, 「끝난 판단은 WORKFLOW.md 로」 규칙 신설
([`docs/log/2026-08-30-dev-workflow-drift-audit.md`](docs/log/2026-08-30-dev-workflow-drift-audit.md)).
② 그 감사가 맞춰 놓은 줄번호가 같은 날 오후 5건 밀려, 「줄번호로 가리키지 마십시오」 규칙과
`check.sh` 의 line-ref 검사를 신설. 이어서 `tools/` 21곳·`js/map.js` 6곳을 식별자로 바꾸고,
검사가 **안 보는 범위**(`js/`·`css/` 주석 74건 · `docs/log/`)를 §0 과 §13 에 명시
([`docs/log/2026-08-30-dev-line-ref-guard.md`](docs/log/2026-08-30-dev-line-ref-guard.md)).
③ 같은 날 재부팅을 세 번 겪고 §12 「컨테이너가 재부팅됐다면」 신설 — 영속 경로,
§13 에 `tools/server.py` 중단 diff 항목 추가
([`docs/log/2026-08-30-deploy-reboot-recovery-push-auth.md`](docs/log/2026-08-30-deploy-reboot-recovery-push-auth.md)).
④ 08-31 — 그 §12 의 「`~/.claude/projects/` 는 영속이 아니다」가 **틀린 것으로 실측돼**
정정. 결론(저장소에 커밋하라)은 유지하고 이유를 바꿨다. §0 에 「증상에서 원인을 추측했으면
재기 전에는 원인으로 적지 마십시오」 신설
([`docs/log/2026-08-31-dev-memory-persistence-correction.md`](docs/log/2026-08-31-dev-memory-persistence-correction.md)).
⑤ 08-31 — §13 의 `tools/server.py` 미완 diff 인계를 개발 Claude 가 **없는 diff 로 확인해
종결**하고 이 파일 전체를 다시 읽어 숫자를 재측정했다 — 드리프트 8건 정정(그중 3건은 ④ 커밋이
심은 것), §15 「README 수정 금지」 위반 1건 자진 기록
([`docs/log/2026-08-31-dev-phantom-diff.md`](docs/log/2026-08-31-dev-phantom-diff.md)).
그 종결은 **배포 컨테이너에서 실물이 확인돼 보류로 되돌렸다** — 커밋된 적 없는 diff라
개발 쪽에서는 보이지 않는다([`docs/log/2026-08-31-deploy-phantom-diff-answer.md`](docs/log/2026-08-31-deploy-phantom-diff-answer.md)).
⑥ 08-31 — 서버 최적화 5건 구현(gzip+캐시 · 상류 캐시+stale · Session · waitress · 프리워밍)
과 서비스 재기동. 첫 화면 876KB → 279KB(-68.1%), 새 배포 주소로 세 곳 갱신
([`docs/log/2026-08-31-deploy-server-optimize.md`](docs/log/2026-08-31-deploy-server-optimize.md)).
⑦ 08-31 — 플로우·로그 전수 재독. **로그에만 적혀 실종돼 있던 규칙 3건**을 올렸다 —
§12 「`cloudflared` 를 끄지 마십시오」 · §12 규칙표 「`?v=` 는 손으로 올리지 않는다」 ·
§15 「화면 문구는 해요체」
([`docs/log/2026-08-31-deploy-workflow-log-full-read.md`](docs/log/2026-08-31-deploy-workflow-log-full-read.md))
⑦ 08-31 — 미뤄 뒀던 **코드 주석의 줄번호 참조 74건을 전부 식별자로** 바꾸고
(74건 중 맞은 것은 1건뿐이었다), `check.sh` line-ref 검사를 `js/`·`css/`·`index.html`
까지 넓혔다. 맨 콜론(`(:114)`) 참조도 잡는다
([`docs/log/2026-08-31-dev-code-comment-line-refs.md`](docs/log/2026-08-31-dev-code-comment-line-refs.md)).
⑧ 08-31 — 배포 Claude 가 `tools/server.py` diff 의 **실물 출력으로 ⑤의 종결을 뒤집었다.**
내 실측 여섯 가지는 개발 컨테이너에서만 참이었고 범위를 안 적은 것이 오류다. §0 에
「그리고 **어디서 쟀는지**를 적으십시오」를 넣고, 대기 중이던 완성/되돌림 판단에
**되돌림**으로 답했으나, 그 답이 닿기 전에 사용자 지시로 배포 Claude 가 구현을 마쳤다
(`4803176`, -68.1%). 판단 경과는 §13 에 표로 남겼다
([`docs/log/2026-08-31-dev-scope-of-measurement.md`](docs/log/2026-08-31-dev-scope-of-measurement.md)).
⑨ 08-31 — 외부 API 6개 전수 점검(전부 정상)과 **brotli 압축** 추가. 첫 화면 −7.8% ·
지연 로드 JSON −33.7%. 곁들여 `?v=` 4곳이 08-25 이후 안 오르던 잠복 버그와,
`AIRKOREA_KEY` 가 죽은 변수인데 §27 이 「표시 중」이라 적던 것을 고쳤다
([`docs/log/2026-08-31-dev-brotli-and-api-audit.md`](docs/log/2026-08-31-dev-brotli-and-api-audit.md))*
