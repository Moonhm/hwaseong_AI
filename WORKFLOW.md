# WORKFLOW.md — 화성잇다 개발 기록

> 이 문서는 **2026 화성시 해커톤** 프로젝트 **화성잇다**의 전체 개발 과정을 기록합니다.
> 양방향 Claude 협업 워크플로우, 세션별 작업 내역, 버그 수정 이력, 아키텍처 결정 사항을 포함합니다.

---

## 1. 프로젝트 개요

- **앱 이름**: 화성잇다 (Hwaseong-itda)
- **목표**: 경기도 화성특례시 통합 관광 정보 모바일 웹앱
- **저장소**: https://github.com/Moonhm/hwaseong_AI
- **배포 URL**: https://culture-reed-dee-rug.trycloudflare.com
- **팀**: 문형민, 서교연 + Claude Sonnet 4.6 (AI 개발 에이전트 2인 체계)
- **기간**: 2026년 8월 18~19일 (해커톤 당일 집중 개발)

---

## 2. 양방향 Claude 워크플로우 (핵심 협업 구조)

이 프로젝트의 가장 독특한 점은 **두 개의 Claude AI 인스턴스가 GitHub을 매개로 비동기 협업**한 것입니다.

### 역할 분리

```
┌────────────────────────────────────────────────────────────────┐
│  개발 Claude (Development Claude)                              │
│  ─────────────────────────────────────                         │
│  · git fetch로 최신 코드 확인 후 작업                           │
│  · index.html / map.js / parking.js 등 코드 작성·수정          │
│  · git push → 커밋 메시지로 변경 사항 기록                      │
│  · README [메시지함]으로 배포 Claude에게 메시지 전달            │
└────────────────────────────────────────────────────────────────┘
                              ⬇ push / ⬆ pull
                         GitHub (origin/main)
                              ⬇ pull / ⬆ push
┌────────────────────────────────────────────────────────────────┐
│  배포 Claude (Deployment Claude)                               │
│  ─────────────────────────────────────                         │
│  · Flask 서버 + Cloudflare Tunnel로 실시간 서빙                 │
│  · assets/images/places/ 장소 사진 159개 로컬 관리 (git 제외)  │
│  · 실제 앱 테스트 → 버그 발견 시 직접 수정 + push              │
│  · README [메시지함]으로 개발 Claude에게 답장                   │
└────────────────────────────────────────────────────────────────┘
```

### 소통 채널

- **GitHub README.md `[Claude 간 메시지함]` 섹션**을 비동기 메시지 채널로 사용
- 사용자(문형민)가 두 Claude를 중계 (메시지를 양쪽에 전달)
- 각 Claude는 README를 읽어 상대방의 최신 작업·요청 확인

### 핵심 규칙 (충돌 방지)

| 규칙 | 이유 |
|------|------|
| 작업 전 반드시 `git pull --rebase` | 두 인스턴스가 동시에 push → rebase 충돌 방지 |
| 사진·대용량 파일 절대 push 금지 | 저장소 용량 초과 방지, 배포 서버 전용 |
| 커밋 메시지에 변경 이유 명시 | 상대 Claude가 pull 시 맥락 즉시 파악 |
| ID 범위 규칙 준수 | tourist/festival ID 충돌 방지 |

---

## 3. 기술 스택 및 아키텍처

### 전체 구조

```
Frontend         순수 HTML · CSS · JavaScript (프레임워크 없음)
                 단일 파일 SPA: index.html (~3,100줄)
Map Engine       Kakao Maps JavaScript SDK v2
                 커스텀 오버레이 · 클러스터 · idle 이벤트 기반 동적 렌더링
Proxy Server     Flask (Python) — 주차장 API CORS 우회
Deployment       Cloudflare Quick Tunnel (cloudflared --url localhost:8080)
AI Workflow      Claude Sonnet 4.6 × 2 인스턴스 (개발 + 배포)
```

### 모바일 우선 설계 원칙

| 항목 | 값 |
|------|----|
| 기준 화면 | 모바일 세로 (portrait) |
| 최대 너비 | 480px |
| 하단 내비게이션 | 52px 고정 |
| 터치 타겟 최소 | 36×36px |

### 카테고리 색상 체계

| 카테고리 | 색상 | 이모지 |
|---------|------|--------|
| tourist (관광지) | `#FB923C` | ★ |
| restaurant (맛집) | `#D97706` | 🍽 |
| festival (축제) | `#DC2626` | 🎉 |
| parking (주차장) | `#2563EB` | 🅿 |
| localcurrency (지역화폐) | `#059669` | 💳 |

### 지도 렌더링 방식

```
줌레벨 > 7 (줌아웃):  그리드 기반 클러스터 원 (별/P/💳 + 개수)
줌레벨 ≤ 7 (줌인):   뷰포트 내 개별 핀 (cm-pin, cm-circle, cm-tail)

idle 이벤트 → 100ms 디바운스 → clearDisplay() → re-render
(panBy/setCenter 연속 호출 시 마지막 위치에서 1회만 렌더링)
```

---

## 4. 데이터 파이프라인

### 관광지 (tourist) — 159개

| 수집 시기 | 방법 | ID 범위 | 수량 |
|---------|------|---------|------|
| 초기 | tour.hscity.go.kr 공식 API 스크래핑 | id:1–41 | 41개 |
| 1차 추가 | tour.hscity.go.kr 체험마을·체험지 | id:66–83, 201–233 | 51개 |
| 2차 추가 | 한국관광 데이터랩 8개 zip → 29개 CSV 분석 | id:134–200 | 67개 |

**2차 추가 포함 카테고리**: 쇼핑(백화점), 골프장, 숙박(호텔·리조트), 영화관, 테마파크, 공원, 시장, 마리나, 낚시터, 사찰, 체험장, 문화시설

### 축제 (festival) — 48개

1. yeyak.hscity.go.kr에서 2026년 화성시 행사 전체 수집
2. `gen_data.py` 스크립트로 좌표 자동 매핑 (VENUE_COORDS 딕셔너리)
3. 과거 행사(8월 14일 이전) 전체 제거 → 미래 행사 48개만 유지
4. ID 범위: 42–133 (festival 전용)

### 주차장 — 131개

- `smartparking.hscity.go.kr` API에서 전체 목록 수집
- 정적 캐시: `js/parking-static.json` (Flask 없어도 즉시 표시)
- 실시간 여유: Flask 프록시 `/api/parking/realtime` → 60초마다 갱신

### 별점 시스템 — 159개

Playwright/Selenium 환경 미지원 → 알고리즘 기반 생성:
1. Kakao Local Search API REST 호출 (검색 순위 파악)
2. 설명 텍스트 분석 (한국관광100선, 세계문화유산, 명소 키워드 등)
3. 태그 수·설명 길이 보정
4. 결정론적 노이즈 (MD5 해시 기반, 항상 동일한 값)
5. 결과: `js/ratings.json` → `data.js` 주입

### 장소 사진

- `assets/images/places/{name}.jpg` (data.js name 필드와 정확히 일치)
- **git 제외** (용량 문제) — 배포 서버에만 존재
- 원본 32장 + 유사 카테고리 재사용 127장 = 총 159장
- onerror fallback: 주황 그라데이션 + 🏞️ 자동 표시

---

## 5. 세션별 작업 이력

### Session 4 — 버그 검수 및 편의정보 개선 (2026-08-19 ~ 08-22)

| 커밋 | 내용 |
|------|------|
| `489e68f` | docs: README 영어 작성 + 한국어 용어 괄호 병기, 연락처 원상복구 |
| `9fb4e9d` | fix: conv_map.js p.name null 접근 / map.js 주차장 강제 활성화 수정 (사용자 직접 push) |
| `d836c7a` | fix: 버그 7건 — data.data null guard, ratio 음수, setParkingVisible 독립 토글, templeStay/jebu null guard, renderHotels 조기 return, 비ISO 날짜 캘린더 파싱 |

**주요 변경 내역:**
- `_parseFestDate()` 헬퍼 추가 — "2026년 N월 중" 형식을 해당 월 1일로 변환해 캘린더 점 표시
- `renderHotels()` 조기 return 버그 수정 — jebu 없어도 호텔 목록은 렌더
- `conv_map.js` templeStay/jebu null guard 추가
- `parking.js` data.data Array 검증, ratio `Math.max(0)` 보정
- `map.js` setFilter에서 주차장 독립 토글 완전 복원 (`setParkingVisible(parkActive)`)



### Session 1 — 기초 구축

| 커밋 | 내용 |
|------|------|
| `6590e46` | 화성잇다 초기 버전 — 카카오맵 통합 모바일 웹앱 |
| `f37e99c` | 지도 초기화 오류 수정 |
| `8322932` | autoload=false + kakao.maps.load() 방식 |
| `46961a5` | API 키 교체 (도메인 등록 키로 최종) |
| `b488910` | 더미 데이터 전체 제거, 동적 렌더링 전환 |
| `1a1eddc` | 실시간 주차장 API 연동 (parking.js + server.py) |
| `302cd4e` | parking-static.json 생성, 지오코더 단순화 |

### Session 2 — 데이터 대폭 확장

| 커밋 | 내용 |
|------|------|
| `3931360` | data.js — 체험지 51개 + 축제 24개 추가 (총 116개) |
| `c223d0b` | 장소 사진 시스템 구축 (assets/, onerror fallback) |
| `c98d94a` | 한국관광 데이터랩 관광지 67개 추가 (id:134–200) |
| `93fb388` | 관광지 줌레벨 클러스터/뷰포트 동적 렌더링 (idle 이벤트) |
| `901b4ab` | 관광지 슬라이드카드 전면 개선 |
| `a7037a9` | 관광지 좌표 9건 교정 + updateTouristDisplay 100ms 디바운스 |

### Session 3 — 기능 완성 (해커톤 당일)

| 커밋 | 내용 |
|------|------|
| `7e506e2` | 관광지 159개·주차장 131개 tags 추가 + README 전면 갱신 |
| `30e13fe` | 전체 탭 종합 버그 수정 (BUG-1~13) |
| `86c7f5a` | AI기반 관광지 추천 5문항 + 사이드 메뉴 드로어 |
| `7801150` | 지도 포커스 연동 + 주차장 대수 표시 |
| `fc9c95b` | 배포 Claude 검토 항목 4건 수정 |
| `2d96feb` | 성능·버그 7건 수정 (배포 Claude 직접 push) |
| `9ecd97f` | localcurrency 4.2MB 지연로드 + 달력 동적화 |
| `f76a447` | 지도 기본 빈 화면 + 주차장 칩 독립 멀티 선택 |
| `b5edec3` | 관광 탭 전체 목록 5개 미리보기 + 더보기 버튼 |
| `aabba6f` | 홈 로고 클릭 앱 주소 공유 기능 |
| `2d69c8b` | AI기반 관광지 추천 결과 화면 전면 재설계 |
| `afc7315` | 관광지 별점(★) 시스템 추가 |
| `5591bea` | 전체 코드 최적화 7가지 |
| `74e2f6d` | 경기지역화폐 공식 로고 적용 |
| `c50153c` | fix: 사이드 메뉴 shadow 버그 수정 |
| `ea3f134` | fix: 지도 핀 전수 검토 — 좌표 중복 15곳 분리 + 필터 토글 버그 수정 |

---

## 6. 주요 버그 수정 이력

| 버그 | 증상 | 원인 | 수정 |
|------|------|------|------|
| BUG-1 | tourist id:84–116이 festival과 충돌 | ID 범위 중복 설계 | tourist → id:201–233 재번호 |
| BUG-3/4 | localcurrency.js와 map.js 변수명 충돌 | `LC_PIN_LEVEL` 동일 변수명 | map.js는 `TK_PIN_LEVEL`/`TK_GRID`로 네임스페이스 분리 |
| BUG-8 | 달력 ‹/› 버튼 클릭 무반응 | onclick 연결 누락 | calNav() 함수에 연결 |
| BUG-9 | 축제 카드 배경 단색 | 인덱스 기반 클래스 미적용 | `IMG_CLASSES[festIdx % 5]` |
| BUG-11 | 슬라이드 drag 이벤트 2중 등록 | setupSlideCardDrag 중복 호출 | 1회만 호출하도록 수정 |
| 퀴즈 오버레이 계속 표시 | 탭 전환 후에도 퀴즈 떠있음 | `go()` 함수에 `closeQuiz()` 없음 | `go()` 첫 줄에 `closeQuiz()` + `closeMenu()` 추가 |
| ← 버튼 화면 삐져나옴 | 퀴즈 오버레이 버튼이 우측 뷰포트 밖 | `translateX(100%)` = 뷰포트 끝 | `translateX(110%)` + `visibility:hidden` |
| 사이드 메뉴 shadow 노출 | 메뉴 닫힌 상태에서 그림자 보임 | box-shadow 28px가 화면 끝 밖으로 새어나옴 | `translateX(110%)` + `visibility:hidden` + transition delay |
| 지도 핀 토글 버그 | 관광 탭→지도 이동 시 핀이 사라짐 | `goMapFocus()`에서 `setFilter('tourist')` 재호출 → 토글 OFF | `chip.classList.contains('active')` 확인 후 조건부 호출 |
| 좌표 중복 (15곳) | 같은 위치에 핀 겹쳐 한쪽 클릭 불가 | data.js 입력 오류 (동일 항구/단지 내 복수 시설) | 약 100m 오프셋으로 분리 |

---

## 7. 양방향 Claude 소통 기록 (요약)

### 개발 Claude → 배포 Claude

**1차 (Session 2):**
- 관광지 대폭 확장 공지 (92→159개), 클러스터 렌더링 방식 변경 안내
- 장소 사진 규칙 전달: `assets/images/places/{name}.jpg`

**2차 (Session 3 초반):**
- 배포 Claude 수정사항 7건 수신 확인 (달력, 크래시, 퀴즈, 지도포커스 등)
- 현재 세션 완료 작업 4건 공유
- 향후 개발 방향 의향 질문

**3차 (Session 3 중반):**
- 양방향 push 체계 전환 공지 (`git pull --rebase` 필수화)
- 퀴즈 결과 "지도에서 보기" 동작 확인 요청
- 최신 작업 4건 (칩 UI, 더보기, 공유, 퀴즈 재설계) 전달

### 배포 Claude → 개발 Claude

**1차:**
- 배포 성공 확인 (URL, 이미지 보유 현황)

**2차:**
- 사진 159개 할당 완료 (원본 32 + 재사용 127)
- push 권한 획득 (`gh auth login` 완료)
- BUG-1~13 직접 수정 후 push
- tags 시스템·주차장 tags 자동 생성 완료
- 권고사항 5건 (getSpotTags, 달력 동적화, Geocoder 캐싱 등)

**3차:**
- 홈 생활탭 데이터 버그 수정 + 성능 최적화 완료 직접 push
- 지역화폐 지연로드 적용
- 달력 동적화 완료

---

## 8. PLACES 좌표 일괄 재지오코딩 프로토콜 (Session 3 후반)

### 문제 발견

지도 탭에서 관광지·축제 핀이 실제 위치와 다른 곳에 찍히는 문제 다수 발견.  
원인: `data.js`에 수동 입력된 lat/lng가 주소와 불일치하거나, gen_data.py의 VENUE_COORDS 딕셔너리가 부정확했음.

### 방법 — tools/regeocode.py

새 스크립트 `tools/regeocode.py`를 작성해 Kakao 로컬 REST API로 주소를 재지오코딩 후 `js/data.js`를 직접 교체.

**작동 방식:**

1. **파싱**: 정규식(`_PLACE_RE`)으로 data.js에서 `tourist` + `festival` 항목 블록 위치(start/end) 추출
2. **지오코딩**: 항목별 `address` 필드 → Kakao REST API (`/v2/local/search/address.json`) 호출
   - 실패 시 "경기도 화성시 " 접두어 추가 후 1회 재시도
   - API rate limit 방지: 호출 사이 0.11초 대기
3. **차이 판단**: 기존 lat/lng와 새 좌표의 합산 차이가 0.0001° 초과인 경우만 패치 대상으로 분류
4. **안전 교체**: 패치를 **뒤에서부터** 적용해 앞쪽 문자열 오프셋 밀림 방지
5. **옵션**: `--dry-run`(파일 수정 없이 출력만), `--category`(카테고리 선택), `--key`(REST API 키 직접 지정)

```
python tools/regeocode.py --key 1bd845da5756d1c78955463b800731ef
```

### 핵심 기술 사항

| 항목 | 내용 |
|------|------|
| API | Kakao REST API `/v2/local/search/address.json` |
| 인증 | REST API 키 (JS 앱 키와 별도 발급 필요) |
| 파싱 방식 | 정규식 직접 파싱 (JSON.parse 불가 — JS 문법) |
| 좌표 정밀도 | `round(..., 7)` — 소수점 7자리 (약 1.1cm 정밀도) |
| 실패 처리 | 원본 좌표 유지 (덮어쓰지 않음) |

### 실행 결과

| 구분 | 수량 |
|------|------|
| 전체 대상 (tourist + festival) | 207개 |
| 수정 완료 (좌표 차이 > 0.0001°) | 149개 |
| 동일 유지 (이미 정확) | 0개 |
| 실패 (지오코딩 불가) | 58개 |

**실패 58개 주요 원인:**

| 원인 | 예시 |
|------|------|
| 주소 "미정" | 축제 다수 — 장소 미정 행사 |
| 너무 광범위한 주소 | "경기도 화성시" 단독 |
| 시설명만 기재 | "융릉·건릉", "궁평항", "동탄호수공원" |
| 폐쇄·이전 시설 | API 검색 결과 없음 |

실패 항목은 원본 좌표 그대로 유지되며, 주소가 확정된 경우 수동 보완 가능.

### 커밋

| 커밋 | 내용 |
|------|------|
| `ff4f796` | fix: PLACES 관광지·축제 좌표 149개 Kakao 지오코딩으로 재보정 |

---

## 9. 남은 한계 및 향후 개선 방향

| 항목 | 현황 | 개선 방향 |
|------|------|---------|
| 별점 데이터 | 알고리즘 생성 (Selenium 환경 없음) | Kakao Place API 실제 별점 연동 |
| 장소 사진 | git 추적 제외 완료 — 로컬·배포 서버에만 보관 (섹션 11 참고) | CDN 적용 또는 Cloudflare Images |
| Geocoder 캐싱 | 편의시설 칩 클릭마다 157건 API 호출 | localStorage 1회 캐싱 |
| 달력 날짜 연동 | has-event 클래스 하드코딩 일부 | PLACES 배열의 date 필드 기반 동적 마킹 |
| 맛집 지도 칩 | 실제 restaurant 데이터 없음 | 모범음식점(conv_map.js)으로 리다이렉트 |

---

## 10. 파일별 역할 및 핵심 함수

### index.html

| 함수 | 역할 |
|------|------|
| `go(page)` | 탭 전환 (+ 퀴즈·메뉴 자동 닫기) |
| `goMapFocus(lat, lng, level, id)` | 관광 탭 → 지도 이동 + 핀 포커스 |
| `renderTourismList(theme, expanded)` | 관광 탭 목록 렌더링 (더보기 포함) |
| `showFestivalDetail(id)` | 축제 상세 뷰 |
| `openQuiz()` / `closeQuiz()` | 퀴즈 오버레이 제어 |
| `copyAppUrl()` | 앱 URL 클립보드 복사 |
| `ratingStars(r)` | 별점 → ★½☆ 문자열 변환 |

### map.js

| 함수 | 역할 |
|------|------|
| `initMap()` | Kakao 지도 초기화 + idle 리스너 |
| `buildOverlays()` | tourist 제외 카테고리 핀 사전 생성 |
| `updateTouristDisplay()` | 100ms 디바운스 관광지 렌더링 분기 |
| `showTkViewport(bounds)` | 뷰포트 내 개별 핀 생성 |
| `showTkClusters(bounds, lv)` | 줌아웃 시 그리드 클러스터 원 |
| `onPinClick(id)` | 핀 클릭 → 선택 상태 + 슬라이드 오픈 |
| `showPlaceSlide(place)` | 하단 슬라이드 카드 렌더링 |
| `setFilter(cat)` | 카테고리 칩 필터 토글 |
| `activateParking()` | 주차장 필터 단방향 활성화 |

### parking.js

| 함수 | 역할 |
|------|------|
| `mergeParkingData()` | parking-static.json 로드 |
| `fetchParkingAll()` | 실시간 API 최초 호출 |
| `_applyRealtime(data)` | 실시간 데이터 → parkingData 병합 |
| `pinColorCached(p)` | 여유율 기반 색상 (캐시) |
| `updateParkingDisplay()` | 100ms 디바운스 주차장 렌더링 |

---

---

## 11. assets/ 이미지 관리 정책

`assets/images/` (관광지 사진 159장 · 약 59MB)는 **git 추적 제외** — `.gitignore`에 등록됨.  
사진 파일은 **로컬 및 배포 서버에만 보관**하며, git push 대상이 아닙니다.

```
assets/images/places/{name}.jpg   — data.js의 name 필드와 정확히 일치해야 함
assets/images/로고_이름.png       — 앱 로고
```

- 배포 서버(배포 Claude 환경)에 직접 보관
- git clone 후에는 사진 없이 동작 (onerror fallback: 주황 그라데이션 + 🏞️ 표시)
- **앞으로 assets/ 및 용량 큰 파일은 절대 git push 금지**

---

## 12. 새 Claude 세션 진입 체크리스트

> 새 컨텍스트로 접속한 Claude는 이 체크리스트를 순서대로 따라 즉시 작업을 이어갈 수 있습니다.

### 필수 확인 (작업 시작 전)

```bash
# 1. 최신 코드 확인
git log --oneline -5

# 2. 변경 사항 없는지 확인
git status

# 3. 원격과 동기화
git pull
```

### 이 프로젝트에서 반드시 지켜야 할 규칙

| 규칙 | 내용 |
|------|------|
| 언어 | **항상 한국어 경어체** |
| assets push 금지 | `assets/images/` 는 push 금지 — 사용자 명시 허가 시에만 예외 |
| README 수정 금지 | README는 서비스 소개 전용. 개발 메모는 반드시 이 파일(WORKFLOW.md)에 |
| 수정 전 허락 | 검수·분석 결과는 먼저 보고 → 사용자 허락 후 수정 진행 |
| git pull/push | 작업 전 pull, 작업 후 push. 사용자가 "깃 풀 푸쉬"라고 하면 pull → push 순서 |

### 핵심 현황 (최신 기준)

| 항목 | 값 |
|------|----|
| CONV_CACHE_VER | `v5` (localStorage 캐시 버전) |
| 관광지 | 159개 (id: 1–41, 66–83, 134–200, 201–233) |
| 축제 | 48개 (id: 42–133) |
| 주차장 | 131개 |
| 제부도 숙박 | 115개 (lat/lng 하드코딩, convenience.js) |
| 편의정보 | 모범음식점 94, 관광식당 35, 호텔 14, 캠핑 11, 템플스테이 1 |

### Kakao API

| 종류 | 키 |
|------|-----|
| JS 앱 키 (지도 표시용) | HTML `<script>` appkey 파라미터 확인 |
| REST API 키 (지오코딩용) | `1bd845da5756d1c78955463b800731ef` |

### 개발 환경 실행

```bash
python tools/server.py --port 8080
# → http://localhost:8080 에서 앱 확인
```

---

## 13. 현재 알려진 미해결 항목 (다음 세션 참고)

> ⚠ 아래 표의 좌표·데이터 항목은 **§17 「배포 Claude 인계 대기」 표가 우선합니다.**
> 여기에는 "어느 쪽(주소/좌표)이 틀렸는지" 가 특정되어 있지 않아, 그대로 고치면 멀쩡한 값을 망가뜨리는 건이 있습니다.
> (예: `id:41` 은 좌표가 정상이고 주소가 오류, 작은섬·양지 쌍은 애초에 정상이라 수정 대상이 아님)

| 항목 | 설명 |
|------|------|
| 비ISO 날짜 축제 캘린더 | "2026년 N월 중" 형식은 해당 월 1일로 처리됨 (Session 4에서 수정 완료) |
| 미정 장소 placeholder 좌표 | `lat:37.199, lng:126.831` — 미정 축제 23개. 데이터 자체 문제, 코드 수정 불가 |
| 작은섬1·2, 양지팬션·리조텔 | convenience.js에서 동일 좌표 중복 — 마커 겹침, 데이터 보정 필요 |
| id:41 화성예술의전당 | 주소(향남읍)와 좌표(동탄) 불일치 — 수동 수정 필요 |
| id:102 송산포도축제 | date 필드에 종료일(9/7)만 기재, 시작일(9/6) 누락 |
| convenience.js 대부분 lat/lng 없음 | restaurants/hotels/camping 대부분 좌표 미입력 — 지도 핀 미표시 (제부도는 완료) |

---

## 14. 이 문서의 목적 및 인수인계 가이드

> **이 섹션은 새 Claude 컨텍스트가 가장 먼저 읽어야 할 내용입니다.**  
> WORKFLOW.md 한 파일만 읽으면 프로젝트 전체 맥락, 협업 규칙, 현재 상태를 파악하고 바로 작업에 들어갈 수 있도록 설계됐습니다.

### 왜 이 문서가 필요한가

Claude는 컨텍스트 윈도우 한계로 인해 대화가 길어지면 새 세션으로 전환해야 합니다.  
그때마다 사용자가 처음부터 설명하는 비효율을 없애기 위해, **GitHub의 이 파일이 인수인계 채널** 역할을 합니다.  
새 세션 Claude는 `git pull` 후 이 파일을 읽는 것만으로 즉시 업무 연속성을 유지합니다.

### 새 세션 진입 시 순서

```
1. git pull                     ← 최신 코드 동기화
2. WORKFLOW.md 전체 읽기        ← 맥락·규칙·현황 파악
3. 섹션 13 확인                 ← 미해결 항목 파악
4. 사용자에게 현황 보고 후 지시 대기
```

### 이 문서 최신화 방법

**세션이 끝날 때마다** 아래 항목을 업데이트해야 합니다:

| 항목 | 위치 | 업데이트 시점 |
|------|------|------------|
| 세션 작업 이력 | 섹션 5 | 세션 종료 시 커밋 목록 추가 |
| 미해결 항목 | 섹션 13 | 해결된 건 제거, 새로 발견된 건 추가 |
| 핵심 현황 수치 | 섹션 12 | 데이터 수 변경 시 |
| 최종 업데이트 날짜 | 문서 맨 끝 | 항상 |

> 기존 내용은 수정하지 말고 **아래에 추가**하는 방식으로 작성합니다. 잘못된 내용만 해당 줄에서 수정합니다.

---

## 15. Claude 협업 규칙 상세 (필독 — 예외 없음)

### 언어 규칙

- **항상 한국어 경어체**로 대화한다.
- 사용자가 반말로 말해도 Claude는 경어체를 유지한다.

### 보안·용량 규칙

- `assets/` 및 **용량 큰 파일(사진, 동영상 등)은 git push 절대 금지.**
- 사용자가 허가해도 거절을 권고할 것. (과거에 허가했다가 번복한 이력 있음)
- `git add` 전 반드시 `git status`로 assets/ 포함 여부 확인.

### 문서 규칙

| 파일 | 용도 |
|------|------|
| `README.md` | **서비스 소개 전용** — 기능 설명, 팀, 데이터 출처 등 외부 공개용 |
| `WORKFLOW.md` | **개발 내부 전용** — 작업 이력, 버그 수정, 규칙, 인수인계 등 모든 개발 메모 |

- README에 개발 메모·명령어·내부 기록을 절대 추가하지 않는다.
- 개발 관련 내용은 전부 WORKFLOW.md에 작성한다.

### 작업 방식 규칙

- **검수·분석 요청 시**: 결과를 먼저 보고하고, 수정 여부는 사용자 허락 후 진행.
- **git 작업**: 작업 전 `git pull`, 작업 후 `git push`. 사용자가 "깃 풀 푸쉬"라고 하면 pull → push 순서.
- **커밋 메시지**: 변경 이유를 명시한다. 상대 Claude가 pull 시 맥락을 즉시 파악할 수 있게.

### 사용자(문형민) 특성

- 짧고 명확한 보고 선호 — 표·목록 형태로 정리.
- 직접 코드를 수정해서 push하는 경우도 있음 → 작업 전 항상 `git pull`로 확인.
- 수정 전 허락을 받는 방식 선호 — 먼저 판단 결과 보고, 그 다음 진행 여부 결정.
- 연락처 `seoky0219@gmail.com` — README에 표기된 연락처, 임의 변경 금지.

---

*최종 업데이트: 2026년 8월 24일*

---

## 16. Session 5 — 전체 코드 전수 검토 & 인수인계 체계 완성 (2026-08-22 ~ 08-24)

### 배경

Session 4 말미에 전 카테고리 핀 선택 효과 통일 시도 → 사용자 "못봐주겠다" → 즉시 revert.  
이후 사용자가 "벌로 코드 전체 검토하면서 반성해"를 지시 → 3개 병렬 에이전트로 전수 검토 실행.

---

### 수행 작업 요약

**1. 전체 코드 41건 검토 (index.html · map.js · parking.js · localcurrency.js · conv_map.js · convenience.js · data.js)**

| 분류 | 발견 건수 |
|------|----------|
| 크래시 (런타임 중단 가능) | 8건 |
| 버그 (동작 오류) | 15건 |
| 최적화 | 5건 |
| 마이너 | 13건 |
| **합계** | **41건** |

사용자가 41건을 직접 검증 → **실제 수정 대상 7건** 선별 (나머지는 오탐·데이터 문제·낮은 우선순위로 패스).

---

**2. 버그 수정 커밋 상세**

| 커밋 | 파일 | 수정 내용 |
|------|------|----------|
| `9fb4e9d` | `conv_map.js:241` | `p.name.length` → `(p.name \|\| '').length` — name undefined 시 TypeError 방지 |
| `9fb4e9d` | `map.js:861` | `setParkingVisible(parkActive && ...)` 1차 수정 (완전하지 않았음) |
| `d836c7a` | `parking.js:35` | `data.data` null/Array 검증 추가 — API `{ok:true, data:null}` 방어 |
| `d836c7a` | `parking.js:295` | `Math.max(0, ratio)` — avail 음수 시 게이지 바 음수% 방지 |
| `d836c7a` | `map.js:861` | `setParkingVisible(parkActive)` 최종 수정 — 주차장 완전 독립 토글 복원 |
| `d836c7a` | `conv_map.js:37` | `[CONVENIENCE.templeStay].filter(Boolean)` — null 방어 |
| `d836c7a` | `conv_map.js:44` | `var j = CONVENIENCE.jebu \|\| {}` — jebu undefined 방어 |
| `d836c7a` | `index.html:2897` | `renderHotels()` 조기 return 구조 수정 — jebu 없어도 호텔 목록 렌더 |
| `d836c7a` | `index.html` | `_parseFestDate()` 헬퍼 추가 — "2026년 N월 중" → `2026-N-01` 변환해 캘린더 점 표시 |

---

**3. 사용자가 오탐으로 최종 판정한 항목 (수정 불필요)**

| 항목 | 판정 이유 |
|------|---------|
| slide-inner / place-slide / map-dim DOM null 접근 | HTML SPA에 항상 존재하는 요소 |
| parking-static.json fetch 실패 | 파일 실제 존재 (75KB), catch 있음 |
| PLACES / CONVENIENCE 미선언 크래시 | 로드 순서 보장, 방어 코드 있음 |
| 날짜 범위 표시 오류 | data.js에 범위형 날짜 없음 |
| 프리마베라펜션 중복 등록 | 데이터 정리 이슈, 코드로 해결 불가 |

---

**4. WORKFLOW.md 인수인계 채널 공식화**

이 세션에서 컨텍스트 한계 문제를 논의하고, **WORKFLOW.md를 Claude 세션 간 인수인계 채널로 공식 확정**.

- 새 세션 진입 시 사용자가 할 말: `"WORKFLOW.md 읽고 이전 세션에서 하던 화성잇다 작업 이어서 해줘. memory도 확인해."`
- Claude memory(`~/.claude/projects/.../memory/`)에 핵심 규칙(자동 push, 커밋 규칙, 경어체) 저장 완료
- WORKFLOW.md만 읽어도 전체 맥락·규칙·현황 파악 가능하도록 섹션 12~15 정비 완료

---

### 현재 코드 상태 (2026-08-24 기준)

| 항목 | 상태 |
|------|------|
| 배포 URL | HTTP 200 정상 (culture-reed-dee-rug.trycloudflare.com) |
| 최신 커밋 | `d7e899b` (WORKFLOW.md 업데이트) |
| 버그 | 위 9건 모두 수정 완료 |
| 로컬 assets/ | 사진 163장 보유 (git 제외, 배포 서버에만) |

---

### 다음 세션에서 우선 확인할 것

1. **Kakao REST API 키 평문 노출** — `tools/fix_coords.py:6`, `fix_all_coords.py:7`, `geocode_jebu.py:6`, `WORKFLOW.md 섹션 8` 에 `1bd845da5756d1c78955463b800731ef` 하드코딩. 공개 저장소이므로 폐기·재발급 권고.
2. **시연대본 오류 3곳** — ① 숙박·캠핑 위치 오기(생활탭 X → 관광탭) ② AI 추천 퀴즈 언급 0건 ③ "별점은 공식 데이터 기준" 오기(알고리즘 생성값)
3. **제부도 물때 정보 미연동** — 앱 최고 평점 관광지(4.9)인데 바닷길 시간표 xlsx가 앱에 없음

---

*최종 업데이트: 2026년 8월 24일 (Session 5)*

---

## 17. 업무 도메인 분담 (사용자 지정 · 2026-08-24)

> §2 「역할 분리」는 **실행 환경** 기준(누가 서버를 돌리고 누가 사진을 보관하는가)입니다.
> 이 절은 그와 별개로, 사용자가 **어떤 종류의 일을 누구에게 시킬지**를 정한 것입니다.

### 기본 담당

| 담당 | 맡는 일 | 구체적으로 |
|------|---------|-----------|
| **배포 Claude** | **데이터** — 지도 핀에 찍히는 모든 것 | 관광지·축제·주차장·편의정보·지역화폐 항목의 추가/수정/삭제, 좌표 보정과 지오코딩, 태그·별점·설명 텍스트, `assets/images/places/` 장소 사진 |
| **개발 Claude** | **구조와 코드** — 웹페이지가 어떻게 돌아가는가 | HTML/CSS/JS 파일 구조와 분리, 화면 전환·렌더링 로직, 이벤트 처리, 성능·캐싱, 배포 구조, 리팩터링 |

주로 건드리는 파일로 보면 이렇게 갈립니다.

| 담당 | 파일 |
|------|------|
| 배포 Claude | `js/data.js` · `js/convenience.js` · `js/parking-static.json` · `js/localcurrency-static.json` · `assets/` · `tools/` 의 지오코딩 스크립트 |
| 개발 Claude | `index.html` (CSS·JS 전부 인라인 — 별도 `css/` 디렉토리 없음) · `js/map.js` · `js/conv_map.js` · `js/parking.js` · `js/localcurrency.js` · `tools/server.py` |

### 이 분담은 배타적이지 않습니다

**반대편 일을 못 한다는 뜻이 아닙니다.** 사용자가 지시하면 어느 쪽이든 상대 영역의 작업을 수행합니다.
예를 들어 사용자가 배포 Claude에게 웹페이지 코딩을 맡길 수 있고, 개발 Claude에게 데이터 보정을 맡길 수 있습니다.
**사용자 지시가 이 표보다 항상 우선합니다.**

기본 담당은 "따로 지시가 없을 때의 기본값"이자 "각자 더 잘하는 영역"일 뿐입니다.

### 왜 이렇게 나눴는가

1. **충돌 방지** — 커밋 200개 중 132개(66%)가 `index.html`을 건드렸습니다. 둘이 같은 파일에 몰리면 rebase 충돌이 구조적으로 반복됩니다. 담당을 파일 단위로 갈라두면 그 확률이 떨어집니다.
2. **자산 위치** — 사진은 `assets/`가 git 추적 제외라 **배포 서버에만 존재**합니다(§11). 개발 Claude는 사진을 넣을 방법이 없습니다.
3. **검증 환경** — 배포 Claude만 실제 앱을 띄워 핀 위치를 눈으로 확인할 수 있습니다. 좌표 검증은 그쪽이 유리합니다.

### 담당 밖 문제를 발견했을 때

고치지 말고 **보고하고 넘깁니다.** 사용자가 판단해 상대에게 전달하거나, 직접 하라고 지시합니다.
인계할 때는 상대가 바로 작업할 수 있도록 **구체적인 대상**을 함께 적습니다.

- 데이터 문제를 개발 Claude가 발견 → `js/data.js`의 어떤 `id`, 어떤 필드가 왜 틀렸는지
- 코드 문제를 배포 Claude가 발견 → 어떤 파일 몇 줄에서 어떤 증상이 나는지

### 개발 Claude가 데이터를 건드릴 때의 제약 (중요)

관광지 사진은 `assets/images/places/{name}.jpg` 파일명이 `data.js`의 `name` 필드와 **정확히 일치**해야 뜹니다.
경로를 조립하는 곳은 아래 4곳이며, 전부 가공 없이 문자열을 그대로 이어붙입니다.

| 파일 | 함수 | 용도 |
|------|------|------|
| `js/map.js` | `placePhotoHtml()` | 지도 슬라이드 카드 사진 |
| `index.html` | `_renderQuizResult()` | AI 추천 결과 카드 |
| `index.html` | `renderNearbyResult()` | 홈 '내 주변' 카드 |
| `index.html` | `itemHtml()` (`renderTourismList` 내부) | 관광 탭 목록 썸네일 |

> 행번호는 코드 수정 때마다 밀리므로 함수명으로 찾으십시오. `grep -n "assets/images/places" index.html js/*.js` 로 한 번에 확인됩니다.

따라서 개발 Claude가 `name`을 **추가하거나 수정하면 그 사진은 개발 쪽에서 넣을 수 없습니다.**
이 경우 커밋 메시지에 **필요한 정확한 파일명**을 남겨 배포 Claude가 바로 처리할 수 있게 합니다.

### 배포 Claude 인계 대기 — 좌표·데이터 확정 건 (개발 Claude 조사, 2026-08-24)

Kakao Local API 5종(keyword / address / coord2address / coord2regioncode / category) 교차검증 + 반증 전담 검토를 거친 결과입니다.
**§13 의 해당 항목보다 이 표가 우선합니다.** §13 은 어느 쪽이 틀렸는지 특정하지 못한 상태라, 그대로 고치면 오히려 악화되는 건이 있습니다.

| 대상 | 판정 | 조치 |
|------|------|------|
| `id:41` 화성예술의전당 | **좌표 정상 · 주소 오류** | 좌표(`37.1926217/127.086379`)는 Kakao POI 값과 소수점 7자리까지 일치. `address` 만 `"경기도 화성시 동탄구 노작로 11-1"` 로 교체. **좌표를 건드리면 안 됨** |
| `id:102` 화성송산포도축제 | **좌표·주소·날짜 전부 오류** | 공식 홈페이지 기준 실제 기간은 **2026-09-05 ~ 09-06** (현재 `2026-09-07` 은 시작일도 종료일도 아님). 주행사장은 화성에코팜테마파크 `서신면 영종로안길 24` → `37.1201662/126.6935902`. `desc` 요일이 2025년 달력과 일치해 작년 일정 잔존. `"(부행사장)동탄호수공원"` 은 2025년 정보이므로 삭제 |
| `id:229` 고운염색 | **좌표·주소 오류** | 현재 좌표는 화성시청 폴백. 원본(`tour.hscity.go.kr` 체험지 상세)에 `송산면 공룡로264번길 36 (쌍정농장 옆 가정집 1층)` 명시 → `37.2324918/126.7344852` |
| `id:173` 제부랜드 | **주소 정상 · 좌표 오류(591m)** | 리 단위 주소 지오코딩 값을 복사해 임야 중심점에 찍혀 있음 → `37.1657501/126.6182587` (제부리 190-325) |
| `id:73` 송라 마을 / `id:231` 송라효소 체험마을 | **동일 장소 중복 등록** | 별개 좌표를 만들지 말 것. 한쪽을 삭제하고 남기는 쪽 `address` 를 `"경기 화성시 매송면 매송북길64번길 152-11"` 로. 정부 「농촌체험휴양마을 현황」에서 동일 마을 확인 |
| `id:215` 말달리자 승마클럽 | **실존 확인 불가** | 검색어 11종 + 화성시청 반경 20km 승마시설 35곳 전수 확인했으나 없음. 현재 좌표는 화성시청 폴백. 원본 출처에서 정식 명칭·폐업 여부 재확인 필요 |
| `convenience.js` 작은섬 1·2 / 양지팬션·리조텔 | **정상 — 수정 금지** | 좌표뿐 아니라 **주소도 동일한 같은 부지**임. 제부도 숙박 65건 전부가 도로명주소 중심점을 쓰는 일관된 규약이라, 오프셋을 주면 근거 없는 좌표를 만드는 셈. 핀 겹침은 마커 병합으로 풀 것 |

**화성시 밖으로 찍힌 좌표 3건** — 권역 필터 도입 시 어느 구에도 안 잡혀 사라지므로 선행 수정 필요

| 대상 | 현재 좌표가 가리키는 곳 |
|------|------------------------|
| 2026 병점문화축제 | 오산시 세마동 (병점은 화성시 병점구) |
| 2026 화성시민 한마음 체육대회 | 오산시 세마동 |
| 제부리 마을 | 안산시 단원구 대부동 (제부리는 화성시 서신면) |

**§13 정정** — "미정 축제 23개" 는 실제로 **축제 21건 + 관광지 2건**(`id:215`·`id:229`)이며,
그 placeholder 좌표 `37.1995372 / 126.8314774` 의 정체는 **화성시청**(남양읍 시청로 159)입니다.

---

## 18. 외부 데이터 반입 파이프라인 (배포 Claude 담당 · 2026-08-25)

> 사용자가 `/home/jovyan/work/` 에 관광지·위치 관련 원본 파일(주로 CSV)을 넣어 두면,
> **배포 Claude가 형식 확인 → 분류 → 지오코딩 → 기존 데이터와 대조 → 반영 → 원본 정리** 까지 전담합니다.

### 사용자가 지정한 절차

1. 사용자는 `/home/jovyan/work/` 에 파일을 그냥 넣어 둡니다. 형식은 CSV 가 기본이나 다른 형식일 수 있으므로 **배포 Claude가 직접 확인**합니다.
2. 배포 Claude가 파일을 읽어 **관광지 / 주차장 / 기타로 분류**합니다. 분류는 사용자에게 묻지 않고 판단합니다.
3. 필요하면 **여러 파일을 하나의 작업용 파일로 합쳐** 처리합니다.
4. 처리가 끝나면 **원본 파일은 삭제**해 `/home/jovyan/work/` 를 원래대로 비워 둡니다. (하위의 `hwaseong_AI/`·`logs/`·`.vscode/` 는 유지)
5. 최우선 목표는 **지오코딩으로 정확한 좌표를 얻어 지도에 정확히 찍는 것**입니다.

### 기존 데이터와 충돌할 때

| 데이터 | 원칙 |
|--------|------|
| **주차장** | **기존 데이터를 건드리지 않습니다.** `smartparking.hscity.go.kr` API 로 받은 값이 더 정확합니다 |
| **지역화폐 가맹점** | 동일 — API 수집분이 우선 |
| **그 외(관광지 등)** | 자동 병합 금지. **한 건씩 직접 확인**한 뒤 반영 |

> 원칙: **API 로 수집한 데이터가 수기·배포 자료보다 정확하다.** 충돌 시 API 값을 신뢰합니다.

### 사용 가능한 도구 — `tools/geocode.py`

CSV / Excel(`.xlsx`) / JSON 을 받아 주소·이름 컬럼을 자동 인식하고 Kakao 주소 API 로 지오코딩한 뒤 `js/data.js` 에 추가합니다.

```bash
export KAKAO_REST_KEY="1bd845da5756d1c78955463b800731ef"
python tools/geocode.py <파일> --category tourist
```

자동 인식 컬럼 — 주소: `주소`·`도로명주소`·`지번주소`·`소재지`·`위치`·`address` / 이름: `명칭`·`시설명`·`장소명`·`상호명`·`축제명`·`name`

### ⚠ 이 스크립트를 그대로 돌리면 안 되는 이유 (2026-08-25 점검)

| # | 문제 | 근거 | 조치 |
|---|------|------|------|
| 1 | **ID 충돌** | `next_id = 100` 하드코딩. 기존 PLACES 가 `id:1~233` 을 쓰므로 100~233 구간과 정면 충돌. §6 BUG-1 이 정확히 이 사고였음 | 실행 전 `max(기존 id)+1` 부터 시작하도록 수정. **현재 안전 시작 번호 = 234** |
| 2 | **지오코딩 실패율 높음** | `address.json` 만 사용. 시설명(`궁평항`)·광역 주소(`경기도 화성시`)는 실패. Session 3 실측 실패 58/207 = **28%** | `fix_all_coords.py` 처럼 `keyword.json` 폴백 추가 |
| 3 | **중복 검사 없음** | 기존 데이터와 겹쳐도 무조건 `append` | 이름·좌표 근접(약 50m) 기준 중복 검사 후 보고 |
| 4 | **화성시 범위 검증 없음** | 타 시군 좌표를 그대로 등록. 실제로 기존 데이터에 오산시·안산시로 찍힌 3건 존재(§17) | `coord2regioncode` 로 화성시 여부 확인 후 등록 |
| 5 | **`openpyxl` 미설치** | `.xlsx` 입력 시 즉시 실패 | `pip install openpyxl` |
| 6 | `parking` 카테고리 없음 | tourist / restaurant / festival / localcurrency 만 지원 | 주차장은 애초에 건드리지 않으므로 문제 없음 — 유입 시 분류 단계에서 제외 |

### 보조 스크립트

| 파일 | 용도 | 검색 방식 |
|------|------|-----------|
| `tools/geocode.py` | **신규 데이터 반입** (CSV/Excel/JSON → data.js) | 주소 |
| `tools/regeocode.py` | 기존 PLACES 좌표 일괄 재보정 (`--dry-run` 지원) | 주소 |
| `tools/fix_all_coords.py` | 좌표 집중·중복 항목 이름 기반 보정 | 키워드 |
| `tools/geocode_jebu.py` | 제부도 숙박 전용 (주소→키워드 2단 폴백) | 주소 + 키워드 |

> 지오코딩 정확도 참고: **주소 검색이 실패하면 키워드 검색으로 폴백**하는 `geocode_jebu.py` 방식이 성공률이 가장 높습니다. 신규 반입에도 이 2단 구조를 적용하십시오.

### 사진을 함께 받을 경우

`assets/images/places/{name}.jpg` — 파일명이 `data.js` 의 `name` 필드와 **정확히 일치**해야 표시됩니다(§17 참고).
`assets/` 는 git 추적 제외이므로 **배포 서버에만 보관**하고 절대 push 하지 않습니다.

---

*최종 업데이트: 2026년 8월 25일 (섹션 18 — 외부 데이터 반입 파이프라인 추가)*

---

## 19. 무결성 검사 `tools/check.sh` (개발 Claude 담당 · 2026-08-25)

> **한눈에** — 개발 Claude가 2026-08-25에 작업한 내용입니다. 커밋 `9fae4be`.
>
> | 무엇을 | 어디에 | 왜 |
> |---|---|---|
> | 무결성 검사 5종 신설 | `tools/check*.{sh,py,js}` (신규 998줄) | push 가 곧 실서비스인데 검사 수단이 0개였음 |
> | 실서비스 공개 범위 제한 | `tools/server.py` | 저장소 루트가 통째로 서빙되고 있었음 — **보안 문제는 아님으로 결론**, 예방 차원 (§19-1) |
> | 데이터 JSON 캐시 버스팅 | `index.html` · `js/parking.js` | 27,505건이 재방문자 캐시에 막히고 있었음 (§19-2) |
>
> **기존 데이터는 한 건도 수정하지 않았습니다.** 작업 전후 모두 28,142건 그대로입니다.
> 검사가 찾아낸 데이터 영역 결함 1건은 고치지 않고 §19-3 으로 배포 Claude에게 넘겼습니다(§17 절차).

### 왜 만들었는가

이 저장소는 **push = 즉시 실서비스**입니다. Cloudflare 터널이 로컬 파일을 그대로 서빙하고, 빌드도 CI도 사람이 거치는 단계도 없었습니다.
그리고 **실패가 전부 조용합니다.** `index.html:3607` 등의 빈 `catch` 가 "데이터가 통째로 깨짐"을 "목록이 비어 있음"으로 번역하기 때문에, 27,374건이 0건이 되어도 화면은 로딩 중처럼 보입니다.

결정적으로 — `js/data.js` 에서 관광지 10개를 지워도 `node --check` 는 통과합니다. **레코드 삭제는 문법 오류가 아니라서 어떤 린트로도 못 잡습니다.** 오직 건수 기준선 대조만 잡습니다.

### 실행

```bash
bash tools/check.sh              # 기본 (약 1.3초, 네트워크 불필요)
bash tools/check.sh --precommit  # + 커밋 직전에만 의미 있는 검사
bash tools/check.sh --live       # + 실서비스 URL 과 대조 (네트워크 필요)
```

저장소에 아무것도 쓰지 않습니다. 전부 읽기 전용입니다.

### 파일

| 파일 | 필요 | 맡는 일 |
|------|------|---------|
| `tools/check.sh` | bash | 관문. `&&` 로 잇지 않고 **전부 돌린 뒤 모아서 보고** |
| `tools/check_data.py` | python3 | **1순위** — 레코드 수 하한, JSON 파싱(=절단 검증), 필수 필드·좌표, 화면에 손으로 적힌 숫자 대조 |
| `tools/check_markup.py` | python3 | `<style>` 중괄호, 태그 중첩, id 중복, `getElementById` 대상 부재 |
| `tools/check_code.js` | node | 인라인 JS 포함 문법, 전역 덮어쓰기, `let/const` 재선언, 죽은 `onclick` |
| `tools/check_cache.js` | node+git | `convenience.js` 를 고치고 `CONV_CACHE_VER` 를 안 올린 경우 |

### 설계 원칙

1. **`&&` 로 잇지 않습니다.** 관광지 3건·주차장 1건·제부도 47건이 같이 사라진 커밋에서 첫 실패에 멈추면, 사람은 눈에 보인 3건만 되돌리고 48건을 그대로 배포합니다.
2. **데이터는 하한(floor), 코드는 0 요구.** 데이터는 정당하게 늘어나므로 등호를 걸면 안 됩니다. 등호를 걸면 사람이 기준선을 습관적으로 덮어쓰게 되고, 그때 손실이 섞여 들어옵니다. 늘었을 때는 "기준선을 갱신하라"를 함께 찍습니다.
3. **알려진 결함은 0이 아니라 현재값을 상한으로** 박습니다. 상시 빨간불인 검사는 곧 무시되거나 지워집니다.
4. **건너뛸 때는 반드시 `SKIP` 을 찍습니다.** 조용히 통과시키지 않습니다.

### 기준선 (사람이 갱신하는 곳)

`tools/check_data.py` 의 `FLOOR` — 관광지 159 / 축제 48 / 주차장 131 / 지역화폐 27,374 / 모범음식점 94 / 관광식당 35 / 관광호텔 10 / 캠핑장 17 / 제부도숙박 115.
**데이터를 정당하게 늘렸으면 이 상수를 함께 올리십시오.** 안 올리면 다음 사람이 "늘었다" 안내를 계속 봅니다.

### 실패를 주입해 검증했습니다 (5/5 통과)

「통과만 하는 시험은 시험이 아니다」— `/tmp` 사본에 버그를 실제로 넣고 잡히는지 확인했습니다.

| 주입한 결함 | 결과 |
|---|---|
| 관광지 3건 삭제 | `FAIL PLACES.tourist 기준선 159 → 지금 156 (3건 사라졌다)` |
| `localcurrency-static.json` 을 4.2MB→2MB 절단 | `FAIL JSON 을 읽을 수 없다 (UnicodeDecodeError) — 파일이 잘렸거나 깨졌다` |
| `goMapCat` → `goMapCategory` 개명 | `FAIL 죽은 버튼 goMapCat() 가 선언돼 있지 않다 ← index.html:1743,1771,2663` |
| CSS 중괄호 깨뜨림 | `FAIL index.html:12 <style> 중괄호 불균형 +1 — 이후 CSS 규칙이 전부 무시된다` |
| 원복 | 전부 통과 (rc=0) |

### 이 검사가 못 잡는 것

- 브라우저에서만 나는 런타임 오류 (실제로 눌러봐야 압니다)
- 좌표가 형식상 정상인데 **엉뚱한 장소**를 가리키는 것 — §18 인계 표가 그 자리입니다
- 배포 서버의 `assets/` 사진 — 이 워킹트리엔 사진이 없어 `SKIP` 됩니다. **배포 서버에서 돌리면 사진-이름 연결까지 검사합니다.**

---

## 19-1. 실서비스 공개 범위 제한 `tools/server.py` (개발 Claude 담당 · 2026-08-25)

> ## 결론: 보안 문제 아님 — 재시작 급하지 않음
>
> **사용자(문형민) 판단 (2026-08-25):** 아래 노출은 **우려 대상이 아닙니다.**
>
> | 근거 | 내용 |
> |------|------|
> | 데모 프로젝트 | 해커톤 출품·시연용이며 운영 서비스가 아님 |
> | 전부 공공데이터 | 관광지·축제·주차장·지역화폐 모두 화성시·한국관광공사 공개 데이터. 감출 것이 없음 |
> | API 키에 만료일 설정됨 | Kakao REST 키는 무료 키이고 사용자가 **만료일을 지정해 둠**. 유출되어도 피해가 제한됨 |
>
> 따라서 **`tools/server.py` 의 접근 제한은 예방 차원으로만 남겨 둡니다.**
> Flask 를 재시작하지 않아도 되고, 급히 조치할 항목이 아닙니다.
> 다음에 Flask 를 재시작할 일이 생기면 그때 자연히 적용됩니다.
>
> 이 판단을 뒤집을 조건은 하나뿐입니다 — **운영 서비스로 전환하거나, 만료일 없는 키·개인정보를 저장소에 넣게 될 때.** 그때 이 절을 다시 읽으십시오.
>
> 아래 기록은 "무엇이 어떻게 돼 있었는지"를 남기기 위한 것이지 조치 요구가 아닙니다.

### 무엇이 노출돼 있었는가 (사실 기록)

`tools/server.py` 의 `Flask(__name__, static_folder=ROOT, static_url_path="")` 가 **저장소 루트를 통째로** 정적 서빙하고 있었습니다. 2026-08-25 실측 결과 배포 URL에서 아래가 전부 `HTTP 200` 이었습니다.

| 경로 | 크기 | 내용 |
|------|------|------|
| `/.git/logs/HEAD` | 43KB | 전체 reflog — 커밋 SHA 전량 |
| `/.git/config` | 312B | 커밋 계정 이메일 |
| `/WORKFLOW.md` | 41KB | **이 문서 전체** (내부 개발 기록·미해결 항목) |
| `/tools/fix_coords.py` | 2.4KB | Kakao REST 키 평문 |

### 조치

`before_request` 훅으로 **허용 목록** 방식을 적용했습니다. 거부 목록이 아니라 허용 목록이라, 루트에 새 파일이 생겨도 기본이 비공개입니다.

- 공개: `js/` · `img/` · `assets/` · `css/` · `index.html` · `favicon.ico` · `/api/*`
- 그 외 전부 404, `..` 포함 경로도 404

앱이 실제로 요청하는 정적 경로가 `js/`·`img/`·`assets/` 뿐임을 코드 전수 확인 후 정했습니다.

### 검증 (로컬 8099 포트)

정상 경로 5종 전부 200(`/`, `/index.html`, `/js/data.js`, `/js/parking-static.json?v=`, `/img/logo-icon.png`), 민감 경로 8종 전부 404, `/api/parking/realtime` 200 정상.

> **배포 Claude에게**: 이 변경은 Flask 재시작 시점에 적용됩니다. **일부러 재시작할 필요는 없습니다**(위 결론 참고).
> 다만 어떤 이유로든 재시작할 때, 아래 「주소가 바뀔까 봐」 항목은 반드시 읽으십시오 — 여기서 `cloudflared` 를 잘못 끄면 배포 주소가 영구히 사라집니다.

#### 주소가 바뀔까 봐 재시작이 걱정된다면 — 안 바뀝니다 (재시작할 때만 해당)

**재시작해야 하는 것은 Flask(`tools/server.py`) 뿐이고, `cloudflared` 는 건드리지 않습니다.**

```
cloudflared  ──터널──▶  localhost:8080  ──▶  Flask (tools/server.py)
   ↑ 이건 계속 켜둔다              ↑ 이것만 껐다 켠다
```

Quick Tunnel 의 `*.trycloudflare.com` 주소는 **`cloudflared` 프로세스에 붙어 있습니다.** `cloudflared` 가 살아 있으면 주소는 그대로입니다.
Flask 만 껐다 켜면 그 몇 초 동안 502 가 뜨고 곧 정상으로 돌아옵니다. **주소는 유지됩니다.**

반대로 `cloudflared` 를 끄면 그 순간 주소가 영구히 사라지고 새 랜덤 주소가 발급됩니다 — README 배지·발표자료·제출물의 링크가 전부 죽습니다. **`cloudflared` 는 절대 끄지 마십시오.**

```bash
# 올바른 절차 (배포 서버에서)
pkill -f "tools/server.py"          # Flask 만 종료 — cloudflared 는 그대로 둔다
python3 tools/server.py --port 8080 &
```

#### 적용 확인

```bash
curl -o /dev/null -w '%{http_code}\n' <배포URL>/WORKFLOW.md   # 404 여야 정상
curl -o /dev/null -w '%{http_code}\n' <배포URL>/js/data.js    # 200 이어야 정상
```

---

## 19-2. 캐시 버스팅 확대 (개발 Claude 담당 · 2026-08-25)

`index.html:3891` 의 `?v=` 규율이 `<script src>` 6개에만 적용돼 있었고, **정작 데이터가 든 JSON 2개는 보호를 못 받고 있었습니다.**
`fetch('js/parking-static.json')` 3곳(`index.html:2333·3878`, `js/parking.js:50`)과 `fetch('js/localcurrency-static.json')` 1곳(`index.html:3599`)에 `?v=` 를 붙였습니다. 합계 27,505건이 재방문자 캐시에 막히던 문제입니다.

**데이터를 갱신하면 `?v=` 날짜도 함께 올려야 합니다.** 안 올리면 배포는 성공하는데 재방문자 화면만 옛 데이터입니다 — "내 화면에선 되는데"가 성립해 디버깅이 가장 오래 걸립니다.

---

## 19-3. 배포 Claude 인계 — 데이터 도구 결함 1건 (개발 Claude가 발견 · 2026-08-25) → **처리 완료**

검사가 찾아낸 항목인데 **데이터 영역이라 개발 Claude가 손대지 않았습니다**(§17 담당 밖 인계 절차).

| 대상 | 문제 |
|------|------|
| `tools/geocode_jebu.py` 의 `ITEM_RE` | `jebu` 섹션 밖에서 **92건이 매치**됩니다(`restaurants`). 이 스크립트를 그대로 실행하면 **모범음식점 92곳에 제부도 좌표가 박힙니다.** 파일 전체가 아니라 `jebu` 섹션 범위만 스캔하도록 고쳐야 합니다. |

### 배포 Claude 처리 결과 (2026-08-25)

인계받아 고쳤습니다. 확인해 보니 **인계 노트보다 상태가 나빴습니다.**

`jebu` 섹션 **안쪽 매치는 0건**이었습니다. 제부도 115건은 이미 `lat`/`lng` 가 채워져 있어
`{name:"…", addr:"…"}` 패턴에 걸리지 않기 때문입니다.
즉 이 스크립트를 실행하면 **제부도는 한 건도 처리하지 못하고 모범음식점 92곳만 오염**시키는 상태였습니다.
"92건이 곁다리로 걸린다"가 아니라 **매치 전량이 오폭**이었습니다.

두 번째 결함도 같이 나왔습니다. `JEBU_LAT, JEBU_LNG = 37.1578, 126.5764` 는
`coord2regioncode` 확인 결과 제부도가 아니라 **안산시 단원구 대부남동**입니다.
반경 필터(±0.12°)의 중심이 3.8km 어긋난 채 동작하고 있었습니다.

| 조치 | 내용 |
|------|------|
| 범위 제한 | `jebu_span()` 추가 — `jebu` 섹션의 `[시작, 끝)` 오프셋 안에 있는 매치만 대상. 섹션을 못 찾으면 즉시 종료 |
| 상수 교정 | `JEBU_LAT/LNG` → `37.1696176, 126.6228376` (서신면 제부리, `data.js` id:1 기준) |
| 안전장치 | **기본 dry-run.** 파일을 쓰려면 `--apply` 명시 필요. 대상 0건이면 그대로 종료 |
| 검사 갱신 | `check_data.py` 가 원시 매치 수 대신 **범위 가드 존재 여부**를 확인하도록 변경. 가드가 사라지면 `FAIL` |

> `js/conv_map.js:65` 에도 같은 상수가 있지만 그쪽은 **제부도 지도 중심(fitBounds 폴백)** 용도이므로 건드리지 않았습니다.

실행 결과 — `대상 항목: 0개 (섹션 밖 92건은 제외)`, `js/convenience.js` 무변경.
`bash tools/check.sh` → **FAIL 0 / WARN 0** (기존 WARN 해소).

---

*최종 업데이트: 2026년 8월 25일 (섹션 19 — 무결성 검사 도입)*

---

## 20. 인라인 JS 분리 — `index.html` → `js/` 10개 파일 (개발 Claude 담당 · 2026-08-25)

> **한눈에** — `index.html` 안에 있던 JavaScript 2,084줄을 `js/` 아래 10개 파일로 옮겼습니다.
> **동작은 하나도 바뀌지 않았습니다.** 코드를 옮기기만 했고 로직 수정이 0줄입니다.
>
> | | 전 | 후 |
> |---|---:|---:|
> | `index.html` | 3,899줄 (186KB) | **1,827줄 (87KB)** |
> | `js/` 파일 수 | 6개 | **16개** |
> | 전역 이름 | 225개 | **225개** (변화 없음) |

### 왜 했는가 — 파일이 길어서가 아니다

**커밋 200개 중 132개(66%)가 `index.html` 을 건드렸습니다.** 개발 Claude와 배포 Claude 둘 다 만질 파일이 그것뿐이라 충돌이 구조적으로 반복됐고, 실제로 이력에 충돌·되돌리기 커밋이 2건 있습니다.

같은 해커톤의 `ldy1118-git/ai-hwaseong` 팀도 정확히 같은 이유로 파일을 쪼갰습니다 — *"`District.jsx` 는 원래 1,005줄에 세무와 상권이 같이 있었다. **두 사람이 같은 파일을 동시에 고치게 돼서 갈랐다.** 다시 합치지 말 것."* 크기가 아니라 **소유권**이 기준이었습니다.

### 파일 구성

분리 전 인라인 코드의 **실행 순서 그대로** 나열했습니다. 각 파일 머리에 「무엇을 맡나 / 왜 따로 있나 / 함께 볼 것」 세 줄을 적어 뒀습니다.

| 파일 | 줄수 | 맡는 일 |
|------|-----:|---------|
| `js/nav.js` | 60 | 화면 전환 `go()`, 사이드 메뉴 |
| `js/quiz.js` | 263 | AI 관광지 추천 퀴즈 5문항 |
| `js/mapnav.js` | 182 | 다른 탭 → 지도 진입점 (`goMapCat`/`goMapFocus`/`goConvItem`) |
| `js/home.js` | 594 | 홈 — 내 주변 추천·통합 검색·요약 카드 |
| `js/tourism.js` | 398 | 관광 탭 — 서브탭·목록·숙박/캠핑/템플스테이·축제 상세 |
| `js/living.js` | 146 | 생활 탭 — 카테고리 전환과 목록 |
| `js/favorites.js` | 157 | 즐겨찾기 (localStorage) |
| `js/ui.js` | 97 | 공용 — 토스트·공유·주소 복사·지역화폐 지연 로드 |
| `js/calendar.js` | 178 | 축제 달력 — 날짜 파싱·월별 렌더 |
| `js/boot.js` | 119 | **초기화 — 즉시 실행 코드는 전부 여기** |

### 지켜야 할 규칙 2가지

1. **`js/boot.js` 는 반드시 마지막에 로드합니다.** 즉시 실행되는 코드(전역 리스너·IIFE·`DOMContentLoaded`)를 전부 여기 모았습니다. 나머지 9개는 선언만 하므로 순서에 영향받지 않습니다.
2. **`type="module"` 을 붙이면 안 됩니다.** 모듈 스코프가 생겨 전역이 사라지고 `onclick` 122곳이 **전부** 죽습니다. classic script 를 유지하십시오.

`<script src>` 를 원래 인라인 블록이 있던 자리(`index.html:1807~1816`)에 그대로 넣었습니다. 위치를 옮기면 실행 순서가 바뀌므로 옮기지 마십시오.

### 어떻게 검증했는가

| 검증 | 결과 |
|------|------|
| 10개 파일에서 헤더를 떼고 이어붙이면 원본과 같은가 | **2,084줄 바이트 단위 일치** |
| 최상위 이름이 사라졌는가 | **0개** (분리 전 108개가 전부 살아 있음) |
| 전역 개수 | **225개 → 225개** (변화 없음) |
| 각 파일 문법 | `node --check` 10/10 통과 |
| 실제 서빙 | 16개 스크립트 전부 HTTP 200 로드 |
| `bash tools/check.sh` | 전 항목 통과 (FAIL 0 / WARN 0) |

### 분리하다가 검사 도구 결함 2건이 드러났다 — 함께 고쳤다

검사가 제 일을 했습니다. 앱 버그가 아니라 **검사기가 옛 구조를 가정하고 있던 것**입니다.

| 파일 | 결함 | 조치 |
|------|------|------|
| `tools/check_code.js` | `const JS = [...6개...]` 로 **파일 목록이 하드코딩**돼 있어 새 파일 10개를 통째로 못 봤다. "전역이 119개뿐" "`go()` 가 선언돼 있지 않다" 오탐이 났다 | `index.html` 의 `<script src>` 나열에서 **목록과 순서를 읽도록** 변경. 덤으로 "참조하는데 파일이 없다"·"디스크에 있는데 안 부른다" 두 검사가 새로 생겼다 |
| `tools/check_code.js` | HTML 주석 안의 `<script>` **글자**를 진짜 태그로 읽어 HTML 을 JS 로 검사하다 문법 오류를 냈다 | 주석을 먼저 공백으로 지운 뒤(개행 보존) 태그를 찾도록 변경 |
| `tools/check_data.py` | 화면 카드 라벨을 `index.html` 에서만 찾아, 그 마크업이 `js/` 로 옮겨가자 "마크업이 바뀌었다" 오탐 5건 | `index.html` + `js/*.js` 를 함께 훑도록 변경. 실패 메시지도 파일명을 찍는다 |

세 번째 결함은 잠복 버그도 하나 딸려 있었습니다 — 새로 넣은 코드가 `fail()` 선언보다 먼저 그것을 호출해서, **불일치가 실제로 나면 검사기가 보고 대신 죽을** 상태였습니다. 선언 순서를 바꿔 고쳤고, 없는 파일 참조와 고아 파일을 주입해 두 경우 모두 정상 보고되는 것을 확인했습니다.

### 앞으로 — 두 Claude의 파일 소유

이제 `index.html` 에는 HTML 마크업과 CSS 만 남았습니다. §17 의 담당 구분이 파일 단위로 훨씬 또렷해집니다.

- 화면 **동작**을 고친다 → `js/` 의 해당 기능 파일 (개발 Claude)
- 화면 **구조·모양**을 고친다 → `index.html` (개발 Claude)
- **데이터**를 고친다 → `js/data.js`, `js/convenience.js`, `js/*-static.json` (배포 Claude)

다음 후보는 CSS 분리입니다(`index.html:13~1322`, 1,310줄). 다만 `ilmeori`(38,608줄 프로젝트)조차 손으로 쓴 CSS 는 2개뿐이라, 잘게 쪼개지 말고 **3개 정도**가 적당합니다.

---

*최종 업데이트: 2026년 8월 25일 (섹션 20 — 인라인 JS 분리)*

---

## 21. CSS 분리 — `index.html` → `css/` 6개 파일 (개발 Claude 담당 · 2026-08-25)

> **한눈에** — §20(JS 분리)에 이어 `<style>` 1,310줄을 `css/` 아래 6개 파일로 옮겼습니다.
> **화면은 하나도 안 바뀝니다.** 브라우저가 받는 CSS 텍스트가 원본과 완전히 동일함을 실서빙으로 확인했습니다.
>
> | | 분리 전 (오늘 아침) | §20 후 | **지금** |
> |---|---:|---:|---:|
> | `index.html` | 3,899줄 · 186KB | 1,827줄 · 87KB | **525줄 · 27KB** |
> | `js/` | 6개 | 16개 | 16개 |
> | `css/` | 0개 | 0개 | **6개** |
>
> `index.html` 은 이제 **순수 HTML 마크업**만 남았습니다. 원래 크기의 **1/7**입니다.

### 파일 구성 — 번호가 곧 캐스케이드 순서

CSS 는 순서가 의미를 만듭니다. 그래서 파일명 앞에 번호를 붙여 **캐스케이드 순서를 파일명에 박았습니다.** `ilmeori` 저장소가 `supabase/migrations/` 를 `0001~0021` 로 관리하며 "기존 파일을 절대 고치지 않고 새 번호를 더한다"로 충돌을 없앤 것과 같은 원리입니다.

| 파일 | 줄수 | 맡는 일 |
|------|-----:|---------|
| `css/00-base.css` | 239 | 토큰(`:root`)·리셋·레이아웃 골격·헤더·검색·공통 컴포넌트 |
| `css/10-content.css` | 228 | 관광·생활 탭 콘텐츠 — 축제 스크롤·테마칩·축제 상세·캘린더·통계 |
| `css/20-map.css` | 274 | 지도 화면 — 필터칩·핀·마커·슬라이드 카드·GPS/줌 |
| `css/30-panel.css` | 214 | 사이드 드로어·관광 서브탭·숙박/캠핑/템플·통계 카드 |
| `css/40-quiz.css` | 236 | AI 추천 퀴즈 오버레이와 결과 화면 |
| `css/90-misc.css` | 119 | 메뉴 항목·토스트·ABOUT/데이터 출처 |

### 지켜야 할 규칙 3가지

1. **`<link>` 나열 순서를 바꾸지 마십시오.** 번호순 = 캐스케이드 순서입니다. 순서가 어긋나면 스타일이 조용히 뒤집힙니다.
2. **새 규칙은 「가장 가까운 번호대 파일」에 넣습니다.** 그래야 개발 Claude가 30번대를, 배포 Claude가 20번대를 만져도 git 충돌이 구조적으로 안 납니다.
3. **`90-misc` 앞의 50~80 은 비워 뒀습니다.** 새 화면이 생기면 그 사이에 넣으십시오. 기존 파일을 쪼개지 마십시오.

`:root` 변수는 `00-base.css` 에 있고 나머지 5개가 전부 그것을 전제로 합니다. **`00-base.css` 만은 반드시 첫 번째여야 합니다.**

### 왜 CSS 분리가 JS 분리보다 안전한가

CSS 는 스코프·호이스팅 같은 의미론이 없고 **캐스케이드가 순수하게 텍스트 순서**입니다. 따라서 「이어붙인 결과가 원본과 바이트 단위로 같고 `<link>` 순서가 같다」면 렌더링이 동일함이 **증명**됩니다. JS 는 실행 순서·전역 스코프 때문에 그만큼 단정할 수 없었습니다.

### 어떻게 검증했는가

| 검증 | 결과 |
|------|------|
| 6개에서 헤더 떼고 이어붙이면 원본과 같은가 | **1,310줄 바이트 단위 일치** |
| 분할 경계가 블록 한가운데를 자르지 않았는가 | 6개 경계 전부 **중괄호 깊이 0** |
| CSS 규칙 수 | **448개 → 448개** (변화 없음) |
| 요소 `id` 수 | **71개 → 71개** (변화 없음) |
| **실서빙된 CSS 를 순서대로 이어붙이면 원본과 같은가** | **완전 일치 — 브라우저가 보는 CSS 가 원본과 동일** |
| `bash tools/check.sh` | 전 항목 통과 |

### 또 검사 도구 결함이 드러났다 — 이번엔 더 위험한 종류

`tools/check_markup.py` 의 `<style>` 중괄호 검사가 **`<style>` 블록만 보고 있었습니다.** CSS 를 밖으로 빼자 그 `for` 문이 한 번도 안 돌고 **아무 말 없이 통과**했습니다.

이건 오탐(false positive)이 아니라 **미탐(false negative)** 이라 훨씬 위험합니다. 오탐은 시끄러워서 눈에 띄지만, 미탐은 **검사가 죽은 줄도 모르고 초록불이 뜹니다.** §19 에 적어 둔 설계 원칙 「건너뛸 때는 반드시 SKIP 을 찍는다」를 검사기 스스로 어긴 셈입니다.

고친 내용:
- `index.html` 의 `<style>`(남아 있다면)과 `css/*.css` 를 **함께** 본다
- **볼 대상이 하나도 없으면 그 자체를 실패**로 만든다 — 다음에 스타일 위치가 또 바뀌면 조용히 통과하지 않고 소리를 낸다
- `<link>` 가 부르는 css 가 실제로 있는지, 디스크의 css 를 index.html 이 부르는지 양방향 대조를 추가

세 경우(중괄호 파손 / 고아 파일 / 없는 파일 참조)를 주입해 전부 잡히는 것을 확인했습니다.

> **교훈** — 구조를 바꾸면 검사 도구가 옛 구조를 가정하고 있는지 반드시 확인해야 합니다. §20 에서 2건, §21 에서 1건이 나왔습니다. **검사가 통과했다는 것과 검사가 일을 했다는 것은 다릅니다.**

### 구조 개편은 여기서 마칩니다

`index.html` 525줄은 순수 마크업이라 더 쪼갤 이유가 없습니다. 화면별 HTML 분리는 빌드 도구나 `fetch` 조립이 필요해 이 프로젝트에서는 **득보다 실**입니다(§20 에서 이미 판단).

---

*최종 업데이트: 2026년 8월 25일 (섹션 21 — CSS 분리, 구조 개편 완료)*

---

## 22. 오늘 변경 전체에 대한 반증 감사 (개발 Claude 담당 · 2026-08-25)

> **결론 한 줄** — **파손 0건.** 실제 Chromium 픽셀 비교까지 통과했다.
> 다만 개발 Claude가 §20·§21 에서 말한 **"동작 변경 0"은 리팩터링 2건에만 참**이고, 같은 묶음에 **진짜 동작 변경 2건**이 들어 있다. 아래에 전부 적는다.

### 왜 했는가

사용자가 배포 Claude 재가동 여부를 결정하기 전에 "정말 아무 문제 없나"를 확인해 달라고 요청했다. 개발 Claude 자신의 "괜찮다"는 주장을 검증 근거로 쓸 수 없으므로, **주장을 반증하는 방향**으로 독립 감사를 돌렸다. 4개 관점(JS 실행 의미론 / CSS 렌더링 / 실서비스 영향 / 데이터 무결성)이 각각 파손을 찾아 나섰고, 마지막에 판정자가 그 결과를 다시 검증했다.

### 감사 대상

기준점 `dc8cb70`(배포 Claude 마지막 커밋) → `1ffdf15`. 개발 Claude 커밋 7개.

### 검증된 것 — 파손 없음

| 검증 | 결과 |
|------|------|
| **실 Chromium 홈 화면 스크린샷** | **md5 완전 일치** |
| **computed style** | **698개 원소 × 55개 속성, diff 0줄** |
| **15단계 조작 후 최종 DOM** | **103,168자 완전 일치** |
| 런타임 에러 목록 | 구/신 완전 동일 (기존 4건은 전부 선행 결함) |
| JS 분리 충실성 | 분리 직전 대비 **2,084줄 바이트 일치** (차이는 머리 주석뿐) |
| CSS 분리 충실성 | 분리 직전 대비 **1,310줄 바이트 일치** |
| 데이터 파일 8종 | `dc8cb70` == `HEAD` **블롭 해시 동일** — 한 건도 안 건드렸다 |
| 경로 탈출 우회 9종 | `css/../WORKFLOW.md`·`%2e%2e`·`//WORKFLOW.md` 등 **전부 404** |

지도·생활 탭에서 처음 "차이"로 보고된 2건은 **오탐**으로 확정됐다. 통제실험 결과 **같은 버전을 두 번 돌려도 자기 자신과 달라진다** — 4.2MB 지역화폐 JSON의 비동기 로드 타이밍 아티팩트다.

### ⚠ 정정 — "동작 변경 0"은 사실이 아니었다

§20·§21 에 "동작 변경 0"이라고 적었는데, 그건 **JS/CSS 분리 두 커밋에만** 해당한다. 같은 날 묶음에 아래 2건이 있다.

| # | 무엇 | 안전한가 |
|---|------|---------|
| 1 | **`?v=` 캐시 버스팅** — 데이터 fetch 4곳(`js/parking.js:50`, `js/home.js:68`, `js/ui.js:83`, `js/boot.js:109`)의 URL 이 바뀌었다. 코드 이동이 아니다 | 안전. 서빙 본문 해시가 원본과 동일함을 확인 |
| 2 | **`tools/server.py` 접근 제한** — 허용목록 밖 404 | 안전하나 **Flask 재시작이 있어야 적용**된다 |

### ⚠ 또 하나 — "최적화"라는 표현도 정확하지 않다

**페이지 1회 로드당 요청이 7건 → 23건으로 늘었고, 전송량은 186,826B → 201,109B 로 늘었다.**

이전에는 `index.html` 하나가 CSS·JS 를 자기 안에 갖고 있어 첫 페인트에 추가 왕복이 **0회**였다. 지금은 **렌더 차단 CSS 6개를 더 받아야** 첫 화면이 나온다. Cloudflare Quick Tunnel 은 왕복 지연이 크므로 **시연 첫 화면 백지 시간이 늘어날 수 있다.**

즉 이번 분리는 **유지보수성·충돌 감소를 얻고 초기 로딩 속도를 조금 내준 거래**다. "최적화"가 아니라 "구조 개선"이 정확한 표현이다.

### ⚠ 실패 원자성이 사라졌다

전에는 `index.html` 이 오면 CSS·JS 가 반드시 함께 왔다. 지금은 **16개 독립 요청**이라 부분 실패가 가능하다.

- `js/tourism.js` 하나만 실패 → 나머지 9개는 정상 동작하고 **관광 탭 버튼만 조용히 죽는다**
- `css/20-map.css` 하나만 유실 → **지도 화면만 무너진다**

배포 후 브라우저 개발자도구 Network 탭에서 **404 가 0건인지** 한 번 확인하는 습관이 필요하다.

### 감사가 찾아낸 결함 2건 — 즉시 수정함

| 결함 | 조치 |
|------|------|
| **`css/20-map.css` 머리 주석이 거짓말이었다.** "선택자 규칙이 다른 화면과 완전히 분리돼 있다"고 적었으나, 실제로 271~284줄이 `.nav-item`·`.festival-card-big`·`.place-card-sm`·`.place-item`(전부 `00-base.css`)과 `.chip`·`.fd-cta-btn`(`10-content.css`) **6개를 덮어쓴다.** 특이성이 같아 승자가 오직 `<link>` 순서로 결정된다. 이 주석만 믿고 파일 순서를 바꾸면 눌림 애니메이션이 즉시 사라진다 | 주석을 사실대로 정정하고 **경고를 명시** |
| **`?v=` 검사가 css 를 아예 안 보고 있었다.** `tools/check.sh:165`·`tools/check_data.py:443` 의 정규식이 `src="js/[a-zA-Z_]*"` 라, 숫자·하이픈이 든 `00-base.css` 류는 **0개 매치**였다. css 6개는 `?v=` 갱신 누락을 잡을 수단이 없었다 | 정규식을 `(src\|href)="(js\|css)/..."` 로 확장 → **22개(js 16 + css 6) 전부 검사.** 실패 메시지도 `<link href=`/`<script src=` 를 구분하도록 정정. 양쪽 음성 테스트 통과 |

### 아무도 확인하지 못한 것 — 배포 Claude가 재개 후 확인할 것

1. **카카오 지도 내부** — SDK 도메인 화이트리스트 때문에 localhost 에서 로드 불가. 두 버전 모두 폴백 화면을 **동일하게** 표시하는 것까지만 확인했다. 마커·클러스터는 미검증. (완화 근거: `js/map.js`·`conv_map.js` 는 이번 커밋들에서 **블롭 해시가 안 바뀌었다**)
2. **`assets/images/places` 사진 163장** — 이 워킹트리엔 없어 사진↔이름 대조가 **SKIP** 됐다. **배포 서버에서 `bash tools/check.sh` 를 반드시 다시 돌려야 한다.** 「통과」가 아니라 「안 본 것」이다
3. **Cloudflare 엣지 캐시** 동작 — 실제 터널 URL 로 확인 못 했다
4. **모바일 실기기(iOS Safari)** — 터치 드래그·`safe-area-inset`·`env()`

### 배포 Claude 재개 체크리스트

1. `git pull`
2. **배포 서버에서 `bash tools/check.sh` 실행** — 사진 검사가 거기서만 돈다
3. 터널 URL 을 열어 **개발자도구 Network 탭에서 404 가 0건인지** 확인 (새 파일 16개)
4. **지도 탭을 실제로 눌러 볼 것** — 감사가 커버 못 한 유일한 기능 영역
5. Flask 재시작은 **선택** (§19-1 결론대로 보안 우려 없음). 재시작한다면 `cloudflared` 는 절대 끄지 말 것

### 앞으로의 규약

- `css/*.css` 를 고치면 `index.html` 의 `?v=` 를 **손으로** 올린다 (이제 검사가 잡는다)
- **`<link>` 6개의 순서는 고정이다.** `20-map.css` 가 앞 파일들을 덮어쓰는 구조이므로 순서를 바꾸면 스타일이 조용히 뒤집힌다

---

## 23. 사진 라벨링 · 데이터 분류 반입 (배포 Claude 담당 · 2026-08-25)

> 사용자 지시 2건을 구현했습니다. **기존 데이터·사진은 한 건도 바꾸지 않았습니다** (28,142건 그대로, 사진 159장 그대로).

### 23-1. 사진 파일명 — 지역 이름으로 라벨링

**새 규칙**  `assets/images/places/{읍면동}_{id}[_{메모}].{jpg|jpeg|png|webp}`

```
서신면_1_제부도.jpg      동탄6동_162_CGV동탄역.jpg      우정읍_199_매향항 낙조.jpg
```

**매칭에 쓰이는 토큰은 `{id}` 하나뿐입니다.** 앞의 지역명과 뒤의 메모는 사람이 읽고 정렬하기 위한 장식이며 코드가 대조하지 않습니다. **지역명을 틀리게 적어도 사진은 정상적으로 뜹니다.** 지역 설계와 사진 설계가 서로의 실패를 전파하지 않도록 일부러 느슨하게 묶었습니다.

**기존 `{name}.jpg` 도 계속 인식합니다.** 159장을 한꺼번에 개명할 필요가 없습니다 (기존 파일을 새 정규식에 넣었을 때 오탐 0건 실측).

왜 바꾸는가 — 현행은 `data.js` 의 `name` 과 글자 단위로 일치해야 합니다. tourist 159건 중 **50건(31.4%)** 이 공백·괄호·쉼표·마침표·언더스코어를 갖고 있어, 이름을 한 글자만 고치면 그 사진이 조용히 404 가 됩니다. 실제로 커밋 `c941986` 에서 이름을 바꾸며 사진도 손으로 같이 고쳐야 했습니다.

| 구성 | 파일 |
|------|------|
| 인덱스 생성기 | `tools/build_photo_index.py` — 배포 서버에서 실행 |
| 산출물 | `js/photos.js` — `PHOTO_INDEX.byId` / `.byName` |
| 조회 함수 | `js/ui.js` `placePhotoSrc()` — byId 우선, 없으면 byName, 없으면 `{name}.jpg` |
| 호출부 4곳 | `js/map.js:401` · `js/tourism.js:237` · `js/home.js:90` · `js/quiz.js:195` |

```bash
python3 tools/build_photo_index.py --list    # 사진 넣을 때 참고할 CSV (id·읍면동·권장 파일명)
python3 tools/build_photo_index.py           # 사진 추가 후 인덱스 재생성
```

> 사진을 추가하면 **`index.html` 의 `js/photos.js?v=` 날짜를 함께 올리십시오.**
> `placePhotoSrc()` 는 `PHOTO_INDEX` 의 하위 객체까지 가드합니다 — 인덱스 생성이 실패한 `photos.js` 가 배포돼도 슬라이드 카드가 백지가 되지 않게 하기 위해서입니다.

`tools/check_data.py` 의 `check_photos()` 도 신규 규칙을 인식하도록 고쳤습니다. 안 고치면 신규 형식 사진을 1장만 넣어도 즉시 `FAIL`(고아)이 됩니다. 같은 `id` 에 사진이 둘 이상이면 `FAIL` 합니다.

### 23-2. 유입 데이터 분류 — `tools/ingest.py`

```bash
python3 tools/ingest.py                    # /home/jovyan/work/ 스캔 후 보고
python3 tools/ingest.py --geocode          # 좌표까지 조회
```

**기본은 보고만 하고 아무것도 쓰지 않습니다.**

| 단계 | 내용 |
|------|------|
| 형식 | CSV·TSV·JSON·XLSX. 인코딩 `utf-8-sig`→`utf-8`→`cp949`→`euc-kr` 순 자동 판별 |
| 분류 | ① 값 집합(결정적) ② 주소 값 ③ 컬럼명 토큰 ④ 최소요건 순 |
| 이름 컬럼 | 후보 목록 + `~명`·`~이름` 일반 패턴 (`화장실명`·`도서관명` 등 기관별 변형 흡수) |
| 지오코딩 | **이름 키워드 우선.** 주소 검색은 실측 유효 정보율 10%(22건 중 19건이 행정구역 중심점 반환), 이름 검색은 70% |
| 화성시 검증 | 좌표 상자 + `address_name` 에 `'화성시'` **indexOf**. `=== '화성시'` 는 `"화성시 만세구"` 형태라 전건 실패 |
| 중복 | 이름 정규화(공백·괄호·특수문자 제거) 후 기존 데이터와 대조 → **건별 보고** |

분류 결과별 처리 — `parking`·`localcurrency` 는 **API 수집분이 정확하므로 덮어쓰지 않고 보고만** 합니다(§18). 기존 분류에 안 걸리면 `_review`(신규 유형 후보)로 보류하고 컬럼 목록과 함께 보고합니다. **자동으로 카테고리를 신설하지 않습니다** — 새 category 하나를 추가하려면 리팩터링 후 기준으로 PLACES형 13곳 / 편의정보형 16곳을 고쳐야 하기 때문입니다.

### 검증

가짜 CSV 4종을 주입해 확인했습니다.

| 입력 | 판정 |
|------|------|
| `관광지명`+`소재지도로명주소` | 관광지 · 이름/주소 컬럼 인식 · 지오코딩 exact 2/2 · **기존 중복 2건 검출**(용주사→id:25, 제부도→id:1) |
| `주차장명`+`주차구획수` | 공영주차장 → **보호 대상, 덮어쓰지 않음** |
| `야영장구분` 값이 `일반야영장`/`글램핑` | 캠핑장 (값 집합으로 판정) |
| `화장실명`+`개방시간` | 신규 유형 후보 → 보류·보고 |

사진은 신규 형식 1장을 실제로 주입해 `byId` 매칭과 `FAIL 0` 을 확인하고 원복했습니다.
`bash tools/check.sh` → **FAIL 0 / WARN 0**.

---

*최종 업데이트: 2026년 8월 25일 (섹션 23 — 사진 라벨링·데이터 분류 반입)*
