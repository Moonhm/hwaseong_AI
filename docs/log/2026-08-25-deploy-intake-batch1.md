# 반입 1차 — 화성시 공공데이터 8종 (444건 지오코딩)

> 배포 Claude · 2026-08-25
> 목록·상태는 `data/CATALOG.md`, 규칙은 `WORKFLOW.md` §18.

## 받은 것

사용자가 `/home/jovyan/work/` 에 CSV 8개(합 0.1MB, 전부 cp949)를 넣었습니다.
공공데이터포털 형식(`경기도 화성시_{주제}_{날짜}.csv`)입니다.

## 분류기가 실제 데이터에서 4건 틀렸습니다

시험용 가짜 CSV 로만 검증했던 게 드러났습니다.

| 파일 | 처음 판정 | 실제 | 원인 |
|------|-----------|------|------|
| 공영주차장 | `_review` | `parking` | 컬럼이 `구간이름/면수/장애면수/정기면수` — 내 단서 목록(`주차구획`·`주차장명`)에 없었음 |
| 관광편의시설업 | `localcurrency` | `touristfacility` | **`업종` 컬럼 하나로 지역화폐 판정** |
| 여행업 | `localcurrency` | `travel` | 동일 |
| 영화상영관 | `localcurrency` | `cinema` | 동일 |

`업종` 은 공공데이터에 흔한 컬럼명이라 단독 단서로 쓰면 안 됐습니다.
`가맹점명`·`카드가맹` 같은 좁은 단서로 바꾸고, **값 집합**으로 판정하도록 고쳤습니다
(`국내여행업`·`관광펜션업`·`영화상영관`·`국가지정문화재` 등).

새 분류 5종을 추가했습니다 — `heritage` · `cinema` · `touristfacility` · `travel` · `stats`.
`stats` 는 "이름·주소 컬럼이 없고 나머지가 숫자 위주" 로 판정합니다(집계표).

## 지오코딩 — 이번엔 주소가 주력

**주소 품질이 기존 데이터와 정반대였습니다.**

| | A등급(번지까지) |
|---|---|
| 이번 공공데이터 | **96~100%** |
| 기존 `js/data.js` | 4.8% |

그래서 `tools/preprocess.py` 는 **주소 A등급 비율을 재서 캐스케이드 방향을 파일마다 다르게** 잡습니다.
70% 이상이면 주소 우선, 아니면 이름 우선입니다.
같은 캐스케이드를 모든 데이터에 쓰면 한쪽이 반드시 나빠집니다.

결과 — **444건 중 434건(97.7%) 좌표 확보.**

| 분류 | 건수 | 주소검색 | 이름검색 | 원본좌표 | 실패 |
|------|-----:|---------:|---------:|---------:|-----:|
| parking | 137 | 1 | 5 | 131 | 0 |
| travel | 187 | 186 | 0 | 0 | 1 |
| restaurant | 95 | 85 | 10 | 0 | 0 |
| heritage | 93 | 81 | 3 | 0 | 9 |
| touristfacility | 56 | 53 | 3 | 0 | 0 |
| cinema | 13 | 13 | 0 | 0 | 0 |

주소 정제도 넣었습니다 — 말미 괄호, `, 1층`/`, A동`, 그리고 **2026 신설 구 표기**를 뗍니다.
Kakao 주소검색은 구 없이도 찾으므로 떼는 편이 매칭률이 높습니다.

## 기존 데이터와 대조

**모범음식점 — 가장 값어치 있는 발견**
신규 95건이 **좌표를 전부 갖고 있습니다.** 기존 `convenience.js` 의 모범음식점은 **좌표가 2/94** 뿐이라
92곳이 지도에 안 뜹니다. 이름이 90건 일치하므로 **좌표만 이식하면 즉시 해결**됩니다.

**지정문화재 93건** — 기존 PLACES 와 5건만 중복(융건릉·당성·공룡알화석산지·용주사).
**88곳이 신규 관광 자원**이고 국보·보물이 포함됩니다.

**관광편의시설업 56건** — 기존과 중복 0건. 관광펜션업·관광극장식당업·한옥체험업.

**공영주차장 137 vs 기존 131** — 보호 대상이라 덮어쓰지 않았습니다.
이름 일치 123 / 신규에만 14 / 기존에만 9.
좌표가 200m 이상 어긋나는 4건(구장리 396m · 동탄일반산업단지 304m · 여울공원 지하 549m · 여울공원 LH 471m)은
**건별 확인 대상**입니다. 신규 쪽에 `면수`·`장애면수`·`정기면수`·`재입장시간` 이 새로 들어옵니다(총 13,620면).

## 실패 9건은 데이터 문제

지정문화재만 실패했는데 전부 주소 자체가 불완전합니다.
`향남읍 토화길 70-1` 4건은 같은 주소인데 Kakao 미등록이고,
`승무(화성 이동안류)` 는 주소가 `미확인` 입니다 — 무형문화재라 위치 개념이 없습니다.

## 앱에는 아직 안 넣었습니다

`js/` 는 한 글자도 건드리지 않았습니다(28,142건 그대로). 전처리까지만 하고
어느 것을 어떻게 반영할지는 사용자 판단을 기다립니다. 우선순위는 `data/CATALOG.md` 에 적었습니다.
